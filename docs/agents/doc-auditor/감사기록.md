# doc-auditor 감사기록

`docs/agents/README.md`의 고정명 규약을 따른다. 감사 1회 = 절 1개, append-only.
**소득 0건도 기록한다** — "언제 돌았고 결과가 0건"이 이력이다.

---

## 2026-08-25 — 첫 기록된 감사 (BUG-03·FEAT-20·FEAT-19 사이클 완료 직후)

**트리거**: 메인 루프 디스패치(런북 9단계). 이 폴더가 없었으므로 저장소에 남은 이전 감사 기록은 없다.

**감사 범위**: TASK_BACKLOG(4건) · apps/backend/CLAUDE.md(주장 14 + 줄번호 인용 9) ·
apps/admin/CLAUDE.md(6) · 루트 CLAUDE.md(3) · docs/release-checks.md(16) ·
docs/plans/verification-paths.md(2) · README.md(3) · README.ko.md(2) — 전부 코드와 대조.

### 어긋남 5건

1. **[유령] `apps/backend/CLAUDE.md:217`** Known Issues "No error handling for failed S3 uploads"
   — BUG-03이 구현함(`main.py:728-742` 래퍼 + `:828-829`·`:843-844`·`:1065` 래핑, `s3_upload_policy.py`).
2. **[유령] `README.md:352`** "No error handling for S3 upload failures" — 동일.
3. **[유령] `README.ko.md:343`** "S3 업로드 실패에 대한 에러 핸들링이 없음" — 동일.
4. **[거짓] `README.ko.md:196`** "실패하거나 **일부만 완료된 실행은 차감하지 않습니다**"
   — 실제는 부분 실행도 생성 클립 수만큼 차감(`functions.ts:639-644`→`:691-695`→
   `entities/uploaded-file/api/index.ts:827` `decrementUserCreditsFloorZero(userId, clipsFound)`).
   영문 `README.md:199-200`은 정확해 **두 README가 서로도 어긋난다**. BUG-06(FAQ 카피)과 같은 부류.
5. **[거짓·줄번호] `apps/backend/CLAUDE.md`의 `main.py` 인용 9건** — BUG-03 편집(상단 import
   +약 10줄·`:700`대 헬퍼)으로 전부 밀림: `88-89→98-99`·`112-116→122-126`·`119-122→129-132`·
   `136→146`·`272-275→282-285`·`339→349`·`388-391→398-401`·`545→555`·`1040-1042→1107-1109`.
   값·식별자는 여전히 맞고 위치만 어긋남.

### 백로그 유효 확인

- **BUG-02 잔존**: `main.py:534-536` — 번역 예외 시 `korean_texts = english_texts` 조용한 폴백, 알림 없음.
- **BUG-04 잔존**: `main.py:1218-1221` — `finally:`에서 성공/실패 무관 `shutil.rmtree`.

### 미확인

- FEAT-01: "마무리"의 완료 기준이 백로그에 정의돼 있지 않아 판정 불가(차감 핵심 로직 자체는 구현돼 있음).
- BUG-07: 시각 렌더 주장이라 정적 코드 대조 대상 아님(실기기/스크린샷 영역).
- verification-paths.md의 실증 사례는 과거 라운드의 역사적 인용 — 재도출 대상 아님(구조 주장만 확인).

### 처리 상태

어긋남 5건 **처리 완료**(2026-08-25, 사용자 "고쳐" 승인 → 메인 루프 동기화 커밋 `eddfc23`). 처리 중 같은 부류 1건을 추가 발견해 함께 정리 — `README.md:354` "Credits not consumed if pipeline fails mid-process"(부분 실행 차감과 모순, ko:196과 동족).

---

## 2026-08-26 — 2차 감사 (BUG-02 웹 절반 FEAT-21 + 스키마 컬럼 + 핫픽스 사이클 완료 직후)

**트리거**: 메인 루프 디스패치(런북 9단계).

**감사 범위**: TASK_BACKLOG(4) · web CLAUDE.md(16) · backend CLAUDE.md(19 — 줄인용 9종 재검 포함) ·
admin CLAUDE.md(3) · schema.prisma(6) · 루트 CLAUDE.md(2) · release-checks(1).

### 어긋남 2건 (둘 다 실질 주장은 참, 줄인용만 낡음)

1. **[거짓·줄번호] `apps/web/CLAUDE.md:80`** — "`main.py:899`는 프롬프트의 요청일 뿐" →
   실제 :899는 `whisperx.align` 인자, 해당 프롬프트는 **`:987`**. (교차 워크스페이스 인용이라
   backend 재동기화 때 누락된 지점.)
2. **[거짓·줄번호] `schema.prisma:116-117` Clip.clipType 주석** — `main.py:1118-1120` →
   실제 **`:1182-1184`**(성공 콜백 POST는 `:1189-1196`), `(:149-151)` → ClipDraft 동명 필드는
   **`schema.prisma:162-164`**.

### 백로그 유효 확인

BUG-04(`main.py:1215-1218` rmtree)·BUG-08(`main.py:1209` clips:[])·FEAT-01(area 실재, 잔여 범위
미정의는 항목 성격)·BUG-07(`owner-banner.tsx:24-30,67-85` 고정 viewBox `0 0 660 96` + `w-full` —
진단 추정이 코드로 확정됨) 전부 잔존.

### 처리 상태

어긋남 2건 **처리 완료**(2026-08-26, 1차 감사에서 승인된 "줄번호 진실 동기화" 방침 승계 —
메인 루프 동기화 커밋, 스키마 주석은 클라이언트 재생성 포함).

---

## 2026-08-26 — 3차 감사 (FEAT-22 사이클 완료 직후)

**트리거**: 메인 루프 디스패치(런북 9단계, HEAD `84bd957`).

**감사 범위**: admin CLAUDE.md(13 — 인벤토리 27파일·60suite·281test, 계약행 2, 소유권 7) ·
TASK_BACKLOG(5) · release-checks FEAT-22 절(3) · verification-paths 구조(2) ·
README·README.ko 크레딧 카피(4) · 루트 CLAUDE.md 명단(2) · backend CLAUDE.md 인용(11, 부수).

### 어긋남 0건

**첫 무결 감사.** FEAT-22 동기화(인수 시 메인 루프가 CLAUDE.md 세 곳 + suite 실측치 정정)가
코드와 일치함을 독립 대조로 확인 — board GET owner(`queries.ts:18-45` 토큰/폴백 분기)·잠금 칩
리터럴(`transitions.ts:168`)·게이트대기 문구(`run-plan.ts:44`)·쓰기 2경로 전수.

### 의심 1건 (판정 불가)

- **`TASK_BACKLOG.md:20-22` FEAT-01** — "Credit System 마무리 (현재 개발 중 상태)"의 전제가
  코드로 뒷받침되지 않는다: 크레딧은 배선돼 동작하고(기본 3 `schema.prisma:49`·클립당 차감
  `uploaded-file/api/index.ts:827`·부족 가드·Polar 3경로), source가 가리키는 README
  "Currently in Development" 문구는 현재 README에 없다(두 README 모두 완성 기능으로 서술).
  다만 항목이 남은 "마무리" 범위를 특정하지 않아 유령/유효 단정 불가 — 사용자 결정 대상.

### 백로그 유효 확인

BUG-04(`main.py:1215-1218` finally rmtree)·BUG-08(`main.py:1209` clips:[] — `clip_results`
:1186 누적분 유실)·FEAT-23(스테퍼 부재·데이터원 `board.ts:82-84`·`:96-98` 실재)·
BUG-07(`owner-banner.tsx:25·29` 고정 viewBox+w-full) 전부 잔존.

### 처리 상태

어긋남 0건이라 수정 없음. FEAT-01 의심은 사용자에게 보고(범위 재정의 또는 항목 정리 결정 대기).

---

## 2026-08-28 — 4차 감사 (BUG-07·FEAT-23·FEAT-24 사이클 완료·배포 직후)

**트리거**: 메인 루프 디스패치(런북 9단계, HEAD `97ffffb` — FEAT-25 게이트① 개방 직후, FEAT-25 코드 0줄).

**감사 범위**: TASK_BACKLOG 잔존 6건(FEAT-25 진단 인용 4·FEAT-27 인용 5, 주장 15) ·
admin CLAUDE.md(인벤토리 28파일·68suite·307test, 표 28행↔파일집합, 라우트 8, DB 단일 findMany,
소유권 8, 인가 3층 — 주장 26) · `remote-agent-pipeline-generalization.md`(루틴 지침 접두·진행 코멘트
규약·읽기② (a)(b)(c)·알려진 약점·계약 사본 — 주장 7) · release-checks FEAT-23·24·BUG-07 절(클래스·
상수·문구 14) · verification-paths 구조(3) · 루트 CLAUDE.md 명단(2) · README 크레딧 카피(2) ·
backend CLAUDE.md(범위 밖 병행, 12).

### 어긋남 2건

- **`remote-agent-pipeline-generalization.md:89`** "화이트리스트 명령 세트 | 6키" ↔
  `apps/admin/src/fsd/features/run-pipeline-command/model/commands.ts:3-10` **7키**(`backend-work`가
  FEAT-18(08-24)에서 추가, `commands.test.mjs:61`이 덮는다). `:46`의 같은 값은 "검증된 참조 구현
  (2026-08-15 기준)" 머리말 아래 날짜 박힌 스냅샷이라 작성 시점 보존이 의도 — `:89` 파라미터 표만
  살아 있는 거짓 주장.
- **`TASK_BACKLOG.md:38`**(FEAT-27 설계 시 판단) "행 제거는 사용자 승인만 — `verification-paths.md:41`"
  ↔ 규칙은 `verification-paths.md:43`에 있다(`:41`은 다른 문장). 포인터 드리프트 2줄, 규칙 자체는 실재.

### 의심 0건

### 백로그 유효 확인

BUG-04(`main.py:1215-1218` finally rmtree)·BUG-08(`main.py:1198-1213` 포괄 except, `:1209` clips:[],
`:1186` clip_results 유실)·FEAT-01(area 3경로 실재)·FEAT-25(코드 0줄, 진단 인용 `config.ts:16`·`:22-24`·
`guard.ts:7-25`·`config.edge.ts:11` 전부 정확)·FEAT-26(`.mcp.json`·`release-verify` 부재 = 신설 대상,
원장 실재)·FEAT-27(`verify-fsd-boundaries.mjs:708`·`:722`, 셀프테스트 13, `plan-verifier.md:31·36·65-82`
정확) 전부 잔존·유효.

### 관찰 (어긋남 아님)

`정찰기록.md` 2차 제안 B(최종 Clip에 hook/payoff/clipType)의 "처리 상태: 미처리"는 보드 FEAT-16(완료,
08-20)과 `main.py:1182-1184`로 볼 때 실질 구현됨. `docs/agents/**`는 작성 시점 보존 문서라 감사 대상
밖(`docs/agents/README.md`) — 기록만.

### 미확인

제안서 Verification Results·Current State 표의 외부 시스템 값(트리거 ID·env ID·#87 시각)은 저장소로
대조 불가(문서 스스로 `:165·:188`에서 감사 불가 선언). `apps/web/CLAUDE.md`는 이번 트리거 범위 밖.

### 처리 상태

어긋남 2건 **처리 완료**(2026-08-28, 2차 감사에서 승계된 "줄번호 진실 동기화" 방침 — 메인 루프
동기화 커밋). `:89` 6키→7키, 백로그 `:41`→`:43`. `:46` 스냅샷은 보존. 관찰(제안 B)은 규약상 무수정.

---

## 2026-08-28 — 5차 감사 (FEAT-25 사이클 구현·인수 직후, PR #107 머지 전)

**트리거**: 메인 루프 디스패치(런북 9단계, HEAD `d710a10` dev).

**감사 범위**: admin CLAUDE.md(라우트 `/login`·인벤토리 29파일/75suite/334test·`config`·`guard`·`verifier` 행 문구·검증기 3층 문단 9점·env `VERIFIER_SECRET` — 주장 16) · release-checks FEAT-25 절(쿠키 2·에러 경로·렌더/폴백·쓰기 거부·1h — 6) · `docs/plans/FEAT-25.md` 「공개 계약」 ↔ CLAUDE.md 검증기 문단 사본 정합(5점) · `.env.example` ↔ `env.js`(1) · TASK_BACKLOG 잔존 5건 · verification-paths 구조(2) · README 크레딧 카피(3) · 루트 CLAUDE.md 로스터(1).

### 어긋남 0건

**두 번째 무결 감사.** 인벤토리 `334 test`는 리터럴 `it()`/`test()` 330건에 `journey.test.mjs:114-131`의 `PROGRESS_CASES` 루프 생성 5건을 반영하면 정확히 330−1+5=334, `75 suite`는 `describe(` 75건과 일치 — 계수 방식까지 독립 재현. 신규 주장 증거: 쿠키명 `@auth/core/lib/utils/cookie.js:49·:69`, 에러 경로 `errors.js:151`(`error=CredentialsSignin&code=credentials`), 검증기 문단 `verifier.ts:6,9,16-21,34-42`·`config.ts:20,24,30-31,37-53`·`guard.ts:6,19-30`·`next-auth.d.ts:1-8`·`admin-header/ui/index.tsx:1,7`·`env.js:37,64`, 쓰기 4곳 `post-pipeline-command.ts:27`·`commit-gate-transition.ts:98`·`:121`·`send-observability-test-event.ts:27`.

### 의심 1건 (지속 — 3·4차와 동일)

- **`TASK_BACKLOG.md:20-21` FEAT-01** "Credit System 마무리 (현재 개발 중 상태)" — area 3경로 실재하나 "마무리"의 잔여 범위 미정의, source의 README "Currently in Development" 문구는 grep 0건. 유령/유효 단정 불가 — 사용자 결정 대상.

### 백로그 유효 확인

BUG-04(`main.py:1215-1218`)·BUG-08(`:1198`·`:1209`·`:1186`)·FEAT-26(`.mcp.json`·`release-verify` 부재 = 신설 대상, 본문에 FEAT-25 이전 줄번호 인용 없음)·FEAT-27(`verify-fsd-boundaries.mjs:708`·`:722`, `scripts/verify-plan/` 부재, `verification-paths.md:43`) 전부 유효.

### 미확인

`apps/web/CLAUDE.md`·`apps/backend/CLAUDE.md`는 트리거 범위 밖(FEAT-25 변경이 admin에 국한). `docs/plans/FEAT-25.md` 「현재 동작」의 구현 전 줄번호는 계획서가 보존하는 작성 시점 스냅샷이라 대상 아님 — 라이브 계약인 「공개 계약」만 대조.

### 처리 상태

어긋남 0건이라 수정 없음. FEAT-01 의심은 사용자 결정 대기 유지.

---

## 2026-08-29 — 6차 감사 (FEAT-26 사이클 구현·인수 직후, PR #108 머지 전)

**트리거**: 메인 루프 디스패치(런북 9단계, HEAD `986f640` dev).

**감사 범위**: 원장 머리말 자동 판정 규칙 5점 ↔ `scripts/release-verify/ledger.mjs`(KNOWN_KEYS 7키 `:6`·pass 되쓰기 `:141`·`FAIL_NOTE_PREFIX` `:132`·괄호 상수 `:4-5`) · 태그 4줄의 리터럴 ↔ 소스(`journey-stepper.tsx:67·69·81`·`journey.ts:41-47`·`run-plan.ts:42·44`·`index.tsx:68·71`·Tailwind 클래스 `:7·10·11·20`, `notext="최대 5분"` 렌더 문자열 부재) · `SKILL.md` 5점 ↔ `run.mjs` CLI(옵션·종료코드 2·`login`/`parse` 필드) · 루트 CLAUDE.md 3 · package.json 2 · 제안서 release-verify 절 6 · `docs/plans/FEAT-26.md` 「고칠 파일」 9 실재 · admin CLAUDE.md 파일 수 29 · README 크레딧 2 · verification-paths 2.

### 어긋남 0건

**세 번째 무결 감사.** 원장 태그 값·규칙 문구가 스크립트 상수·코드 리터럴과 전부 일치.

### 의심 0건

### 백로그 유효 확인 — 그리고 메인 루프의 정정

감사자가 잔존을 **3건**(BUG-04·BUG-08·FEAT-01)으로 보고하며 "FEAT-27이 백로그에 없다"를 미확인으로 남겼다. 메인 루프가 대조한 결과 **`986f640`에서 FEAT-26 블록을 지운 정규식이 `re.S`로 줄을 넘어 탐욕 매치해 「Pipeline 운영 / 검증 하니스」 절·FEAT-27 블록·「비고」 제목까지 12줄을 삭제**했다(의도 4줄). 감사가 이 손실을 드러냈다 — 직전 커밋의 파일에서 FEAT-26 4줄만 인덱스로 제거하는 방식으로 복원(이 커밋). 잔존은 BUG-04·BUG-08·FEAT-01·FEAT-27 4건이 맞다.

### 미확인

루틴 지침·config는 저장소 밖(제안서 `:196` 자기 선언). 제안서 `:194` "매일 + PR 머지(best effort)" ↔ 원장 `:33` "미배선(사용자 결정)"은 외부 상태 진술이고 양쪽 hedge — 어긋남 아님. admin CLAUDE.md suite/test 계수·FEAT-01 완결 판정은 범위 밖.

### 처리 상태

어긋남 0건. 감사가 간접으로 드러낸 백로그 손실은 메인 루프가 복원(같은 커밋). FEAT-01 의심은 사용자 결정 대기 유지.

---

## 2026-08-29 — 7차 감사 (BUG-04·BUG-08 사이클 구현·인수 직후, PR #108 미머지·modal deploy 전)

**트리거**: 메인 루프 디스패치(런북 9단계, HEAD `0024133` dev).

**감사 범위**: backend CLAUDE.md(13) · README·README.ko Known Issues(4) · release-checks BUG-04·08 절(6) · `docs/plans/BUG-08.md` web 인용(8) · `main.py` 배선·`add_local_python_source` 4인자·순수 모듈 stdlib(3) · TASK_BACKLOG 잔존 2건(9) · admin CLAUDE.md·verification-paths·루트 CLAUDE.md 회귀.

### 어긋남 9건 — 전부 줄번호 드리프트

BUG-04(`temp_cleanup_policy` import 4줄)·BUG-08(`error_callback` import 1줄)이 `main.py` 상단에 5줄을 더해 아래 코드가 밀렸는데 `apps/backend/CLAUDE.md`의 `main.py:줄` 인용 9곳이 그대로였다: `:104-105`→`109-110`(MAX/MIN_CLIP_DURATION), `:1104-1106`→`1110-1112`·`1189-1191`(type/hook/payoff 소비), `:152`→`157`(`resolve_caption_style`), `:288-291`→`291-297`(영어 기본값), `:404-407`→`407-413`(한국어 기본값), `:355`→`360`(Anton), `:550`→`555`(Noto Sans KR), `:127-131`→`133-137`(`CAPTION_POSITION_ALIGNMENT`), `:134-137`→`140-143`(`CAPTION_POSITION_MARGINV`). 갱신된 Known Issues 문구·README 두 벌·원장 두 절·BUG-08 web 인용·순수 모듈 계약은 전부 일치.

### 의심 1건 (지속)

FEAT-01 — 3~7차 동일. 사용자 결정 대상.

### 백로그 유효 확인

FEAT-27 유효(`scripts/verify-plan` 부재 = 미구현 정상, 인용 `:29`·`:43`·`plan-verifier.md:31·36·65-82`·`verify-fsd-boundaries.mjs:708·722` 일치).

### 처리 상태

어긋남 9건 **처리 완료**(2026-08-29, 승계 방침 "줄번호 진실 동기화" — 메인 루프가 새 줄을 실측 재확인 후 치환, 같은 커밋). 교훈: 파일 상단 import 추가는 그 파일을 인용하는 워크스페이스 CLAUDE.md 줄번호 전부를 밀어낸다 — 인수 handoff에 "문구"뿐 아니라 "인용 줄번호 재대조"를 넣어야 한다(FEAT-10 ⑭와 같은 부류).

---

## 2026-09-03 — 클린코드 개선 77건 이후 문서 대조

**트리거**: 메인 루프 디스패치. 보드 항목이 아니라 사용자 직접 지시로 돈 `apps/web` 클린코드 리팩터(커밋 `9dd6dfb`~`a75dcd6`, 159파일) 직후, 그 변경이 거짓으로 만든 문서 주장을 찾기 위해.

**감사 범위**(호출자가 5개 문서로 명시): `apps/web/CLAUDE.md`(주장 11건) · `docs/conventions/fsd-architecture-guidelines.md`(2) · `docs/conventions/tanstack-query-fsd-guidelines.md`(2) · 루트 `CLAUDE.md`(대조 대상 없음 — 파이프라인 절차서라 `apps/web` 내부를 기술하지 않는다) · `docs/proposals/active/` 두 문서(22).

### 어긋남 8건

| # | 위치 | 내용 | 원인 |
| --- | --- | --- | --- |
| 1 | `apps/web/CLAUDE.md:123` | JWT 세션 전략 인용 `config.edge.ts:13` → 실제 `:23` | C-39가 `PROTECTED_ROUTES`/`AUTH_ROUTES` export와 JSDoc을 추가해 10줄 밀림 |
| 2 | `processing-concurrency-hardening-2026-06-30.md` 전체 | 본문이 "제안"으로 기술하는 항목이 **전부 이미 구현·배포**됐는데 frontmatter가 `status: pending`·`stage: draft` | 이전부터 있던 드리프트(이번 리팩터와 무관) |
| 3 | 같은 문서 `:183` | `completeUploadedFileProcessingAttempt`가 `entities/uploaded-file/api/index.ts`에 있다 → 실제 `features/upload/api/complete-processing-attempt.ts:15` | C-75(엔티티 간 오케스트레이션을 feature로 이동) |
| 4 | 같은 문서 `:112` | `updateUploadedFileStatus()` 호출자 확인 지시 → 그 함수가 `apps/web/src` 전체에 0건 | C-05(죽은 export 삭제). FEAT-29가 그 위에 붙였던 ⚠️ 불변식 주석보다 삭제가 강한 방어라 회귀는 아니다 |
| 5 | `fsd-architecture-compliance-proposal.md:974-977` | 「남음」의 크로스 슬라이스 세그먼트 직접 참조 3건이 전부 이미 해소 | C-07·C-06·C-08(barrel 런타임 분할과 깊은 임포트 되돌리기) |
| 6 | 같은 문서 `:985` | `SubscriptionStatus.tsx:15` → 실제 `:26`(나머지 두 건 `PlanCard.tsx:14`·`OrderHistory.tsx:16`은 정확) | C-71이 AlertDialog 임포트 블록을 추가해 밀림 |
| 7 | 같은 문서 `:962` | `features 8/9` → 실제 **9/9** | C-08이 `features/clip/index.ts` 생성 |
| 8 | 같은 문서 `:231`·`:423` | `UploadedFileCard.tsx:30`의 `usePlayUrl` 호출 → 실제 `:32`이고 반환도 구조분해가 아니라 `playUrlState` 판별 union | C-10(usePlayUrl union화)·C-66 |

여덟 건 중 **일곱 건(1·3·4·5·6·7·8)이 이번 리팩터가 만든 드리프트**다. 2번만 이전부터 있던 것이다.

### 의심 1건

`tanstack-query-fsd-guidelines.md:159,165` — §5 예시 코드가 `~/fsd/entities/uploaded-file/model/query-keys` 깊은 임포트와 `getUploadedFileDetails`를 쓴다. 전자는 C-07 이후 barrel로 대체 가능해졌고 후자는 C-28이 `reconcileAndGetUploadedFileDetails`로 개명해 이제 존재하지 않는다. 감사자가 "규범 예시인지 현재 발췌인지 문서가 표시하지 않는다"고 유보했으나, **문자 그대로 복사하면 즉시 깨진다**는 점은 확실하다고 판단해 처리 대상에 넣었다.

### 미확인(감사자 명시)

`TASK_BACKLOG.md`·`apps/admin/CLAUDE.md`·`apps/backend/CLAUDE.md`·루트 `README*`는 지정 범위 밖이라 보지 않았다. `fsd-architecture-compliance-proposal.md` 본문 V1~V15 감사표는 문서 자신이 "2026-04-18 당시 상태"로 못박고 있어 부기 섹션만 재대조했다.

### 처리 상태

**어긋남 1·3·4·5·6·7·8과 의심 1건 처리 완료**(2026-09-03, 같은 커밋). 방침:

- 줄번호·위치·수치 드리프트(1·3·6·7)는 실측 재확인 후 치환.
- 삭제된 함수 참조(4)는 지우지 않고 "C-05로 삭제됐다"는 사실과 이유를 덧붙였다 — 그 문장이 원래 지키려던 불변식(‘processing 진입 writer는 하나’)이 어떻게 더 강하게 닫혔는지가 기록으로 남아야 한다.
- 「남음」 목록(5·7)은 2026-08-03 스냅샷이라 **덮어쓰지 않고** 「재대조 (2026-09-03)」 절을 덧붙였다. 8번도 같은 문서의 dated 본문이라 이 절이 함께 덮는다.
- 의심 건은 예시 코드를 현재 이름·barrel 임포트로 고치고, 왜 barrel이어야 하는지와 그 액션이 reconcile 쓰기를 겸한다는 사실을 주석으로 달았다.

**2번은 미처리 — 사용자 결정 대상.** 제안서 하나를 `completed/`로 옮기고 `status`를 바꾸는 것은 상태 전이라 메인 루프가 임의로 하지 않는다.

**교훈**: 이번 어긋남 일곱 중 넷(1·6·7·8)이 순수 줄번호·수치 드리프트다. 2026-08-29 backend 감사에서 얻은 교훈("파일 상단 import 추가는 그 파일을 인용하는 문서 줄번호 전부를 밀어낸다")이 이번엔 **문서 인용이 워크스페이스 CLAUDE.md 밖의 제안서·규약서에도 흩어져 있다**는 형태로 재발했다. 대규모 리팩터의 인수 handoff에는 "코드를 인용하는 모든 문서"를 대상으로 한 줄번호 재대조가 들어가야 한다 — CLAUDE.md 한 파일만으로는 부족하다.

**후속(2026-09-03, 같은 날).** 위 어긋남 2번을 사용자 지시로 처리했다 — `processing-concurrency-hardening-2026-06-30.md`를 `completed/2026-06-30-processing-concurrency-hardening.md`로 옮기고 `status: "completed"`로 바꿨다. 감사 보고를 그대로 믿지 않고 `origin/main` 기준으로 다섯 항목(partial unique index 마이그레이션 · `StartProcessingAttemptResult` union + P2002 catch · `hasProcessingUploadForUser`의 `tx` 인자 · 크레딧 게이트 `credits < clipCount` · pricing 정책 문구 두 줄)의 실재를 다시 확인했고, 구현 커밋이 `89663c9`(제안서와 **같은 날** 2026-06-30)임을 찾아 `completed-at`에 그 날짜를 넣었다 — 발견일이 아니라 실제 완료일이 기록으로 남아야 한다. 이로써 이번 감사의 어긋남 8건이 전부 닫혔다.

## 2026-09-08 — 8차 감사 (FEAT-36 사이클 구현·인수 직후, `598b0f4`)

**대상**: `TASK_BACKLOG.md` 4항목, `apps/web/CLAUDE.md` 주장 6건, `apps/backend/CLAUDE.md` 주장 7건,
FEAT-36 신규 코드의 `main.py` 인용 11건, 유저 대면 카피 5건.

**어긋남 0건.** 이번 사이클이 만든 코드가 `apps/backend/main.py`와 맺는 계약을 줄 단위로 대조했고
(묶기 로직 `:287-345` 전체와 `:300-305`·`:317`·`:321-322`·`:324-326`·`:334-338`·`:340-342`·`:344-345`·
`:354`·`:369`·`:384-385`·`:562`) 전부 일치했다. `apps/web/CLAUDE.md`의 새 행이 주장하는
"`end === clipEnd` 포함" 경계도 테스트 파일에 실재하고 백엔드 필터와 맞는다. 한국어 미리보기가
영어 원문을 보여주는 것은 코드의 알려진 근사이고 화면 안내문이 그것을 고지하므로 드리프트가 아니다.

**의심 2건**

1. **FEAT-01의 근거가 사라졌다.** 백로그 FEAT-01(`Credit System 마무리`)의 `source`가
   "README 'Currently in Development'"를 가리키는데 현재 `README.md`에 그 표제가 없다. 오히려
   `README.md:194-204`가 크레딧·차감·Polar 체크아웃/포털/웹훅을 **완결된 기능**으로 서술하고,
   `features/billing/api/index.ts`에도 해당 로직이 갖춰져 있다. 감사자는 "마무리"가 무엇을
   가리키는지 항목이 밝히지 않아 참·거짓을 물을 수 없다고 판정했다. → **메인 루프 판단**: 이건
   백로그 위생 문제이지 코드 드리프트가 아니다. 항목을 지우는 것도 남기는 것도 소유자 결정이라
   여기 기록만 하고 손대지 않는다. 사용자에게 전달했다.
2. **`CAPTION_RENDER.EM_SCALE`의 폰트 메트릭을 재현하지 못했다** — 폰트 바이너리를 받아 OS/2 표를
   파싱해야 해서 `Read`/`Glob`/`Grep`만 가진 감사자의 도구 밖이다. → **이미 두 번 독립 실측된 값이다**:
   계획 검증 라운드 1에서 메인 루프가 설치 URL(`main.py:70·74`)에서 폰트를 받아 파싱했고,
   `plan-verifier`가 같은 일을 자기 컨텍스트에서 독립으로 반복해 같은 값(Anton 2048/3550,
   Noto 1000/1448)을 얻었다. 감사 범위의 한계이지 미확인 주장이 아니다.

**백로그 유효성 재확인**: BUG-09·BUG-12·FEAT-27의 핵심 인용을 다시 읽어 전부 유효.

**관측(드리프트 아님)**: 감사 도중 다른 세션의 FEAT-35가 인수되어 `apps/web/CLAUDE.md`의 테스트
총계가 16파일·25 suite·107 테스트에서 19파일·29 suite·123으로 갱신됐다. 감사자가 두 시점 모두
정적 카운트로 재현해 실제 파일 집합과 일치함을 확인했다. FEAT-36이 넣은 `caption-preview.test.mjs`
행도 그대로 살아 있다(같은 파일 77행). 부수적으로 `apps/admin/CLAUDE.md`의 "334개 test"가 정적
카운트 330과 어긋나 보이지만 `journey.test.mjs:114-131`이 루프 안에서 `it()`을 5회 생성하므로
정확히 맞는다 — 정적 카운트만으로 판정하면 안 된다는 사례라 남긴다.

## 2026-09-15 — 9차 감사 (FEAT-42 구현·인수 직후, `ec96bb2`)

대상:
- `apps/web/CLAUDE.md` 「테스트」 절 — 개수 문구와 새 두 행
- `docs/release-checks.md` — FEAT-42 절, FEAT-41 절 「(FEAT-42 배포 후)」 두 줄
- `captionStyleSchema` 위치를 가리키는 코드 주석 전부(`apps/web/src/**`·`packages/db/prisma/schema.prisma`)

**확인 15건.**
- 테스트 개수 23파일·35 suite·154를 정적 카운트로 재현했다. 메인 루프가 인수 때 돌린 러너 출력 `# tests 154 # suites 35`와 일치한다.
- 새 두 행의 서술: `firstCueText` 경유, KR 샘플 9단어, auto/render/null 세 분기와 `functions.ts` 배선, 백엔드 `caption_style_source.py` `select_caption_style` 실재.
- 원장 FEAT-42 절의 테스트 수와 계측 호출부, FEAT-41 두 줄이 배포 대기로 열린 채인 것의 정합.
- `constants.ts:112`의 새 스키마 경로.

**어긋남 2건**
- `packages/db/prisma/schema.prisma:59` `// 검증은 captionStyleSchema(features/clip-review/model/schemas.ts) 한 곳.`
- `:186` `// validated by captionStyleSchema (features/clip-review/model/schemas.ts).`

정의는 `apps/web/src/fsd/shared/config/caption-style-schema.ts`로 이관됐고 `schemas.ts`는 재수출만 한다.

→ **메인 루프 판단**: 알려진 드리프트다. 계획서 「범위 밖 의존」이 `packages/db`를 web-dev 쓰기 범위 밖으로 미뤘다. 인수 때 "다음 스키마 변경 항목 FEAT-47에 덧붙이는 안"을 소유자에게 제시했으나 등재 승인을 받지 않았다(소유자는 FEAT-50만 승인).
주석 한 줄에도 생성 클라이언트 재생성이 따르므로 지금 고치지 않고 기록만 한다. 재수출이 남아 인용 경로가 깨지지는 않는다. 사용자에게 전달했다.

**미확인(성격상 범위 밖)**: 원장의 미체크 실물 확인 줄은 배포 뒤 예정 확인이라 판정 대상이 아니다. 테스트 개수는 감사자가 정적으로 셌고, 러너 실행은 메인 루프 인수 기록에 있다.

## 2026-09-15 — 10차 감사 (FEAT-45 구현·인수 직후, `61fd37b`)

대상:
- `apps/web/CLAUDE.md` — 테스트 표 `review-language-notice.test.mjs` 행, 「테스트」 개수 문구
- `docs/release-checks.md` — FEAT-45 절, FEAT-37 절(대체 두 줄·열린 셋째 줄), FEAT-43 절 라벨 언급
- 검토 화면 안내·카드 라벨·캡션 편집기 안내의 문구·위치를 가리키는 코드 주석(`apps/web/src/**`)

**확인 18건.**
- 테스트 개수 23·35·154(정적 카운트).
- 새 문구 세 개가 원장 FEAT-45 절과 `review-language-notice.ts:19`·`ClipDraftCard.tsx:482`·`CaptionStyleEditor.tsx:318-323` 실제 렌더 문자열과 일치.
- FEAT-37 대체 두 줄과 열린 셋째 줄이 `showsEnglishSourceForTranslation` 로직과 정합, FEAT-43 라벨 언급 일치.
- `review-language-notice.ts:9`의 `subtitle-status.ts:5` 참조 정확.

**어긋남 4건 — 전부 코드 주석의 낡은 줄번호 인용이다(사용자 문구·동작 무관).**
1. `widgets/clip-draft-review/model/review-language-notice.ts:2` `(ClipDraftCard.tsx:113)` — `previewText` 정의는 `:114`.
2. `widgets/clip-draft-review/ui/_component/ClipDraftCard.tsx:476` `(:113)` — 같은 파일 자기 참조, 실제 `:114`.
3. `review-language-notice.ts:3-5`의 `main.py:837·840·474·535` — 실제 `:844`(`elif selected_language == "Korean":`)·`:847`(호출)·`:478`(Gemini 번역 주석, 호출은 `:515`)·`:542`(`korean_texts = english_texts`).
4. `widgets/clip-draft-review/ui/index.tsx:281-282` `CaptionStyleEditor :310-311` — 그 줄은 편집기 주석이고 실제 안내 문단은 `:318-323`.

→ **메인 루프 판단**
- 3은 백로그 FEAT-44의 열거(`review-language-notice.ts` 1줄)에 이미 들어 있는 드리프트다.
- 4는 FEAT-45 인수 기록이 관측으로 남긴 것이다(계획서가 「문구만」 범위 밖으로 둠).
- 1·2는 이번에 새로 드러났다 — FEAT-45 전부터 한 줄 밀려 있던 인용이다(FEAT-45는 그 줄을 바꾸지 않았다).
- 넷 다 동작 영향이 없고 FEAT-44(교차 줄번호 인용을 함수명·내용 앵커로 교체)와 성격이 같다. FEAT-44 범위에 1·2·4를 덧붙일지는 소유자 결정이다. 사용자에게 전달했다.

**미확인(범위 밖)**: `TASK_BACKLOG.md`·다른 워크스페이스 `CLAUDE.md`는 지정 대상이 아니었다. 원장은 FEAT-45·37·43 절이 든 1~302줄만 읽었다.

## 2026-09-15 — 11차 감사 (FEAT-46 구현·인수 직후, `8ae9701`)

대상(지정 — 작업 트리에 병행 세션의 커밋 안 된 FEAT-49 문서 변경이 있어 기본값 대신 FEAT-46 관련으로 좁혔다):
- `TASK_BACKLOG.md` — FEAT-44·FEAT-47·FEAT-48 항목
- `apps/backend/CLAUDE.md` 전체
- `apps/web/CLAUDE.md` — 테스트 표 `caption-preview.test.mjs` 행
- `docs/release-checks.md` — FEAT-46 절

**확인 52건.**
- FEAT-44 12건: web 교차 인용 30줄 집계(23·2·5), 추가 범위 ⑤ (a)(b)(c), FEAT-40 경로 이동.
- FEAT-47 3건: `schema.prisma:179-181`·`:133` 주석, 컬럼 미존재.
- FEAT-48 9건: `modal-contract.ts`, 드래프트 매핑, 카드, `referenceTranslation` 미배선.
- `apps/backend/CLAUDE.md` 18건: 갱신한 줄번호 11곳, FEAT-46 단락, 기본값 표.
- web `caption-preview.test.mjs` 행 4건.
- 원장 FEAT-46 절 6건.
- 백로그 유효 확인: FEAT-44(낡은 교차 인용 30건 잔존)·FEAT-47(컬럼 0건)·FEAT-48(web에 `referenceTranslation` 0건).

**어긋남 1건.**
1. `TASK_BACKLOG.md` FEAT-44 「제외」 절 `packages/db/prisma/schema.prisma:116` `// 백엔드가 클립마다 생성해 성공 콜백에 싣는 선택 근거다(main.py:1182-1184).` — `:116`은 `@@index([userId, status, createdAt])`이고 해당 주석은 `:133`이다. 같은 날 쓴 FEAT-47은 `:133`으로 바르게 적었다. 작성 시점의 인용 오류로 보인다.

**감사자 미확인 메모**: FEAT-48의 `inngest/functions.ts:921`·`:931`이 `:929`·`:939`로 밀림(내용 존재, 어긋남으로 올리지 않음). `apps/web/CLAUDE.md` 머리의 테스트 총수는 실행 없이 확인 불가(지정 행 밖). 원장의 117/0은 Bash가 없어 두 기록의 교차 일치로 갈음.

→ **메인 루프 판단**
- 어긋남 1은 줄을 다시 읽어 확인했다.
- **감사자가 놓친 것 5건(메인 루프 교차 확인)**: FEAT-44 본문이 "현재 줄"로 적은 `main.py` 인용 6개 중 5개가 낡았다.
  - `:838`→`:852`(`elif selected_language == "Korean":`)
  - `:142`→`:156`(`"top": 200,`)
  - `:288`→`:302`(`def create_subtitles_with_ffmpeg(`)
  - `:158`→`:172`(`def resolve_caption_style(`)
  - `:47`→`:56`(`class ProcessVideoRequest(BaseModel):`)
  - `:44` import만 맞다. FEAT-41에서 이미 밀려 있었고 FEAT-46이 +8을 더했다. 감사자의 FEAT-44 확인은 web 쪽 인용 대조였다.
- 여섯 모두 백로그 서술의 줄번호다. 코드·동작·원장 확인 항목에 영향이 없고, FEAT-44는 완료 시 grep을 다시 돌려 구현을 오도하지 않는다. 백로그를 고칠지는 소유자 결정이다. 사용자에게 전달했다.
- FEAT-48 `functions.ts` 메모는 메인 루프가 인용 글자 추출에 실패해 판정을 보류했다.

**미확인(범위 밖)**: 백로그의 다른 항목, `apps/admin`·루트 `CLAUDE.md`, 원장의 다른 절은 지정 대상이 아니었다.

## 2026-09-16 — 12차 감사 (FEAT-47 구현·인수 직후, `5a2762a`)

범위: FEAT-47(`ClipDraft.referenceTranslation` 컬럼 추가, `Clip` 주석 앵커 교체, 마이그레이션 프로덕션 적용, 생성 클라이언트 7파일)과 직전 사이클(FEAT-46 backend · FEAT-49 web)의 여파.

### 감사자가 대조한 범위

`packages/db/prisma/schema.prisma` 주장 7건(`Clip` 주석 앵커 2 · `referenceTranslation` 주석 4 · 마이그레이션 SQL 1), `apps/backend/CLAUDE.md` 12건, `apps/web/CLAUDE.md` 2건, `apps/admin/CLAUDE.md` 2건, `PROJECT_BOARD.md` 3건(`area` ↔ 실제 변경 파일), `docs/release-checks.md` 2건(심볼 실재), `TASK_BACKLOG.md`의 FEAT-48·BUG-14 인용 8건.

### 어긋남 2건 — 둘 다 `TASK_BACKLOG.md` 자신의 교차 파일 줄번호 인용

**메인 루프가 `sed`로 여섯 줄 전부 직접 재확인했다**(감사 보고를 그대로 옮기지 않았다).

- **FEAT-48 항목**: `inngest/functions.ts:921` `await createClipDraftsBulk(` · `:931` `hook: moment.hook ?? null,` → 실제 `:929`·`:939`. 인용된 `:921`엔 `);`, `:931`엔 `uploadedFileId,`가 있다. **8줄 표류.** — **11차에서 "메인 루프가 인용 글자 추출에 실패해 판정 보류"로 남겼던 바로 그 메모가 이번에 닫혔다.**
- **BUG-14 항목**: `main.py:847`·`:486`·`:505`·`:548` 넷 → 실제는 전부 정확히 8줄 뒤(`:855`·`:494`·`:513`·`:556`). 원래 줄은 각각 `s3_key=english_s3_key,` · `# Step 4: Gemini로 일괄 번역` · 빈 줄 · `except Exception as e:`로 인용 내용과 무관하다. 원인은 FEAT-46이 `main.py` 앞부분에 넣은 import 블록과 `build_reference_translations`(+8). **8줄 표류.**

둘 다 **항목 자체는 유효하다** — 틀린 것은 가리키는 위치뿐이고 요구(참고 번역 배선 미완성 · Korean 렌더 자막 품질 판정)는 그대로다.

### 어긋남 없음으로 확인된 곳

FEAT-47이 이번에 새로 쓴 주장은 코드와 일치한다 — `ClipDraft.referenceTranslation` 주석 넷(FEAT-46 analyze 콜백이 출처 / 렌더는 재사용하지 않고 `create_korean_subtitles_with_ffmpeg`가 따로 번역 / null의 의미 / AI 구간 기준)과 `Clip` 주석의 새 앵커 둘(`_do_process_video`, `model ClipDraft`). `apps/web`에 `referenceTranslation` grep 0건인 것은 결함이 아니라 설계다(FEAT-47은 저장소만, 배선은 FEAT-48).

### 미확인 (감사자 자기 신고)

`TASK_BACKLOG.md`의 FEAT-27·BUG-12, `docs/release-checks.md`의 다른 절, `.claude/agents/*.md`는 이번 사이클과 무관해 제외. `functions.ts`를 가리키는 다른 인용도 FEAT-48·BUG-14가 인용한 지점 외에는 대조하지 않았다.

### 처리 상태

고칠지는 소유자 결정으로 남긴다 — 앞서 대기 중인 FEAT-44 백로그 인용과 같은 성격이라 함께 물었다 — 세어 보면 **백로그 세 항목의 열한 곳**이다(FEAT-44 5곳: `schema.prisma:116`은 FEAT-47이 처리했고 `main.py` 다섯이 남았다 / FEAT-48 2곳 / BUG-14 4곳). 근본 해법은 줄번호 대신 함수명·문구 앵커이고 그것이 FEAT-44 항목의 요구다. 사용자에게 전달했다.

## 2026-09-16 — 13차 감사 (FEAT-48 구현·인수 직후, `a0a3526`)

대상 지정 없이 기본 범위로 돌렸다 — `TASK_BACKLOG.md` 전체 + `Glob`이 찾은 `CLAUDE.md` 넷(web·admin·backend·루트) + 백로그가 가리키는 유저 대면 카피(pricing·product-tour). 확인한 주장은 백로그 24 · backend 13 · web 4 · admin 3 · 루트 1 · 카피 4 = **49건**.

**FEAT-48 사이클이 이번에 새로 쓴 주장에는 어긋남이 0건이다** — 원장 FEAT-48 절, `apps/web/CLAUDE.md` 테스트 표의 새 행 둘과 머리 수치가 모두 통과했다. 감사자는 파일 수(25)를 `Glob`으로 확인했고 suite·test 수(41·176)는 실행 없이 셀 수 없어 [미확인]에 남겼는데, 그 둘은 인수 때 메인 루프가 `npm test -w apps/web`를 직접 돌려 확인한 값이다.

### 어긋남 4건 — 메인 루프가 전부 직접 재대조했다

| # | 문서 | 감사 주장 | 내 재대조 |
| --- | --- | --- | --- |
| 1 | 백로그 FEAT-01 | 유령 — 이미 되어 있는데 할 일로 남아 있다 | **확인.** 항목의 유일한 근거가 `source: README "Currently in Development"`인데 README에 그 문구가 없다(grep 0건). `README.md:194` 「Credits & Billing」 절이 완결된 시스템으로 서술하고, 코드도 일치한다 — `/api/checkout`·`/api/portal`·`/api/webhooks/polar` 라우트 셋 실재, `features/upload/api/complete-processing-attempt.ts:38`이 `decrementUserCreditsFloorZero(userId, clipsFound)`로 **생성된 클립 수만큼** 차감 |
| 2 | 백로그 BUG-14 | `main.py` 인용 4곳이 +8 밀림 | **확인.** `:847`은 이제 `s3_key=english_s3_key,`이고 Korean 자막 호출은 `:855`. `:486`→`:494`, `:505`→`:513`, `:548`→`:556`. 12차 감사가 잡은 그대로이고 아직 미결이다(사용자 판단 대기 중이라 방치가 아니다) |
| 3 | 백로그 FEAT-44 | 이 항목이 "정답"이라 적어둔 줄번호들이 다시 밀렸다 | **확인. 이번 감사에서 가장 값진 발견이다** — FEAT-44가 고치려는 병이 FEAT-44 자신의 예시에서 재발했다. `main.py:838`(Korean 분기)→`:852`, `:142-143`(MARGINV 200/260)→`:156-157`, `:288`(`create_subtitles_with_ffmpeg`)→`:302`, `:158`(`resolve_caption_style`)→`:172`, `:47`(`ProcessVideoRequest`)→`:56`. 추가 범위 ⑤의 web 인용도 하루 만에 밀렸다 — `ClipDraftCard.tsx:114`→`:115`, 자기인용 `:476`→`:485`. **`CaptionStyleEditor`는 대상이 한 곳이 아니게 됐다**: FEAT-44가 가리킨 `:318-323`의 문장이 지금은 `:331`과 `:339` 두 곳에 있다(FEAT-49가 분기를 나눴다) |
| 4 | 백로그 BUG-12 | "여섯"이라 쓰고 일곱을 나열 | **확인.** `다른 toLocaleString() 여섯(:59,60,61,154,161,198,235)` — 괄호 안이 일곱이다. 핵심 결함 서술(`:236`)은 정확하다 |

### 의심 1건 — 유저 대면 약속의 모호함

`pages/pricing/config/index.ts:38` `"Processing starts only when the account has at least one credit per requested clip."`

재대조 결과 코드에는 최소 크레딧 규칙이 **둘**이다 — `inngest/functions.ts:306` `if (context.user.credits < clipCount)`(auto·render 디스패치)와 `:763` `if (context.user.credits < 1)`(검토 플로우의 analyze 단계). 그래서 이 문구는 auto 경로에서는 참이고, **`Review first`로 올린 사용자에게는 거짓이다**(분석은 크레딧 1이면 시작된다). 감사자의 「문서가 모호」 판정에 동의하되, 모호한 게 아니라 **두 경로 중 하나에서 틀린 약속**이라고 좁혀 기록한다. BUG-06(코드는 바뀌었는데 판매 문구가 남아 거짓 약속을 한 사건)과 같은 부류다.

### 미확인으로 남은 것

감사자가 스스로 밝힌 것 셋: (a) `apps/web`·`apps/admin` CLAUDE.md의 suite·test 수는 실행 없이 못 센다(web 쪽은 인수 때 메인 루프가 실행으로 확인), (b) FEAT-44가 나열한 30줄 중 표본 9곳만 재확인, (c) BUG-14의 본체 가설(Korean 렌더 자막 품질)은 실물 시청이 전제라 감사로 판정 불가 — 항목 자신도 그렇게 적고 있다.

### 사용자 결정 대기

- FEAT-01을 백로그에서 지울지(유령).
- 낡은 줄번호 인용 — 이제 BUG-14 4곳 + FEAT-44의 자기 예시 다수. FEAT-44를 실행할 때 그 안의 "정답" 줄번호를 믿으면 안 된다는 사실 자체가 이 항목이 함수명·문구 앵커로 가야 하는 근거다.
- BUG-12의 "여섯" → "일곱".
- pricing 문구를 검토 플로우까지 참이 되게 고칠지.

## 2026-09-16 — 14차 감사 (FEAT-50 구현·인수 직후, `329eda9`)

기본 범위(`TASK_BACKLOG.md` + 모든 `**/CLAUDE.md`). 감사자 보고를 중계하지 않고 전부 직접 재현했다.

### 어긋남 1건 (감사 보고 2건 중 1건만 사실)

| 대상 | 감사 주장 | 내 재현 | 판정 |
| --- | --- | --- | --- |
| `TASK_BACKLOG.md` BUG-14의 `main.py` 인용 4건 | `:847`·`:486`·`:505`·`:548`이 각각 +8줄 밀려 실제로는 `:855`·`:494`·`:513`·`:556` | 주장된 줄을 열어 보니 각각 `s3_key=english_s3_key,` · `# Step 4: Gemini로 일괄 번역` · 빈 줄 · `except Exception as e:`로 **인용 문자열이 없다**. 지목된 줄에는 그대로 있다 | **사실 — 인용 드리프트** |
| `apps/admin/CLAUDE.md:37` "334개 test" | 실제 330건 | **러너 실측**: `npm test -w apps/admin` → `# tests 334 / # suites 75 / pass 334 / fail 0`. 문서가 맞다 | **감사가 틀렸다** |

둘째 건의 원인은 집계 방법이다. 감사자는 `it(`/`test(` 문자열을 세어 330을 얻었는데 그 방식이 4건을 놓쳤다(문자열 안 매치·다른 호출 형태). **테스트 수는 세는 게 아니라 돌리는 것이다** — 이 사이클에서 내 하니스가 다섯 번 거짓을 냈던 것과 같은 부류이고, 그래서 재현을 러너로 했다.

BUG-14 건은 유효하지만 **항목 자체는 살아 있다** — 가리키는 대상(영어 3단어 묶음 단위 번역)은 `main.py:855`에 그대로 있다. 줄번호만 낡았다. FEAT-44의 area에는 이 백로그 자기 인용이 들어 있지 않아, FEAT-44를 실행해도 이 네 줄은 남는다.

### 의심 1건 — FEAT-01

감사자 판정에 동의한다. 재현: 백로그 `source`가 가리키는 README의 `"Currently in Development"`가 **0건**이고, 지금 README의 크레딧 서술(`README.md:194-200`)은 완성된 동작을 기술한다(기본 3, 클립당 1 차감, 부분 생성 시 생성분만 차감). 코드도 구현돼 있다.

다만 **유령으로 확정하지는 못한다** — `NEXT_PUBLIC_SUBSCRIPTION_ENABLED`가 `.default("false")`(`apps/web/src/env.js:56-59`)라 결제가 실제로 켜져 있는지는 코드로 갈리지 않는다. 프로덕션 값을 아는 것은 소유자뿐이다. 13차 감사에서 "유령"으로 기울었던 판정을 여기서 **"환경값에 달렸다"로 좁힌다.**

### 감사가 확인해 준 것

`apps/web/CLAUDE.md`의 테스트 인벤토리(**26개 파일 · 42 suite · 183개 테스트**)가 정확하다고 독립적으로 판정했다 — FEAT-50 인수에서 내가 갱신한 값이고, 나도 러너로 확인했다. `apps/backend/CLAUDE.md`는 `main.py` 줄번호 인용 10곳 이상이 전부 정확했다(FEAT-43/49 이후로도 갱신이 따라간 유일한 문서다).

FEAT-44는 **이번 사이클로 근거가 더 늘었다**: FEAT-50이 `ClipDraftCard.tsx`에서 지역 함수 19줄을 걷어내면서 `review-language-notice.ts:2`의 자기 인용 대상(`previewText` 선언)이 `:113` → `:101`로 **위로** 밀렸다. 지금까지 드리프트는 아래로만 밀렸는데 이번엔 반대 방향이다 — 줄번호 인용이 삽입뿐 아니라 삭제에도 깨진다는 실증이다.

### 미확인으로 남은 것

감사자가 스스로 밝힌 것 넷: (a) FEAT-44가 나열한 30줄 중 표본 2곳만 재확인, (b) `apps/admin/CLAUDE.md` 표 29행의 「핵심 계약」 서술 내용은 미검증(수치만 대조), (c) README·README.ko는 지정 대상이 아니라 미감사(FEAT-01 근거로 일부만), (d) BUG-14 본체 가설(Korean 렌더 자막 품질)은 실물 시청이 전제라 판정 불가.

### 사용자 결정 대기

- **BUG-14의 인용 4건 교정** — 줄번호를 +8 맞출지, FEAT-44의 원리대로 함수명·문구 앵커로 바꿀지. 후자를 권한다(또 밀린다).
- **FEAT-01** — 백로그에서 지울지. 판정이 `NEXT_PUBLIC_SUBSCRIPTION_ENABLED`의 프로덕션 값에 달렸다.
- 기존 대기: pricing 문구(검토 플로우에서 거짓), FEAT-44 실행, BUG-12 "여섯"→"일곱", 마이그레이션 히스토리 드리프트 등재.

---

## 2026-09-17 — 15차 감사 (FEAT-51·FEAT-52 구현·인수·배포 직후, `6852180`)

브리핑에 이번 변경으로 **사실이 달라진 것 다섯**을 대조 기준점으로 주고, **유저 대면 약속 카피를
반드시 포함**하라고 명시했다. 이유: 캡션 스타일이 검토 화면에서 사라졌으므로 "클립마다 자막을
조절할 수 있다" 류 판매 문구가 있으면 오늘부로 거짓이 되는데, 그 부류는 이번 사이클에서 아무도
보지 않았다(BUG-06과 같은 모양).

감사 범위: `TASK_BACKLOG.md` 주장 36건 · `apps/web/CLAUDE.md` 5건 · `apps/backend/CLAUDE.md`
8건 · `apps/admin/CLAUDE.md` 1건 · 유저 대면 카피 다수(pricing·FAQ·product-tour·guides·
changelog·home·settings·youtube-shorts-generator).

### 어긋남 2건 — 메인 루프가 둘 다 직접 재대조했다

1. **`apps/web/CLAUDE.md:170`의 이벤트 개수가 틀렸다.** 문서는 `이벤트 이름 29개`라 했는데
   `packages/db/src/analytics-contract.ts`의 `ANALYTICS_EVENT_NAMES`를 세면 **31개**다.
   내가 `sed`로 배열 구간만 잘라 `"`로 시작하는 줄을 센 결과도 31. 이번 사이클과 무관한 기존
   드리프트다. **즉시 고쳤다**(29 → 31) — 사실 오류이고 판단이 필요 없다.

2. **홈페이지가 만들지 못하는 것을 약속한다.** `apps/web/src/fsd/pages/home/config/index.ts:100`
   `      "Accept, tweak, or regenerate. Export vertical, square, and landscape ratios."`
   그런데 백엔드는 세로 하나만 만든다 — `apps/backend/main.py:214` `    target_width = 1080` ·
   `:215` `    target_height = 1920`. **`square`·`landscape`·`aspect` 경로를 `apps/backend/*.py`와
   `apps/web/src/inngest/` 전역에서 찾으면 0건이다**(내가 직접 grep). 다른 마케팅 페이지는 전부
   "vertical 1080x1920"만 약속하는데 홈 3단계만 어긋난다.
   **이건 BUG-06과 같은 부류다** — 코드가 아니라 판매 문구가 거짓을 말하고, 발견한 것이 감사다.
   이번에는 사용자의 우연이 아니라 감사가 먼저 잡았다. **고치는 방향(약속 철회 vs 기능 구현)은
   제품 결정이라 사용자 몫이다.**

### 의심 2건 — 둘 다 사실 관계는 확인됐고 결정만 남았다

1. **FEAT-01이 유령일 수 있다.** 그 항목의 `source`는 `README "Currently in Development"`인데
   **`README.md`·`README.ko.md` 어디에도 그 문구가 없다**(내가 재검색, 0건). README의
   `Credits & Billing` 절은 완결된 시스템처럼 서술하고 `features/billing/`에 `BillingPage`·
   `PlanCard`·`SubscriptionStatus`·`OrderHistory`·checkout/portal/webhook이 이미 있다.
   다만 항목이 "마무리"가 무엇인지 적지 않아(관측·진단 없이 부트스트랩 문구뿐) 코드만으로는
   완료 판정이 안 된다. **소유자 결정 대기.**

2. **`CaptionStyleEditor`의 `!sample` 라이브 미리보기 분기가 죽었다.** `CaptionStyleDialog`가
   유일한 비-sample 소비자였는데 FEAT-52가 파일째 지웠다. 내가 전수 확인: 렌더 소비자는
   `pages/settings/ui/index.tsx:260`의 하나뿐이고 `sample`(항상 true)·`playUrl={null}`을 고정
   전달한다. 따라서 `CaptionStyleEditor.tsx:336` `        {sample ? (`의 else 쪽 안내 둘과
   `sample-captions.ts`의 라이브 경로(`koreanSampleCues`·`previewCaptionCues`의 `!sample`)가
   도달 불가다. **감사가 덧붙인 경고가 중요하다**: `FEAT-56`의 게이트(`npm run check` 경고 0)로는
   이게 안 잡힌다 — `sample`·`playUrl`은 타입상 여전히 "쓰이는" prop이라 `no-unused-vars`가
   걸리지 않는다. **FEAT-56을 그대로 끝내도 이 잔재는 남는다.**

### 감사가 확인해 준 것

- **유저 대면 카피에 "클립마다 자막을 조절할 수 있다" 류 약속은 0건이었다.** 이번 이관이 거짓으로
  만든 판매 문구는 없다 — 내가 가장 걱정한 자리였는데 깨끗했다.
- 백로그 신규 5항목(FEAT-53·54·55·56·57)의 인용이 **전부 그대로 유효**하다. 내가 쓴 직후
  `metadata.ts`에서 한 건이 밀렸던 전례가 있어 확인이 필요했다.
- FEAT-44의 필요성이 오히려 커졌다 — 그 항목이 "현재"라고 적은 줄번호조차 FEAT-51 커밋으로 또
  밀렸다(`main.py:838`→`:852`, `:142-143`→`:156-157`). 줄번호를 다시 맞추지 않고 앵커로
  교체하기로 한 결정이 옳았음을 실측이 다시 보인다.

### 미확인으로 남은 것

- `apps/web/CLAUDE.md`·`apps/admin/CLAUDE.md`의 suite·test **개수**는 확인 못 했다 — 이
  에이전트에는 `Bash`가 없어 `npm test`를 못 돌린다(설계상 그렇다). 파일 수만 `Glob`으로 재현했다.
- `apps/admin/CLAUDE.md`의 표 29행 상세 서술은 대조하지 않았다(이번 사이클이 admin을 안 건드림).
- `apps/backend/CLAUDE.md`의 caption 절 밖(WhisperX·Columbia ASD·Face Tracking)은 대조하지 않았다.
- 루트 `README.md`·`README.ko.md` 전수는 기본 대상이 아니라 하지 않았다(FEAT-01 관련 한 문구만 확인).

### 사용자 결정 — 같은 날 셋 다 내려졌다

1. **홈 카피의 `square, landscape` 약속 → 철회.** 화면비 3종을 만드는 쪽은 기각했다 — 제품이
   세로 쇼츠이고(`youtube-shorts-generator`), 만들려면 `create_vertical_video` 합성부·ASS
   `PlayRes`·크롭 로직·미리보기의 9:16 전제를 전부 건드려야 한다. 감사 뒤 추가로 확인한 것:
   **같은 파일 `:51`이 이미 `1080x1920 crops`를 약속해 한 페이지가 자기모순이다.** `BUG-15`로
   등재했고 요구 ②로 다른 카피의 여집합 확인(`square|landscape|aspect|ratio`)을 넣었다.
2. **FEAT-01 → 제거.** 유령 항목이었다. `source`의 근거 문구가 README에서 사라졌고, 관측·진단이
   없어 어느 에이전트도 착수할 수 없었다. 백로그 자기 규칙("여기가 틀리면 보드도 틀린다")상
   판정 불가능한 항목이 목록에 남으면 pm이 매일 건너뛴다. 제거 자리에 사유와 이 기록으로의
   포인터를 한 줄 남겼다 — 결제·크레딧에 실제 결함이 드러나면 관측을 갖춘 새 항목으로 선다.
3. **죽은 `!sample` 경로 → FEAT-56에 합쳤다**(넷 → 다섯). 같은 원인(FEAT-52의 삭제 잔재)·같은
   파일군이라 별도 항목의 값어치가 없다. **대신 감사가 짚은 게이트 구멍을 고쳤다** — 성공 기준이
   "`check` 경고 0"뿐이었는데 그걸로는 이게 안 잡히므로(타입상 쓰이는 prop이라 lint 무반응),
   "**`sample=false`로 도달하는 경로가 0임을 소비자 전수 열거로 보인다**"를 게이트 (b)로 추가했다.
   `sample-captions.test.mjs`의 라이브 케이스 판정도 요구 ③으로 넣었다.

어긋남 ①(이벤트 개수 29→31)은 사실 오류라 결정 없이 즉시 고쳤다(`2ee155f`).
BUG-15·FEAT-56의 인용 12건은 등재 후 전수 대조했다 — 불일치 0.
