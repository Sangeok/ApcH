# FEAT-13 — main-loop 기록

## 게이트① 개방 (2026-08-18)

- 전이: `승인대기` → `계획지시`
- 결정자: 사용자 — 채팅에서 "FEAT-13.md를 작성해". 계획서 작성은 `계획지시` 상태의 일이므로 이 지시를 게이트① 개방으로 기록한다
- 기록자: 메인 루프 (규약: 사용자가 게이트를 열고 메인 루프가 기록한다 — `docs/agents/README.md`)
- 경위: 소유자 직접 지정(pm 미경유)으로 `b882cb2`에서 `승인대기` 진입. 같은 날 게이트① 개방
- 다음: admin-dev 디스패치 → `docs/plans/FEAT-13.md` 작성 → `검토대기`

## 계획서 검증 (2026-08-19) — reconciling-proposals-with-codebase, High-Risk 프로파일

대상: `docs/plans/FEAT-13.md`. 프로파일 근거: bounce 변경이 게이트 PUT(GitHub 커밋) 산출물을 바꾼다 — 외부 효과 + 보드 무결성.

### 1라운드 — 결함 3건, 편집 5회

인용 전수 실측(4개 코드 파일 전 줄번호·before 스니펫 바이트 대조, 토큰 oklch 값, 테스트 단언 방식, template 절 구조, FEAT-09 선례, CLAUDE.md:35 인벤토리):

1. **[실질] `VALIDATION_LINE_RE` 비전역.** 실재 경로: 검토대기+검증 → 보류(줄 생존, 설계대로) → 수동 재개 때 규약의 줄 제거 누락 → 재검증이 둘째 줄 기록(파서 last-wins라 표시 정상) → bounce가 첫 줄만 제거 → 새 줄 생존 → 재작성 계획서에 거짓 「검증 통과」. `/g`로 전 경로 차단, 부작용 없음. 1줄 픽스처만으로는 플래그 제거 돌연변이가 살아남아 **2줄 제거 단언을 테스트 명세에 추가**.
2. **[인용] switch 범위 불일치** — 한 문서 안 `:73-94`/`:73-93` 혼용. 실측 `:73-93`으로 통일.
3. **[인용] 따옴표 인용 2곳이 실문과 다름** — `--picked` 주석(실문: "일반 강조색으로 쓰지 말 것"·"재사용하지 않는다"), FEAT-09 선례(실문은 비고 보고 방식, `FEAT-09.md:88`·`:695`). 실문·실경로로 교체.

### 2라운드 — 무편집 클린 패스 (편집 0건, 결함 0건)

- 저장본 전문 재독, INV-1~6 재수행. 구조 상이 경로 검사: **스케치 정규식 실제 실행** — LF/CRLF × 검증 0·1·2줄 × 중간/마지막 6케이스 + 무검증 바이트 회귀. 전부 통과(중복 전부 제거, CRLF 보존, 빈 줄 없음, 무검증 시 status 외 바이트 동일).
- 상태 전이 표: 칩(서버 렌더·no-store 재조회·실패 시 `검증 전`으로 안전 강하)·bounce(스테일 가드가 이중 제출 차단, sha 낙관적 잠금이 경쟁 커밋 차단, 단일 파일 PUT이라 부분 실패 없음, auth-first 불변) 전 행 답변.
- 3중 바닥: 의존성 드리프트 없음(신규 import 0) / 픽스처 낡음 없음(기존 픽스처는 검증 줄 무, 필드는 양방향 호환 — 구 파서는 미지 줄 무시) / 관측성 표면 없음.
- 미검사 경계의 근거 있는 제외: 파서 last-wins 중복 표시(위반 상태 표시일 뿐, 위험 경로는 /g가 폐쇄), FSD fixture(신규 import·owner 0), CSP(신규 브라우저 호출 0), env·i18n(무관).

**판정: 클린 패스.** 보드 정지 규칙(무편집 클린 패스 1회) 충족. 총 2라운드(편집 1·클린 1), 결함 3건 수정.

재현 앵커(적용성 증거일 뿐 완전성 증명 아님): HEAD `018ae78` 시점 검증, 계획서 blob `4a843b1`(수정 후, 커밋 예정), 경계 = 계획서가 인용한 7개 대상 파일 + backlog·board 안내·template·FEAT-09·admin CLAUDE.md·globals.css, 폐쇄 레시피 = 인용 전수 실측 + 스니펫 바이트 대조 + 정규식 실행 시뮬레이션(6케이스).

## 재검증 (2026-08-19, 사용자 지시) — 3·4라운드

드리프트: 인용 코드 7파일 무변경(018ae78 이후 문서만), 계획서 blob `4a843b1` 동일 기점.

### 3라운드 — 결함 2건, 편집 3회

신규 폐쇄 경로 둘: "현 파서 미지 줄 무시" 주장을 실제 `parseBoard` 실행으로 확증(2라운드는 코드 읽기만), 롤아웃 창 상호작용 분석.

1. **[실질] 대안 1의 "규약 먼저·코드 나중이 안전"은 과잉 주장** — 표시 경로만 안전하다. 청소 코드 배포 전에 필드 기록을 시작하면 그 창의 대시보드 반려가 현행 코드(status만 교체)로 돌아 줄이 살아남고, 배포 후 재작성 계획서에 거짓 「검증 통과」가 붙는다. 수정: 대안 1 한정 서술 + 범위 밖 의존 2에 채택 순서 명시(코드 먼저, 필드 기록은 배포 후).
2. **[문구] handoff (c)의 집행 열거가 수동 bounce를 암묵에 둠** — "수동 재개(보류→계획지시)"만 명시돼 직접 편집의 검토대기→계획지시 되돌리기가 빠져 있었다. 열거 확장.

### 4라운드 — 무편집 클린 패스 (편집 0건, 결함 0건)

`git diff` 로 3라운드 변경이 정확히 3줄임을 증명 — 나머지는 2라운드 검증 번들과 바이트 동일. 세 hunk 전문 재독, 상호 참조 정합(대안1 ↔ 범위밖2), 판정 계약(정상 상태 서술)과 무모순.

**판정: 클린 패스 유지.** 누계 4라운드(편집 2·클린 2), 결함 5건 수정. 재현 앵커 갱신: 계획서 blob `bf8d802`, 코드 기점 불변.

## 재검증 2차 (2026-08-19, 사용자 지시) — 5라운드

드리프트 0(계획서 blob `bf8d802` 동일, 인용 코드 무변경). 미사용이던 최중량 폐쇄 경로를 실행 — **스케치 전체를 실제 트리 사본에 적용해 진짜 도구로 구동**(FEAT-10 검증 선례):

- 스케치 12개 편집 전부 정확 앵커로 적용 성공 (앵커 불일치 0 — 스케치가 현 코드와 바이트 정합)
- 패치본을 실제 tsconfig로 `tsc --noEmit`: **0 에러** — SpeechItem·BoardItem 필수 필드 추가의 타입 총체성 확증
- 패치본에서 기존 테스트 **182/182 통과** — "기존 단언 안 깨진다" 주장 실행 증명
- 미래 모듈 실동작 e2e 5건 통과: 파서 값/부재, 브리핑 전달, bounce 제거+재파싱 null, **g 플래그 2줄 제거**, 무검증 최소 diff 회귀

과정에서 실패 2건은 모두 **검증 하네스 자신의 결함**이었다(셸 전송의 백슬래시 소모, `String.replace` 치환문의 `$`+백틱 특수 패턴 — 후자는 스케치 주석이 경고하는 바로 그 함정). 계획서 결함이 아니며 구현자는 리터럴 치환 도구를 쓰므로 해당 없음.

**판정: 개선점 0건 — 클린 패스 유지.** 이 패스는 편집 0건의 무편집 패스다. 누계 5라운드(편집 2·클린 3), 결함 5건 수정. 같은 경계의 6라운드는 FEAT-02의 무소득 4차를 반복할 가능성이 높다 — 재검증은 계획서나 인용 코드가 바뀔 때 돌린다(보드 정지 규칙).

## 재검증 3차 (2026-08-19, 사용자 지시) — 6·7라운드

미사용 경로 둘을 폐쇄. (a) 패치본에 **실제 FSD 경계 검사(migration·final)와 실제 ESLint 구동** — 전부 통과(이로써 B-5의 세 명령 전 구성이 패치본에서 실증됨: tsc·182테스트는 5라운드). (b) **briefing 테스트 명세 돌연변이 검사** — 1라운드가 transitions에만 적용했던 방식.

### 6라운드 — 결함 1건, 편집 1회

**[명세 구멍] briefing.test의 "승인대기·feed는 항상 null" 단언이 픽스처에 `검증:` 줄이 없으면 통과-경유 돌연변이를 못 죽인다.** null인 이유(분기의 의도적 null vs 보드에 줄 부재)가 구분되지 않기 때문. 실측: 분기가 `item.validation`을 흘리는 돌연변이 주입 → 줄 없는 픽스처(명세 그대로)에서 **생존**, 줄 있는 픽스처에서 **사멸**. feed 쪽은 위반 상태도 아니다 — 완료 항목은 승인 전이가 줄을 지우지 않아 줄을 가진 채 feed에 실재한다. 수정: 테스트 절에 픽스처 요건 명시(승인대기 + 완료 feed 항목에 `검증:` 줄 부여).

### 7라운드 — 무편집 클린 패스 (편집 0건, 결함 0건)

`git diff`로 6라운드 변경이 정확히 1줄임을 증명(나머지는 5라운드 검증 번들과 바이트 동일). 편집 hunk 재독, 정합 확인(승인-전이-무청소 서술과 무모순 — 계획서 어디에도 승인이 줄을 지운다는 주장 없음).

**판정: 클린 패스.** 누계 7라운드(편집 3·클린 4), 결함 6건 수정. 계획서 blob은 6라운드 편집으로 갱신됨.

## 재검증 4차 (2026-08-19, 사용자 지시) — 8라운드

미답 경로 셋을 폐쇄. ① RSC 직렬화 경계: 파이프라인 페이지 트리에 `"use client"` 0건 — `SpeechItem.validation`은 서버 컴포넌트 안에 머물고 경계를 새로 넘지 않는다(넘어도 string|null 직렬화 가능). ② `next build`: 고유 검출 대상(RSC 위반·Tailwind 방출)이 ①과 2라운드 선례로 이미 닫혀 미실행 — 중복 검사로 판단. ③ **독립 교차검토** — 별도 컨텍스트의 리뷰어에게 적대적 지시로 위임(main-loop 기록·검증 커밋 메시지 접근 금지 = 본 검토의 판정 미제공).

### 독립 리뷰어 판정: PASS (발견 0건)

리뷰어가 자체 수행: 인용 전수 바이트 대조(4개 코드 파일 + 보드·FEAT-09 선례), globals.css 토큰·클래스 해석 확증(TONE_TEXT의 `text-active` 컴파일 실증 포함), 정규식·파서 Node 실행(5필드 회귀 바이트 동일·g 플래그 사멸 확인·EOF 케이스·이웃 항목 불변), 타입 총체성(생성처 전수), 세 대상 테스트 파일 기준선 67/67, 돌연변이 커버리지 재확인. 결함 아닌 구현 유의점 2건은 계획서의 기존 서술로 이미 커버돼 반영 불요 판단(픽스처 줄 추가의 기존 단언 무영향 / diff 단언은 after-파싱 null 앵커가 견고하고 오작성 시 조용한 통과가 아니라 시끄러운 실패).

**판정: 개선점 0건 — 클린 패스 유지.** 이 패스 편집 0건. 누계 8라운드(편집 3·클린 5), 결함 6건 수정. 앵커링 없는 신선한 검사자의 독립 PASS가 추가된 것이 이번 라운드의 수확이다.
