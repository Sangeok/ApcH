# FEAT-21 — 메인 루프 기록 (게이트 결정·검증 라운드)

## 게이트① 개방 (2026-08-25)

사용자가 **대시보드 도장 버튼**으로 `승인대기` → `계획지시` 전이(`9d356d1` — via dashboard gate).
도장 직후 FEAT-20 잠금 칩("도장 찍음 · 보드 반영 대기")의 첫 실사용 관측이 이뤄져 배포 확인
원장의 해당 줄을 닫았다. 선정 주체는 원격 pm(검증 ② 실측 왕복, `cc7cea2`) — 이 항목은 BUG-02가
남긴 범위 밖 의존을 승계 규칙(런북 7단계)으로 등재한 것이 pm 선정으로 이어진 첫 사례다.
web-dev를 계획서 작성에 디스패치. BUG-07은 승인대기 잔류(사용자 결정 대기).

## 계획서 접수 (2026-08-25)

web-dev → `docs/plans/FEAT-21.md`, 보드 행 `검토대기`(커밋 `1f5ee66`). 유실 5지점(웹훅 정규화·
이벤트 타입·워커 재정규화·DB create/patch·ClipCard 표시) + 스키마 공백(`Clip`에 컬럼 없음 —
`schema.prisma:107-139` 전수) 진단. **범위 밖 의존: `Clip.subtitleStatus String?` 컬럼 +
마이그레이션 + Prisma 재생성(packages/db — 무담당 영역)**, FEAT-16 전례(`544ac12` 선행 적용)대로
구현 전 선행 적용이 전제. 사용자 노출 문구는 앱 UI 언어(영어)로 확정 — partial/full 두 문구.

## 검증 필수 경로 확정 (2026-08-25, 카탈로그)

| # | 경로 | 판정 | 근거 |
| --- | --- | --- | --- |
| 1 | 인용 전수 대조 | **필수** | 모든 항목 |
| 2 | 스케치 추출·실행 | **필수** | tsc·eslint·테스트 — **단, 조립은 스키마 선행 적용 후에만 정직**(컬럼 없이는 tsc가 설계상 실패) |
| 3 | before/after 기계 적용 | **필수** | 수정 5파일 |
| 4 | 전칭 여집합 열거 | **필수** | "유실 지점 5뿐"·"컬럼만 생기면 조회는 자동 전달" 전칭 |
| 5 | 돌연변이 검사 | **필수** | 판정 순수 모듈 신설 |
| 6 | 실제 사건 재생 | **필수(변형)** | 외부 신호(콜백 clips) 해석 — 실제 백엔드 페이로드 형상(camelCase·세 상태값)을 파서에 통과 |
| 7 | 음성 시험 | 제외 | 경계·화이트리스트 신설 없음(FSD 경계는 web에도 있으나 owner 변경 없음 — 조립 check가 덮음) |
| 8 | 실물 렌더 | **필수** | ClipCard 폴백 안내 렌더 |
| 9 | 구조적 아티팩트 | **필수** | `schema.prisma` 컬럼 — 구조 파싱으로 확인(선행 적용 검산) |

**선결 결정(사용자)**: 스키마 선행 적용 시점 — 검증 조립 전 적용(권장, FEAT-16과 동일 상태로
검증) vs 게이트② 후 적용. 적용 자체는 nullable 컬럼 추가라 저위험이나 프로덕션 DB ALTER이므로
사용자 승인으로만 실행한다.
