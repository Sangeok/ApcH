# FEAT-17 — 행위자 상세 페이지의 역할 정의를 제목 주도 점진 공개로

## 게이트① (2026-08-23)

사용자가 세션에서 "Feat-17 계획 지시"로 개방. 보드 행을 `승인대기` → `계획지시`로 전이했다.

발주 계약은 백로그 FEAT-17이 원천이다 — 요구 넷: (1) 펜스 밖 `##` 경계 분할 순수 함수(`pm.md:57`의
코드 펜스 안 `## YYYY-MM-DD`를 절로 찢으면 안 된다 — 핵심 테스트 케이스) (2) 첫 덩어리 펼침 +
나머지 절 native `<details>`(FEAT-04 「보고」 피드 선례) (3) 절 이름 문자열에 UI 비결합
(4) 정의 파일 단일 출처 유지 — frontmatter 화면 필드 금지.

설계 대안 비교(이름 기반 특별 취급·frontmatter 필드·현상 유지)와 기각 사유는 발주 전
소유자와의 논의에서 확정됐다 — 근거는 정의 파일 7개의 제목 구조 실측(공통 뼈대 `# 역할` + `##` 절,
절 이름은 파일마다 상이).

**이 항목은 개정된 검증 구조(2026-08-23, 커밋 153ee0b)의 첫 사이클이다** — `검토대기` 진입 시
메인 루프가 `docs/plans/verification-paths.md`에서 필수 경로를 확정해 여기 기록하고,
편집 라운드 소진 후 `plan-verifier` 첫 독립 패스를 받는다.
