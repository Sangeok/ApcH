# FEAT-17 — 행위자 상세 페이지의 역할 정의를 제목 주도 점진 공개로

## 게이트① (2026-08-23)

사용자가 세션에서 "Feat-17 계획 지시"로 개방. 보드 행을 `승인대기` → `계획지시`로 전이했다.

발주 계약은 백로그 FEAT-17이 원천이다 — 요구 넷: (1) 펜스 밖 `##` 경계 분할 순수 함수(`pm.md:57`의
코드 펜스 안 `## YYYY-MM-DD`를 절로 찢으면 안 된다 — 핵심 테스트 케이스) (2) 첫 덩어리 펼침 +
나머지 절 native `<details>`(FEAT-04 「보고」 피드 선례) (3) 절 이름 문자열에 UI 비결합
(4) 정의 파일 단일 출처 유지 — frontmatter 화면 필드 금지.

설계 대안 비교(이름 기반 특별 취급·frontmatter 필드·현상 유지)와 기각 사유는 발주 전
소유자와의 논의에서 확정됐다 — 근거는 정의 파일 7개의 제목 구조 실측(공통 뼈대 `# 역할` + `##` 절,
절 이름은 파일마다 상이).

**이 항목은 개정된 검증 구조(2026-08-23, 커밋 153ee0b)의 첫 사이클이다** — `검토대기` 진입 시
메인 루프가 `docs/plans/verification-paths.md`에서 필수 경로를 확정해 여기 기록하고,
편집 라운드 소진 후 `plan-verifier` 첫 독립 패스를 받는다.

## 필수 검증 경로 확정 (2026-08-23, 검토대기 진입)

카탈로그 대조 결과 — 트리거가 걸리는 경로 여섯:

| 카탈로그 # | 경로 | 트리거 근거 |
| --- | --- | --- |
| 1 | 인용 전수 대조 | 모든 항목 공통 |
| 2 | 스케치 추출·실행 | §1~§3에 코드 있음. 실제 정의 파일 7개 전부에 분할 함수를 돌린다 |
| 3 | before/after 기계 적용 | 기존 파일 2개(`build-profile-view.ts`·`ui/index.tsx`) 수정 |
| 4 | 전칭 여집합 열거 | "유일한 slice 밖 소비자는 라우트 page"·"`bodyHtml` 참조 없음"·"새 import/export/owner 없음" 전칭 셋 |
| 5 | 돌연변이 검사 | 순수 함수 `outlineDefinitionBody` 신설. 핵심: 펜스 추적 제거 시 pm 픽스처가 실패해야 함 |
| 8 | 실물 렌더 | `AgentProfile` UI 교체 — `<details>` 스택 |

비트리거: 6(외부 신호 해석 없음 — 입력은 저장소 내 정의 파일), 7(새 경계·화이트리스트 없음), 9(schema·config 변경 없음).

## 검증 라운드 (2026-08-23, 메인 루프)

**라운드 1 — 결함 1건.**
- 경로 1(인용 전수): 「현재 동작」·「디자인 방향」·스케치·「테스트」의 `파일:줄` 인용 전부 실측 일치 — `markdown.ts:17-32/66-73/75-81`, `page.tsx:32`, `repo-doc/index.ts:13`, `pm.md:56-63` 펜스, 정의 7개 제목 구조, `globals.css:82-90/11-12`, FEAT-04 details 선례(`:207-214`·`:229-247`·`:231`·`:233`·`:237`), `eslint.config.js:12`, 기존 테스트 `:67-95`. 부수 확인: 렌더러의 미닫힘 펜스 처리(끝까지 소비)와 스케치 토글(끝까지 `inFence`)의 동작 정합.
- 경로 3(before/after): before 조각 4개 현행 파일과 바이트 일치. 스크래치패드에 기계 적용 후 diff가 스케치 hunks에 정확히 갇힘(model +47/−3, ui +27/−5 — 「역할 정의」 블록만).
- 경로 4(전칭 여집합): `bodyHtml` 전 출현 6곳이 「고칠 파일」 3개 안 / slice 밖 소비자는 `page.tsx` 하나(`AgentProfile`·`buildAgentProfileView`만 임포트) / `AgentProfileView` 타입 외부 미사용 → shape 교체 안전 / `renderInline` 재수출 실재(`index.ts:13`).
- 경로 2(스케치 추출·실행): 패치본을 실제 `~` 별칭·실제 repo-doc 모듈로 로드해 스펙 케이스 10 + 실파일 스위프 8(**독립 awk 계산 기대값** — admin-dev 10·backend-dev 10·doc-auditor 8·feature-scout 14·plan-verifier 7·pm 7·web-dev 9절, pm 제목 전수 일치·YYYY-MM-DD 부재) 통과. `tsc --noEmit`(strict·noUncheckedIndexedAccess) 0 에러.
- 경로 8(실물 렌더): `renderToStaticMarkup`으로 4케이스 — intro 카드+details 2개+`<code>` summary+`group-open:rotate-45` 마커, null 정의 시 섹션 부재, intro 없는 정의, **실제 정의 7개 전부 렌더 무예외**.
- 경로 5(돌연변이): 명세를 실행 케이스 10개로 옮겨 6종 심음 → **M3 생존**: `startsWith("```")`→`=== "```"`가 전 케이스 통과. 실해 실측 — 언어 태그 펜스(````ts`)가 실재하는 `admin-dev`·`backend-dev`·`web-dev`에서 각 **절 3개 조용히 소실**(10→7, 10→7, 9→6). 명세의 펜스 케이스가 전부 플레인 ```` ``` ````이었기 때문. → 「테스트」 절에 언어 태그 펜스 단언 추가(계획서 편집 1회).

**라운드 2 — 무편집 무소득 (트리거).** 강화된 명세 반영해 전체 재실행: 23/23 통과, 돌연변이 **6/6 전부 사멸**, 편집이 인용을 건드리지 않음 확인, `git status` 트리 청결(계획서·이 기록 외 무변경). 메인 루프 무소득 라운드 도달 — `plan-verifier` 디스패치 자격 충족. 판정은 독립 패스가 내린다.

## plan-verifier 독립 패스 — 1사이클 (2026-08-23, 사상 첫 실행)

**결함 1건(문서 위생) — 메인 루프 라운드가 놓친 것을 잡았다.** 「디자인 방향」 팔레트 인용
`globals.css:82-90`에 나열된 토큰 5개 중 `--muted-foreground`(실제 `:73`)·`--foreground`(`:63`)가
범위 밖(범위 안은 3개뿐). "기존 토큰만" 주장 자체는 5개 전부 참, 렌더 하니스에서 유틸 해석도
정상 — 줄번호 인용만 어긋남. 나노트 1건: `:207-214`는 「보고」 피드가 아니라 항목 카드
「근거 보기」 details(선례 주장은 성립, 라벨만 부정확).

검증자는 6경로 전부에 경로별 증거를 냈다(브리핑 규약 준수 — 직전 결함 미전달, 저장소 무수정을
`git status`로 메인 루프가 직접 검산: 청결). 특기 실측: M5(절 body trim 제거)가 생존했으나
`renderMarkdown`의 빈줄 정규화로 7파일 전부 `bodyHtml` 바이트 동일 — **등가 돌연변이**로 판정,
명세 구멍 아님. 언어 태그 펜스 실해(각 3절 소실)도 독립 재현으로 계획서 주장과 정확 일치.

**2사이클 편집(메인 루프)**: 팔레트 인용을 토큰별 정확 줄로 교체(`:90`·`:85`·`:86`·`:73`·`:63`),
선례 라벨을 「근거 보기」+「보고」 피드 두 곳으로 정정. 편집분 확인 라운드 무편집(새 인용 다섯과
라벨 전부 이 세션의 실줄 읽기와 일치, 타 절 파급 없음) → 2사이클 독립 패스 디스패치.

## plan-verifier 독립 패스 — 2사이클 (2026-08-23): 결함 0건 → 클린 패스

무편집 무소득. 6경로 전부 증거 첨부, 미실행 경로 없음:
인용 전수(정정된 팔레트 줄 5개 포함) 일치 · 스케치 바이트 동일 + `tsc` CLEAN + **타입체크 lint 레이어를
스크래치패드에 재구성해 실행, 위반 0**(FEAT-10이 걸렸던 `no-unsafe-*`·`prefer-string-starts-ends-with`가
실제로 걸리는 음성 대조 포함, `no-array-index-key`는 next 룰셋에 부재 확인) · before 3블록 정확 1회
매칭·무개입 치환 · 여집합 넷(`bodyHtml` 3파일뿐 / slice 밖 소비자 `page.tsx:6` 하나 / 언어 태그 펜스
3파일뿐 / `<details>` 사용처 2곳뿐) · 돌연변이 5종 전부 사멸 — **1사이클에서 보강한 언어 태그 단언이
M1을 잡음을 독립 재현**(그 단언 없으면 나머지 전부 통과, 실파일 각 3절 소실 수치 일치) · 실물 렌더는
실제 `markdown.ts`·실제 정의(admin-dev 10서랍·pm 7서랍, `## YYYY-MM-DD` summary 미누출).

**수리 검사(메인 루프)**: `git status` 트리 청결(검증자 무수정 직접 검산) · 증거↔브리핑 경로 대조
누락 없음 → 수리. **클린 패스 확정 — 보드에 `검증:` 줄 기록.** 새 정지 규칙의 첫 완주:
편집 라운드(결함 1) → 트리거 → 독립 1사이클(결함 1, 메인 루프가 놓친 것) → 편집 → 독립 2사이클(0건).
