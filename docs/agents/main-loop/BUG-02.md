# BUG-02 — 메인 루프 기록 (게이트 결정·검증 라운드)

## 게이트① 개방 (2026-08-25)

사용자가 세션에서 "진행"으로 `승인대기` → `계획지시` 전이를 결정했다(대화 채널 — 게이트는
"누가"의 제약). 메인 루프가 보드를 편집했고 결정은 사용자의 것이다. backend-dev를 계획서
작성에 디스패치. 참고 근거: 2026-08-25 doc-auditor 감사가 결함 잔존을 실측 확인했다 —
`main.py:534-536` 번역 API 예외 시 `korean_texts = english_texts` 조용한 폴백, 서버 print만
있고 사용자 알림 없음(감사기록 「백로그 유효 확인」).

## 계획서 접수 (2026-08-25)

backend-dev → `docs/plans/BUG-02.md`, 보드 행 `검토대기`(커밋 `96a2565`). 요지: 폴백 세 갈래
(`main.py:516-524` 비-list → 빈 맵 / `:526-532` 줄 단위 / `:534-536` 예외 전량)의 사실이 반환
경계(`:592-593`)에서 소멸 → 콜백(`:1198`)·웹 `normalizeClip`까지 무흔적. 계획은 순수 모듈
`translation_fallback.py`(4함수) + 반환 튜플화 + 콜백에 `subtitleStatus` 순증분 탑재 — 웹 소비는
범위 밖(BUG-03 경계 방식 준용, 백엔드 절반만 닫음을 명시).

## 검증 필수 경로 확정 (2026-08-25, 카탈로그 `docs/plans/verification-paths.md`)

| # | 경로 | 판정 | 근거 |
| --- | --- | --- | --- |
| 1 | 인용 전수 대조 | **필수** | 모든 항목 |
| 2 | 스케치 추출·실행 | **필수** | 순수 모듈·테스트 스케치 — 맨 python 실행 |
| 3 | before/after 기계 적용 | **필수** | `main.py` 수정 조각 |
| 4 | 전칭 여집합 열거 | **필수** | "호출부는 `:839` 하나뿐"·"폴백은 세 갈래 전부"·유실 사슬 전칭 |
| 5 | 돌연변이 검사 | **필수** | 판정 로직 신설(parse·assemble·classify·is_fallback) |
| 6 | 실제 사건 재생(변형) | **필수** | 외부 신호(Gemini 응답) 해석 — 실제 응답 형상(펜스·`{"error":...}`·비-list·부분 누락)을 파서에 통과 + **기존 인라인 로직과의 출력 동치 비교** |
| 7 | 음성 시험 | 제외 | 경계·화이트리스트 신설 없음 |
| 8 | 실물 렌더 | 제외 | 화면 변경 없음 |
| 9 | 구조적 아티팩트 | 제외 | schema·config 없음(이미지 체인은 3이 덮음) |
