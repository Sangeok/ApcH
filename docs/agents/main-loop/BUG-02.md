# BUG-02 — 메인 루프 기록 (게이트 결정·검증 라운드)

## 게이트① 개방 (2026-08-25)

사용자가 세션에서 "진행"으로 `승인대기` → `계획지시` 전이를 결정했다(대화 채널 — 게이트는
"누가"의 제약). 메인 루프가 보드를 편집했고 결정은 사용자의 것이다. backend-dev를 계획서
작성에 디스패치. 참고 근거: 2026-08-25 doc-auditor 감사가 결함 잔존을 실측 확인했다 —
`main.py:534-536` 번역 API 예외 시 `korean_texts = english_texts` 조용한 폴백, 서버 print만
있고 사용자 알림 없음(감사기록 「백로그 유효 확인」).

## 계획서 접수 (2026-08-25)

backend-dev → `docs/plans/BUG-02.md`, 보드 행 `검토대기`(커밋 `96a2565`). 요지: 폴백 세 갈래
(`main.py:516-524` 비-list → 빈 맵 / `:526-532` 줄 단위 / `:534-536` 예외 전량)의 사실이 반환
경계(`:592-593`)에서 소멸 → 콜백(`:1198`)·웹 `normalizeClip`까지 무흔적. 계획은 순수 모듈
`translation_fallback.py`(4함수) + 반환 튜플화 + 콜백에 `subtitleStatus` 순증분 탑재 — 웹 소비는
범위 밖(BUG-03 경계 방식 준용, 백엔드 절반만 닫음을 명시).

## 검증 필수 경로 확정 (2026-08-25, 카탈로그 `docs/plans/verification-paths.md`)

| # | 경로 | 판정 | 근거 |
| --- | --- | --- | --- |
| 1 | 인용 전수 대조 | **필수** | 모든 항목 |
| 2 | 스케치 추출·실행 | **필수** | 순수 모듈·테스트 스케치 — 맨 python 실행 |
| 3 | before/after 기계 적용 | **필수** | `main.py` 수정 조각 |
| 4 | 전칭 여집합 열거 | **필수** | "호출부는 `:839` 하나뿐"·"폴백은 세 갈래 전부"·유실 사슬 전칭 |
| 5 | 돌연변이 검사 | **필수** | 판정 로직 신설(parse·assemble·classify·is_fallback) |
| 6 | 실제 사건 재생(변형) | **필수** | 외부 신호(Gemini 응답) 해석 — 실제 응답 형상(펜스·`{"error":...}`·비-list·부분 누락)을 파서에 통과 + **기존 인라인 로직과의 출력 동치 비교** |
| 7 | 음성 시험 | 제외 | 경계·화이트리스트 신설 없음 |
| 8 | 실물 렌더 | 제외 | 화면 변경 없음 |
| 9 | 구조적 아티팩트 | 제외 | schema·config 없음(이미지 체인은 3이 덮음) |

## 검증 1라운드 (2026-08-25, reconciling — Standard 프로파일·전체 증거) — 결함 0건 (편집 0회)

하니스: 스크래치패드 `bug02/` (모듈·테스트 추출본, 동치+돌연변이 러너).

- **1 인용 전수**: `:392`·`:414-458`·`:477`·`:497-504`·`:514-536`(before 바이트 일치)·`:538-583`·
  `:592-593`·`:818-819`·`:839`·`:859-869`·`:1019`·`:1189`·`:1192-1199`·`:25-32`·`:66-67`·
  `:389-390` 전부 내용 일치. 유실 사슬의 웹 끝단 `normalizeClip`(`route.ts:131-154`)도 실측 —
  알려진 키만 담고 미지 키 버림, **route.ts에 zod/strict 검증 부재**(grep 0건)라 `subtitleStatus`
  순증분이 웹을 깨지 않는다는 주장 성립.
- **4 여집합**: 한국어 함수 호출부 grep 전수 — `:839` 하나뿐(전칭 성립). 함수 내부 return도
  `:593` 하나뿐(튜플화 완전). `english_texts` 폴백 지점은 세 갈래(`:527-536`)뿐.
  before 조각 5종 전부 유일 출현(기계 적용 가능). 신규 파일명 충돌 없음.
- **2 스케치 추출·실행**: 순수 모듈+명세 테스트를 맨 python으로 — **19/19 OK**.
- **6 실사건 재생(변형) — 구·신 동치 스윕**: 기존 인라인(`:516-532`)을 재현해 실제 Gemini 응답
  형상 15케이스(정상·`{"error":"cannot-translate"}`·비-list·None·부분 누락·빈/공백 번역·비-dict·
  비-int index·비-str translation·중복 index 후자 승·범위 밖 index·**bool index 기벽(True==1)**·
  빈 입력)에 통과 — **렌더 출력 15/15 동일**, missing 집계도 정의 검산 통과. 계획의 "동치" 주장 실증.
- **5 돌연변이 10종**: strip 제거·빈문자 저장·falsy 가드 약화·missing 수집 제거·0줄 우선 제거·
  `>=`→`>`·hard_failure 무시·is_fallback partial 누락·상수 드리프트·비-list 통과 — **전원 사멸**.
- 부수: `subtitle_status`가 try/except 양 경로에서 정의됨(`english_texts`는 try 밖 `:458`) ✓,
  §2 import 4심볼 전부 사용처 있음 ✓, 검증 명령은 BUG-03과 동일 러너(합산 34 test 예상) ✓.

## 검증 2라운드 (2026-08-25) — 무편집 확인 패스 (편집 0건, 결함 0건)

계획서 blob `c8f6080` = HEAD 불변(1라운드가 편집 0회였으므로 저장본 동일). Coverage Stability —
최고 영향 전칭("호출부 하나뿐")을 함수명 grep과 별개로 return 전수·`english_texts` 참조 전수로
교차 확인. **판정: 메인 루프 무소득 — plan-verifier 디스패치 자격 성립.**

## plan-verifier 독립 무편집 패스 — 1사이클 (2026-08-25) — 결함 0건, 클린 패스 확정

새 컨텍스트가 필수 경로 6개를 전부 실행하고 무소득 보고. 실질: 인용 전수 일치(웹 route.ts 포함) /
스케치 추출 `py_compile` + 명세 26단언 `OK` + 실트리 러너 확인 / **before 6조각 유일 매치 →
기계 적용 → 조립본 `py_compile` 통과**(메인 루프 라운드보다 한 걸음 더 — 적용 후 컴파일까지) /
여집합 4종(호출부·세 갈래·비엄격 웹 스키마 — TS interface는 런타임 소거임까지 짚음·양 경로 대입) /
돌연변이 **13/13 사멸** / 구·신 동치 **23/23 형상 동일**. 트리 무수정 — 메인 루프가 `git status`
직접 검산(청결, HEAD `a9ef6bc` 불변).

**판정: 클린 패스 (2026-08-25, 독립 무편집 1라운드).** 보드에 `검증:` 줄 기록. 게이트② 대기.
비고: BUG-03에 이어 backend-dev 계획서가 검증자 1사이클을 무결로 통과 — 두 사이클의 교정
패턴이 계획 품질로 승계되고 있다는 신호.

## 게이트② 개방 (2026-08-25)

사용자가 세션에서 "구현 승인"으로 `구현승인` 전이를 결정했다(대화 채널). 메인 루프가 보드를
편집했고 결정은 사용자의 것이다. `검증:` 줄 유지(전진 전이). backend-dev를 구현으로 디스패치.
배포는 BUG-03과 묶어 한 번의 `modal deploy`로 처리하는 안을 사용자에게 제시한 상태 —
구현 인수 후 원장 등재 시 함께 정리한다.

## 완료 인수 (2026-08-25) — 다섯 조건 직접 재현, 전부 통과

1. **변경 파일 ↔ 「고칠 파일」**: 신규 2 + main.py 수정, 표와 1:1. 표 밖 없음.
2. **diff ↔ 스케치**: `translation_fallback.py`는 계획서 §1과 **바이트 동일**(기계 대조).
   main.py 추가 18줄 전부 스케치 원문(§2 import·§5 체인 2인자·§3 교체+튜플 반환·§4 초기화/
   언패킹/dict 필드).
3. **검증 재실행**: `Ran 40 tests ... OK`(기존 15 + 신규 25, N 육안 확인)·`py_compile` exit 0 — 직접.
4. **백로그 제거**: BUG-02 소멸, **BUG-04 3줄 무결 직접 확인**(섹션 헤더 바로 아래로 승격).
5. **상세 기록 실재**: `docs/agents/backend-dev/BUG-02.md`(4,396B) — 주장 전부 직접 재검증과 일치.

런북 8단계: 못 덮는 범위 3줄을 원장 BUG-02 절로 등재(배포는 BUG-03과 묶음 명시).
status `완료` 수리. Backend / Pipeline 백로그 잔여는 BUG-04 하나.
