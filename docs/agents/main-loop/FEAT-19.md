# FEAT-19 — 배포 확인 원장 도입 (메인 루프 직접 구현)

## 발주 경위 (2026-08-24)

사용자가 "파이프라인에 보완·확장이 아니라 신규로 추가할 것"의 파악을 지시했고, 메인 루프의
제안 넷 중 ①(배포 후 확인 단계)을 승인했다("좋다 진행하자"). 게이트①② 미경유 —
루트 문서 작업은 담당 dev가 없고, FEAT-12 전례(소유자 발주 → 메인 루프 직접 구현 → 보드 기록)를
따랐다. 코드 무변경.

## 발주 근거가 된 관측

`못 덮` grep 실측: 계획서 13건 + 구현 보고 6건 + 제안서 1건이 「배포 후 수동 확인」 목록을
선언했으나, 그 마감 기록은 저장소 전체에서 0건. 파이프라인의 검증 엄밀함(검증 라운드·돌연변이
검사·실물 렌더)이 merge 시점에서 끊기고, 실물 화면 확인은 원장 없는 부채로만 쌓이고 있었다.

## 만든 것

| 산출물 | 내용 |
| --- | --- |
| `docs/release-checks.md` | 신설. 배포 확인 원장 — 규칙(등재·마감 3종) + 완료 16건 백필 |
| `CLAUDE.md` | 문서 지도에 원장 행 추가, 절차에 8단계(등재·마감) 삽입 — 기존 8은 9로 |
| `PROJECT_BOARD.md` | FEAT-19 행(완료, agent: main-loop) |

## 설계 결정

1. **원장 위치는 `docs/` 루트.** `docs/agents/`는 활동 기록(append-only)이고 원장은 상태
   문서(체크박스 갱신형)라 규약이 충돌한다 — `docs/agents/README.md`의 상태/활동 구분 자체가
   근거다. 보드에 넣지 않은 이유는 FEAT-12 감압 취지(보드는 요약만).
2. **마감은 증거 3종으로만** — 확인(실물 관측)·대체(후속 항목의 화면 교체·재선언)·이관(백로그).
   "확인했다" 한 줄 금지는 `verification-paths.md` 증거 형식과 같은 원칙이다.
3. **백필 16건**: FEAT-18·17·16·15·14·13·12·10·09·08·07·06·04·03·02·BUG-05.
   원천 우선순위는 구현 보고 > 보드 `결과` > 계획서. BUG-05는 보드 행이 없으나 구현 커밋
   `1a38e1e`(2026-08-05)와 계획서의 수동 검증 시나리오가 실재해 포함했다.
   BUG-06은 제외 — 선언(카피↔로직 사람 대조)이 구현 시점에 닫히는 성격이다.
4. **백필에서 닫은 것**: FEAT-03·04의 명령 경로(fetch·POST·토스트)는
   `docs/proposals/active/remote-agent-pipeline-generalization.md` Verification Results ③
   (2026-08-15, 버튼→토스트→코멘트→답글 전 구간 관측)으로 확인 마감. 대체 마감은 FEAT-06 전부,
   FEAT-04 TeamZone·세리프 폴백, FEAT-07 반응형(5→7책상), FEAT-03 카드 렌더.
   **FEAT-08 도장 실사용은 닫지 않았다** — FEAT-09 근거의 "도장으로 열 예정(첫 실측 겸)"은
   예정 기록일 뿐 실측 결과 기록을 못 찾았다. 소유자가 실사용 기억으로 닫을 수 있다.
5. **보드 `agent: main-loop` 안전성 확인**: 대시보드 `known-agents.ts:48` `identityFor`가
   roster 밖 id를 `{handle: id, role: "에이전트"}`로 폴백 렌더한다 — 페이지가 깨지지 않는다.
   책상은 roster 고정 7이라 새 책상도 생기지 않는다.

## 열어 둔 것

- **확장형(release-verifier 에이전트) 미결정.** 원장의 기계 판정 가능한 줄(렌더·반응형·폴링)을
  Playwright로 배포 화면에 실행하는 보고-only 에이전트. 로스터 8인화는 대시보드 재동기화
  (FEAT-18 계열 admin 항목) 비용이 따라온다 — 소유자 결정 대기.
- 원장 백필 중 미확인 줄들의 실제 마감 — 소유자의 실물 관측 몫.

## 2026-08-24 — 1차 스윕 (메인 루프 직접, Playwright)

소유자가 "전부 한 번도 확인 안 했다"고 밝혀 기억 기반 마감이 불가 → 메인 루프가 이 세션에서
Playwright(MCP)로 admin 프로덕션을 직접 스윕했다. 에이전트 신설 없이 실행 — 방식 검증을 겸했다.

**인증**: 소유자가 Playwright 영속 프로필에서 Google 로그인 1회 → NextAuth 세션 쿠키가
프로필에 남는다. **브라우저 종료·재기동 후에도 세션 유지 실측** — "1안(프로필 세션)" 성립.
만료(약 30일) 시 재로그인 1회가 유지비 전부다.

**실측 결과** (원장에 반영, 순수 기계 확인만 체크):

| 확인 | 방법·결과 |
| --- | --- |
| raw CDN `.claude/agents/*.md` 서빙 | curl 200 ×2 (backend-dev·plan-verifier) |
| 비로그인 `/pipeline` → `/login` | 리다이렉트 실측 |
| 폰 375px 가로 스크롤 | scrollWidth 360 = clientWidth 360, 넘침 0 |
| 폰 책상 격자 | `display: grid`, `150.5px 150.5px` — 2열 실측 |
| `/pipeline/agents/pm` | 렌더·frontmatter 요약·빈 기록 상태·GFM 표/코드 정상 |
| roster 밖 프로필 | `/pipeline/agents/backend-dev` → 404 (배포판 로스터 5인 기준 정상) |
| 문서 뷰어 | `/pipeline/docs/plans/FEAT-16` h1+prose 18블록+형제 탭 3 렌더 |
| 화이트리스트 밖 docs | `/pipeline/docs/secrets/nope` → 404 |
| 실행 콘솔 | 여집합 보드에서 비활성 버튼+게이트대기 설명+5분 카피+pill idle — 결정 사슬대로 |
| 폴링 | 35초 관찰, 신규 콘솔 에러 0 |
| `<details>` 펼침 | 결재함 "근거 보기" 펼침 실측 |
| contents API·기록 N건 | 책상 "기록 5건/1건" 실표시 |
| BudgetFlag | "150자 초과" 칩 실렌더 (FEAT-19 행은 예산 내 — 무칩) |
| `agent: main-loop` 폴백 | 보고 피드에 "M" 아바타로 안전 렌더 — 설계 결정 6 실증 |

**막힌 것과 이유**: FEAT-17·18은 **배포 전** — main 마지막 합류가 PR #100이라 프로덕션에
그 코드가 없다(책상 5개·역할 정의 전문 덤프로 실측 확인). dev→main 합류 후 재스윕.
web 3건(FEAT-16·02·BUG-05)은 web 로그인 세션 필요 — 미스윕. 외부 효과 버튼(도장·반려·명령)은
클릭하지 않았다 — 실사용 시 자연 마감. 시각 판정 줄은 스크린샷 3장(데스크톱·폰·뷰어)을
소유자에게 전달, 판정 대기.

**부수 관찰(원장 밖)**: ① `favicon.ico` 404 (사소). ② 보고 피드가 FEAT-18의 누적 `결과:` 2줄 중
첫 줄(계획서 작성 완료)을 표시하고 150자 초과 칩이 붙는다 — 누적 결과 표시 방식은 의도 확인 필요.

## 2026-08-24 — 2차 스윕: 시각 판정 (메인 루프 직접, 스크린샷 판독 + 시안 대조)

소유자가 "스크린샷을 네가 판단할 수 없냐"고 물어 메인 루프가 직접 판독했다. 취향 추측이 아니라
**저장소의 계약 문서 대조**로 판정했다 — FEAT-07 승인 시안(`docs/design/FEAT-07/pixel-office-mock.html`,
v7)을 로컬 서버로 렌더해 실물과 나란히 대조.

**판정 합격(원장 마감)**:

- FEAT-07 픽셀 사무실 = 시안 v7 일치 — 그림체(머리·상의 색·소품)·말풍선 형태·명패(검정 바탕
  흰 픽셀 폰트)·체커 바닥·벽·액자·화분·픽셀 버튼·폰 2열. 명패 폭은 시안 ⑥이 명시 허용.
- FEAT-14 뷰어 시각 — 명조 제목·오커 인용 블록·모노 코드·서류철 탭 활성·배지·375px 래핑 정상.
  행위자 기록 문서(`agents/admin-dev/FEAT-18`) 렌더도 실측 추가.
- FEAT-15 `.doc-prose`·FEAT-04 색토큰/세리프/line-clamp·FEAT-10 헤더 배치 — 스크린샷 판정 합격.
- FEAT-08 임프린트 렌더·라벨 가독, FEAT-09 여백 펜 메모 시각 — 합격(각 줄의 잔여는 hover/클릭 동작).

**발견 1건**: 폰에서 "당신의 책상" 배너 SVG가 비율 축소돼 라벨이 매우 작다. 시안 v7의 폰 데모에는
배너가 없어(사무실만 있음) 계약 위반은 아니다 — 개선 후보로 원장에 관찰 기록, 백로그 이관은 소유자 몫.

시각 판정의 한계 명시: 이 판정은 "계약 문서·일반 품질 기준 부합"이다. 소유자 취향의 최종 거부권은
남는다(FEAT-06 전례) — 이의가 있으면 해당 줄을 재개하고 이관한다.

## 2026-08-24 — 3차 스윕: PR #101 합류 직후 재스윕

소유자가 PR #101(dev→main)을 머지(12:24Z)해 FEAT-17·18이 배포됐다. 합류 전 404였던
`/pipeline/agents/backend-dev`가 200으로 전환된 것을 배포 완료 신호로 삼아 재스윕했다.

- **FEAT-18 6줄 중 5줄 마감**: 7책상 실렌더(신규 스프라이트·명패·소품), 프로필 라우트 2종 개방,
  폰 2열(143px×2)·넘침 0, 새 hex 색 판정 합격. 특히 말풍선 파생은 **실보드 라이브로 관측** —
  BUG-03이 마침 검토대기가 되어 plan-verifier "검증 중", FEAT-20 계획지시로 admin-dev "작업 중"이
  동시에 떴다. 잔여 1줄: backend-work 버튼 클릭(외부 효과 — 실사용 시).
- **FEAT-17 4줄 중 2줄 마감**: 10절 접힘·펼침·마커 회전 실측(주의 — Tailwind v4는 `rotate-45`를
  `transform`이 아닌 `rotate` 속성으로 방출한다. 첫 검사가 `transform`을 읽어 거짓 결함이 될 뻔했고,
  속성 재확인으로 해소). 잔여: hover 시각·실기기 폰 폴백.
- **FEAT-13 점선 「검증 전」 칩 라이브 확인**: BUG-03 검토대기 카드에 점선 칩, 승인대기 카드엔 없음 —
  조건부 양·음성 동시. 실선 「검증 통과」는 BUG-03 검증 후 자연 확인.
- **FEAT-14 잔여 전부 마감**: 카드 "계획서 →" 클릭 경유 → 뷰어가 방금 푸시된 BUG-03 계획서 렌더.
  게이트 가시성(검토대기 카드의 구현승인 도장+칩)도 관측.
- **FEAT-12 DeskReports 줄 재판정**: 미확인이 아니라 **대체**였다 — FEAT-14·15가 그 details 표면을
  "기록 N건" 문구+프로필 페이지로 교체해 확인할 대상 자체가 없다.
- FEAT-10 동적 라벨의 활성 변형("FEAT-20 계획서 작성")도 실보드에서 관측 — 여집합 비활성(1차)과
  합쳐 라벨 도출 양쪽 다 실증.

## 4차 스윕 (2026-09-03) — 클린코드 77건 배포 직후, 공개 라우트만

PR #111(2026-09-03 11:02 KST)로 `dev`가 `main`에 합류해 클린코드 개선 77건(159파일)·FEAT-28·29가
프로덕션에 나갔다. 그 직후 **로그인이 필요 없는 web 공개 라우트만** 훑었다. 나머지(대시보드·결제·
워커·admin 클릭·실기기 폰)는 사용자 손이 필요해 이 스윕에서 제외했다.

방법: `curl`로 프로덕션 응답을 직접 읽었다 — 상태코드·리다이렉트 수·`url_effective`·본문 h1·
`<header>`/`<footer>` 태그·내부 링크 집합. 브라우저 렌더는 쓰지 않았다(정적 마케팅 라우트라 응답
본문에 콘텐츠가 다 실린다).

- **C-36/C-37 마감**: `/about`·`/contact`·`/security`·`/how-it-works`·`/changelog` 다섯 전부 200,
  리다이렉트 0(URL 유지), h1 실렌더. nav 내부링크는 `/about`이 방출하는 18개 경로를 전수 요청해
  전부 200 — 마케팅 5개 + `/`·`/features`·`/pricing`·`/login`·`/privacy`·`/terms` + SEO 6개
  (`/ai-podcast-clipper`·`/compare`·`/guides`·`/podcast-to-shorts`·`/product-tour`·
  `/youtube-shorts-generator`). **한계**: "분할 전과 같은 화면"의 픽셀 대조는 분할 전 스크린샷이
  없어 불가능하다. 이 줄을 닫은 근거는 라우트 생존·헤더/푸터 동반·본문 렌더·링크 전수 생존까지다.
- **C-72/C-73 절반**: `/privacy`·`/terms`가 200·리다이렉트 0·`<header>`/`<footer>` 각 1개·내부링크
  18개(마케팅 나머지와 동일 집합)·h1 "Privacy Policy"/"Terms of Service". 남은 절반(로그인 상태의
  헤더 아바타 일치)은 웹 세션이 필요해 열어 뒀다.
- **C-73 절반**: `/features` 응답 본문에 `public_header` 1건, 옛 `site_header` **0건**. 라벨 교체
  자체는 실물에서 확인됐고, 클릭이 실제로 `cta_clicked`를 그 값으로 기록하는지는 분석 백엔드
  관측이라 남겼다.

원장 문서 정정도 함께 했다 — 클린코드·FEAT-29·FEAT-28 세 절의 머리말이 아직 "**배포 대기** —
`dev`에 커밋, `main` 합류 전"이었다. PR #111 합류로 낡은 문구였고 셋 다 배포 완료로 고쳤다.
FEAT-29의 「배포 전 전제 — 전환 구간 공백」 줄에는 배포가 이미 지나갔다는 사실과 남은 확인이
"그 순간 `processing`인 업로드가 있었나" 하나뿐임을 붙였다.

미마감 89 → 88.
