# FEAT-19 — 배포 확인 원장 도입 (메인 루프 직접 구현)

## 발주 경위 (2026-08-24)

사용자가 "파이프라인에 보완·확장이 아니라 신규로 추가할 것"의 파악을 지시했고, 메인 루프의
제안 넷 중 ①(배포 후 확인 단계)을 승인했다("좋다 진행하자"). 게이트①② 미경유 —
루트 문서 작업은 담당 dev가 없고, FEAT-12 전례(소유자 발주 → 메인 루프 직접 구현 → 보드 기록)를
따랐다. 코드 무변경.

## 발주 근거가 된 관측

`못 덮` grep 실측: 계획서 13건 + 구현 보고 6건 + 제안서 1건이 「배포 후 수동 확인」 목록을
선언했으나, 그 마감 기록은 저장소 전체에서 0건. 파이프라인의 검증 엄밀함(검증 라운드·돌연변이
검사·실물 렌더)이 merge 시점에서 끊기고, 실물 화면 확인은 원장 없는 부채로만 쌓이고 있었다.

## 만든 것

| 산출물 | 내용 |
| --- | --- |
| `docs/release-checks.md` | 신설. 배포 확인 원장 — 규칙(등재·마감 3종) + 완료 16건 백필 |
| `CLAUDE.md` | 문서 지도에 원장 행 추가, 절차에 8단계(등재·마감) 삽입 — 기존 8은 9로 |
| `PROJECT_BOARD.md` | FEAT-19 행(완료, agent: main-loop) |

## 설계 결정

1. **원장 위치는 `docs/` 루트.** `docs/agents/`는 활동 기록(append-only)이고 원장은 상태
   문서(체크박스 갱신형)라 규약이 충돌한다 — `docs/agents/README.md`의 상태/활동 구분 자체가
   근거다. 보드에 넣지 않은 이유는 FEAT-12 감압 취지(보드는 요약만).
2. **마감은 증거 3종으로만** — 확인(실물 관측)·대체(후속 항목의 화면 교체·재선언)·이관(백로그).
   "확인했다" 한 줄 금지는 `verification-paths.md` 증거 형식과 같은 원칙이다.
3. **백필 16건**: FEAT-18·17·16·15·14·13·12·10·09·08·07·06·04·03·02·BUG-05.
   원천 우선순위는 구현 보고 > 보드 `결과` > 계획서. BUG-05는 보드 행이 없으나 구현 커밋
   `1a38e1e`(2026-08-05)와 계획서의 수동 검증 시나리오가 실재해 포함했다.
   BUG-06은 제외 — 선언(카피↔로직 사람 대조)이 구현 시점에 닫히는 성격이다.
4. **백필에서 닫은 것**: FEAT-03·04의 명령 경로(fetch·POST·토스트)는
   `docs/proposals/active/remote-agent-pipeline-generalization.md` Verification Results ③
   (2026-08-15, 버튼→토스트→코멘트→답글 전 구간 관측)으로 확인 마감. 대체 마감은 FEAT-06 전부,
   FEAT-04 TeamZone·세리프 폴백, FEAT-07 반응형(5→7책상), FEAT-03 카드 렌더.
   **FEAT-08 도장 실사용은 닫지 않았다** — FEAT-09 근거의 "도장으로 열 예정(첫 실측 겸)"은
   예정 기록일 뿐 실측 결과 기록을 못 찾았다. 소유자가 실사용 기억으로 닫을 수 있다.
5. **보드 `agent: main-loop` 안전성 확인**: 대시보드 `known-agents.ts:48` `identityFor`가
   roster 밖 id를 `{handle: id, role: "에이전트"}`로 폴백 렌더한다 — 페이지가 깨지지 않는다.
   책상은 roster 고정 7이라 새 책상도 생기지 않는다.

## 열어 둔 것

- **확장형(release-verifier 에이전트) 미결정.** 원장의 기계 판정 가능한 줄(렌더·반응형·폴링)을
  Playwright로 배포 화면에 실행하는 보고-only 에이전트. 로스터 8인화는 대시보드 재동기화
  (FEAT-18 계열 admin 항목) 비용이 따라온다 — 소유자 결정 대기.
- 원장 백필 중 미확인 줄들의 실제 마감 — 소유자의 실물 관측 몫.

## 2026-08-24 — 1차 스윕 (메인 루프 직접, Playwright)

소유자가 "전부 한 번도 확인 안 했다"고 밝혀 기억 기반 마감이 불가 → 메인 루프가 이 세션에서
Playwright(MCP)로 admin 프로덕션을 직접 스윕했다. 에이전트 신설 없이 실행 — 방식 검증을 겸했다.

**인증**: 소유자가 Playwright 영속 프로필에서 Google 로그인 1회 → NextAuth 세션 쿠키가
프로필에 남는다. **브라우저 종료·재기동 후에도 세션 유지 실측** — "1안(프로필 세션)" 성립.
만료(약 30일) 시 재로그인 1회가 유지비 전부다.

**실측 결과** (원장에 반영, 순수 기계 확인만 체크):

| 확인 | 방법·결과 |
| --- | --- |
| raw CDN `.claude/agents/*.md` 서빙 | curl 200 ×2 (backend-dev·plan-verifier) |
| 비로그인 `/pipeline` → `/login` | 리다이렉트 실측 |
| 폰 375px 가로 스크롤 | scrollWidth 360 = clientWidth 360, 넘침 0 |
| 폰 책상 격자 | `display: grid`, `150.5px 150.5px` — 2열 실측 |
| `/pipeline/agents/pm` | 렌더·frontmatter 요약·빈 기록 상태·GFM 표/코드 정상 |
| roster 밖 프로필 | `/pipeline/agents/backend-dev` → 404 (배포판 로스터 5인 기준 정상) |
| 문서 뷰어 | `/pipeline/docs/plans/FEAT-16` h1+prose 18블록+형제 탭 3 렌더 |
| 화이트리스트 밖 docs | `/pipeline/docs/secrets/nope` → 404 |
| 실행 콘솔 | 여집합 보드에서 비활성 버튼+게이트대기 설명+5분 카피+pill idle — 결정 사슬대로 |
| 폴링 | 35초 관찰, 신규 콘솔 에러 0 |
| `<details>` 펼침 | 결재함 "근거 보기" 펼침 실측 |
| contents API·기록 N건 | 책상 "기록 5건/1건" 실표시 |
| BudgetFlag | "150자 초과" 칩 실렌더 (FEAT-19 행은 예산 내 — 무칩) |
| `agent: main-loop` 폴백 | 보고 피드에 "M" 아바타로 안전 렌더 — 설계 결정 6 실증 |

**막힌 것과 이유**: FEAT-17·18은 **배포 전** — main 마지막 합류가 PR #100이라 프로덕션에
그 코드가 없다(책상 5개·역할 정의 전문 덤프로 실측 확인). dev→main 합류 후 재스윕.
web 3건(FEAT-16·02·BUG-05)은 web 로그인 세션 필요 — 미스윕. 외부 효과 버튼(도장·반려·명령)은
클릭하지 않았다 — 실사용 시 자연 마감. 시각 판정 줄은 스크린샷 3장(데스크톱·폰·뷰어)을
소유자에게 전달, 판정 대기.

**부수 관찰(원장 밖)**: ① `favicon.ico` 404 (사소). ② 보고 피드가 FEAT-18의 누적 `결과:` 2줄 중
첫 줄(계획서 작성 완료)을 표시하고 150자 초과 칩이 붙는다 — 누적 결과 표시 방식은 의도 확인 필요.
