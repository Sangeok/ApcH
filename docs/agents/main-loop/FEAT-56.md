# FEAT-56 — 메인 루프 기록

## 필수 경로 확정 (2026-09-21)

**일곱**을 골랐다. FEAT-53·55와 달리 **경로 8(실물 렌더)이 들어간다** — JSX가 바뀐다.

| # | 경로 | 판정 | 이유 |
| --- | --- | --- | --- |
| 1 | 인용 전수 대조 | **채택** | 모든 항목 |
| 2 | 스케치 추출·실행 | **채택** | tsx/ts/js 코드 블록 22개 |
| 3 | before/after 기계 적용 | **채택** | 기존 파일 수정 11개 |
| 4 | 전칭 여집합 열거 | **채택** | 「소비자 0」·「정의1+재수출2뿐」·「38·54에만」·「사용처는 …뿐」·「여기 없는 파일은 고치지 않는다」 |
| 5 | 돌연변이 검사 | **채택** | 순수 모듈에서 **함수를 지운다**. 삭제 항목의 고유 위험은 「남은 명세가 비는 것」이고, 그건 돌연변이로만 보인다 |
| 7 | 음성 시험 | **채택** | 계획이 「`sample=false` 도달 경로 0」과 「`previewCaptionCues`는 항등」이라는 불변식에 기댄다 |
| 8 | 실물 렌더 | **채택** | `CaptionStyleEditor`의 JSX 삼항을 지운다. 「컴파일 통과 ≠ 렌더 성공」이 그대로 트리거다 |
| 6 | 실제 사건 재생 | **비채택** | 외부 신호 해석 없음 |
| 9 | 구조적 아티팩트 | **비채택** | schema·config·생성 파일 변경 없음 |

## 라운드 1 — 일곱 경로 전수 (소득 2건)

하니스: `scratchpad/feat56/`.

**경로 1.** 인용 **24건**을 (파일, 줄, 기대 부분문자열)로 기계 대조 — **불일치 0**.
수 주장도 확인했다: `ClipDraftCard` 전문의 `playUrl` 출현 = `[38, 54]` 정확히 둘,
`readyPlayUrl` = `[100, 228, 454]`, test describe = `[13, 55, 96]`, `it` 총 **14**.

「현재 동작」이 describe를 `:55`·`:96`으로, 「구현 스케치」가 삭제 범위를 `:53-92`·`:94-117`로
적어 **달라 보이지만 모순이 아니다** — `:53`·`:94`는 각 describe의 선행 주석이고, 삭제 범위가
그것을 포함하는 쪽이 옳다(안 그러면 고아 주석이 남는다).

**경로 3.** before **14개**(펜스 블록 11 + 인라인 3)가 각각 트리에 **정확히 1회** 출현.
전부 손 개입 없이 after로 치환됐다.

**경로 2 — 실제 게이트 실행.** 치환본을 트리에 적용해 게이트 둘을 직접 돌렸다.

| 게이트 | 결과 |
| --- | --- |
| `npm run check -w apps/web` | **EXIT 0 · `✔ No ESLint warnings or errors`** — 백로그 요구 ②a(경고 0) 충족. `playUrl` 경고 소멸 |
| `npm test -w apps/web` | **tests 170 / suites 40 / fail 0** — 계획서 예측과 **정확히** 일치 |

기대값 170/40의 전제도 따로 검산했다. `describe` 총수 **42 == suites 42**(러너가 describe를
suite로 센다), `it` 172 + `test(` 6(`middleware.test.mjs` 3 · `event-catalog.test.mjs` 3) = **178**.
제거 구간의 `it`은 **평범한 8개, 루프 0** — 따라서 178−8=170, 42−2=40이 산술로 선다.

**경로 8 — 실물 렌더.** 계획서의 before/after JSX를 esbuild로 트랜스파일해
`renderToStaticMarkup`으로 실제 렌더했다(React 19).

| 입력 | 마크업 동일 |
| --- | --- |
| `sample=true` · English | **true** |
| `sample=true` · Korean | **true** |
| `sample=false` · English | false |
| `sample=false` · Korean | false |

**앞 둘이 이 항목의 「동작 무변경」 증명이고, 뒤 둘이 「지운 것이 실재했다」는 증명이다** —
빈 분기를 지운 게 아니라 실제로 다른 마크업을 내던 분기를 지웠다.

**경로 7 — 음성 시험(등가 증명).** 계획이 기대는 「`previewCaptionCues`는 `sample=true`에서
항등」을 `git show HEAD:`의 **원본 함수**로 실행 검증했다. 언어 English·Korean·Japanese·빈문자열
넷 모두 `out === cues`(동일 객체) — `buildCaptionCues` 직접 호출과 동치다.

반대 방향도 봤다: 라이브 경로의 실제 입력(**영어 전사 + Korean 업로드**)에서
`sample=false`는 첫 큐를 `"Style your captions"` → `"지금 자막 스타일을"`로 **실제 치환한다.**
지우는 것이 no-op이 아니었다.

> 첫 시도는 한국어 입력에 한국어 치환을 걸어 「치환이 안 일어난다」는 거짓 음성을 냈다.
> 하니스 결함이었고, 입력을 라이브 경로의 실제 모양으로 바꿔 다시 돌렸다.

**경로 5 — 돌연변이 검사.** 삭제 항목의 고유 위험은 「남은 명세가 비는 것」이다.
남은 4 export(`SAMPLE_CAPTION_CLIP_END`·`sampleCaptionWords`·`firstCueText`·`firstSampleCueText`)에
돌연변이 4종을 심었다.

| 돌연변이 | 남은 6케이스 |
| --- | --- |
| M1 `firstSampleCueText`가 항상 빈 문자열 | 사멸 |
| M2 `sampleCaptionWords`가 언어 무시 | 사멸 |
| M3 단어 `start: i → i+1` | **생존** |
| M4 단어 `end: i+0.9 → i+0.4` | **생존** |

M3·M4 생존이 이 삭제 탓인지 가리려고 **삭제 전 원본 14케이스**로 같은 돌연변이를
전체 스위트에 대고 다시 돌렸다 — **둘 다 똑같이 생존**한다.
**타이밍 구멍은 원래 있던 것이고 이 항목이 만든 게 아니다. 삭제로 잃은 커버리지 0.**

**경로 4 — 전칭 여집합.** 적용본에서 `previewCaptionCues`·`koreanSampleCues`·`playUrl={readyPlayUrl}`
전역 잔존 **0건**, `CaptionStyleInput`은 정의 1줄만 남음(계획대로). 변경 파일은 **정확히 11개**로
「여기 없는 파일은 고치지 않는다」와 일치. `readyPlayUrl`이 죽지 않았다는 주장은 경고 0 통과가
기계로 확인한다(죽었으면 미사용 경고가 난다).

### 소득 2건

| # | 결함 | 근거 | 성격 |
| --- | --- | --- | --- |
| 1 | 머리말이 「보드 area에 `features/caption-style`가 없다」고 단언 — **거짓** | 게이트① 때 메인 루프가 백로그 원본에 더해뒀다. 실제로 빠진 것은 관측 6의 `caption-style-schema.ts` 하나뿐 | 문서 위생 |
| 2 | 「`sample=false` 기반 죽은 코드(A+B+C)**만** 제거」라는 전칭이 **넷째를 빠뜨린다** | `CaptionPreviewPlayer.tsx:125` `props.sample === true ? firstCueText(cues) : activeText`의 else 갈래도 `sample=false` 기반이고 도달 불가로 **남는다** | 문서 위생(구현 지시는 명시적이라 안 바뀜) |

**결함 2가 이 항목의 존재 이유와 같은 모양이다** — FEAT-56은 FEAT-52의 계획서가
「삭제로 죽는 것」을 열거하지 않아 생겼는데, 그 계획서가 다시 열거를 하나 빠뜨렸다.
다만 `:125`를 남기는 것 자체는 옳다 — 그것은 **유지하기로 한 `playUrl` machinery와의 접합부**이고,
지우려면 (B)를 통째로 해야 한다. 그래서 전칭을 고쳐 「셋을 제거하고 하나는 (B)에 딸려 남긴다」로
명시하고, 후속 정리 후보에 `:125`를 `playUrl` machinery와 **한 덩어리**로 묶었다.

**적용한 편집(일괄 1회)**: 머리말의 area 서술 정정 · 「대안」⑤에 `sample` 의존 코드 여집합
전수(넷)와 `:125`를 남기는 이유 추가.

## 라운드 2 — 무소득

편집이 코드 블록을 건드리지 않았음을 기계로 확인: 재추출 **22개**가 라운드 1 추출본과
**바이트 동일**, before 11개 여전히 1회 일치. 트리는 계획서 한 파일만 수정 상태.

**소득 0 → `plan-verifier` 독립 패스 디스패치 자격.**

## 라운드 3 — `plan-verifier` 독립 무편집 패스 (2026-09-21)

**결함 0건. 무편집.** 브리핑은 계약된 셋만 실었고 검증자가 그 사실을 확인했다.
무편집은 보고가 아니라 `git status --porcelain`(→ `?? nul` 하나)과 `git diff HEAD`(→ 비었음)로
직접 검산했다. 하니스 둘은 스크래치패드에만 만들었다.

### 정직하게 — 경로 5는 명세대로 실행되지 않았다

검증자는 **경로 5(돌연변이 검사)에 「시드 대상 부재」**라고 판정했다. 근거는 이 항목이
판정 로직을 **신설·변경하지 않고 삭제만** 한다는 것이고, 그 자리를 경로 2(삭제될 함수를
실행해 `sample=true` 항등 증명)·4(여집합)·8(실물 렌더)이 메웠다고 보고했다.

**카탈로그 문면으로는 검증자가 옳다** — 경로 5의 트리거는 「판정 로직·순수 함수의
**신설·변경**」이고 삭제는 거기 없다. 필수 목록에 5를 넣은 것은 **내가 트리거를 넓게 읽은
결과**다(「삭제 항목의 고유 위험은 남은 명세가 비는 것」).

다만 그 넓은 읽기에 실익이 있었다는 것도 사실이다. 내 라운드 1이 경로 5를 실제로 돌려
**돌연변이 2종 생존**을 찾았고, 그것이 이 삭제 탓인지 가리려고 **삭제 전 원본 14케이스로
재측정**해 「원래 있던 구멍, 잃은 커버리지 0」을 확정했다. 검증자의 대체 경로들은 그 질문에
닿지 않는다 — 답이 같았을 뿐이다.

**판정**: 클린 패스로 받는다. 경로 5가 답해야 할 질문은 메인 루프 기록에 증거와 함께 있고,
검증자는 건너뛴 것이 아니라 판정하고 대체했다. 다만 보드 `검증:` 줄에 이 사실을 명시한다 —
「7경로 전수」로 읽히면 거짓이 된다.

### 독립 패스가 새로 보탠 관측

- **`CaptionPreviewPlayer`의 렌더 소비자도 하나뿐**(`CaptionStyleEditor.tsx:318`). 나는
  `CaptionStyleEditor`의 소비자만 열거했는데, 검증자가 한 단계 더 내려가 체인이 끝까지
  단일함을 보였다.
- **주석 재작성이 새 죽은 참조를 만들지 않는다** — after 주석이 언급하는 `saveMutation`이
  실재함을 선언(`:97`)·사용(`:269`·`:292`)으로 확인. 나는 지워지는 이름만 봤고 **새로 쓰는
  이름은 보지 않았다.** 이 항목이 「죽은 참조 정리」인 만큼 정확히 맞는 검사다.
- **Prisma 스키마에서 「두 컬럼」 확인** — `User.defaultCaptionStyle`·`UploadedFile.captionStyle`만
  남았음을 스키마에서 직접 봤다. 나는 FEAT-53 적용 기록으로 대신했다.

셋 다 계획서를 뒤집지 않고 보강했다.

### 카탈로그 갱신 후보

경로 5의 트리거 문면(「신설·변경」)이 **삭제 항목을 담지 못한다.** 이번에 실익이 있었으므로
「순수 모듈에서 함수를 제거할 때, 남은 명세가 비지 않았는지」를 트리거에 넣을지 검토할 만하다.
판정 기준은 삭제 전후로 같은 돌연변이를 돌려 **생존 집합이 늘지 않았음**을 보이는 것이다.

## 완료 인수 (2026-09-21)

인수 조건 **다섯**을 직접 재현했다.

| # | 조건 | 결과 |
| --- | --- | --- |
| 1 | 변경 파일 ↔ 「고칠 파일」 | 코드 **11파일 정확히**, 초과 0 |
| 2 | diff ↔ 스케치 | 계획서 before/after를 `git show HEAD:` 원본에 **다시 기계 적용**해 구현본과 대조 — **11/11 바이트 동일** |
| 3 | 게이트 재실행 | `check` **EXIT 0 · `✔ No ESLint warnings or errors`** / `test` **170 / suites 40 / fail 0** |
| 4 | 백로그 제거 | 0건 |
| 5 | 상세 기록 | `docs/agents/web-dev/FEAT-56.md` 43줄 |

②가 이 항목에서 가장 강한 형태로 나왔다 — 검증 라운드에서 만든 패치를 **재생성해** 대조했으므로,
「스케치대로 구현했는가」가 사람의 눈이 아니라 바이트 비교로 판정됐다.

### 문서 갱신 셋

**`apps/web/CLAUDE.md`** — 구현 보고가 둘을 지목했고 둘 다 처리했다.
- `:69` 테스트 수 `25파일 42 suite 178` → **`25파일 40 suite 170`**
- `sample-captions.test.mjs` 행이 삭제된 FEAT-49 계약(`koreanSampleCues`·`previewCaptionCues`)을
  여전히 설명하고 있었다. 그 서술을 걷어내고 **왜 사라졌는지**(검토 다이얼로그 소멸 →
  `sample=false` 소비자 0)를 남겼다. 더해서 **단어 타이밍이 어느 파일도 안 덮는다**는 사실을
  적었다 — 내 경로 5가 실측한 것이고, 표가 「이 파일이 지키는 것」을 말하는 자리이므로
  지키지 **않는** 것도 거기 있어야 한다.

**`TASK_BACKLOG.md` FEAT-44** — 줄번호 드리프트를 확인하다 **그 항목의 관측이 스스로 낡은 것**을
발견했다. 관측 ⑤(a)가 `previewText` 선언을 `ClipDraftCard.tsx:114`라 적었는데 **FEAT-56 이전에
이미 `:84`**였고(30줄 차이) 지금은 `:82`다. ⑤(b)의 `:476`도 이전 `:437`, 지금 `:435`.

**숫자를 고치지 않았다** — 이 항목의 결정이 정확히 「줄번호를 다시 맞추지 않는다」이기 때문이다.
대신 관측의 숫자를 이력으로 읽고 **계획 단계에서 심볼명으로 다시 찾으라**는 경고를 달았다.
이 사실 자체가 FEAT-44 진단의 **세 번째 실증**이다(FEAT-43 때 30줄, FEAT-55 때 `CLAUDE.md` 11건,
이번엔 FEAT-44 자신).

### `docs/release-checks.md` — 줄을 **하나만** 등재했다

이 항목의 화면 확인 대부분은 **검증 단계에서 이미 닫혔다.** 경로 8이 `sample=true` 마크업의
바이트 동일을 보였고, 등가성은 원본 함수 실행으로 증명했으며, `playUrl`은 본문 소비가 0이라
마크업에 닿지 않는다(lint 경고가 그 증거였다).

남는 것은 「조합된 화면이 런타임에 뜨는가」 하나뿐이라 **그것만 적었다.**
오늘 157줄을 분류하며 얻은 교훈이 「확인할 수 없는 줄을 쌓지 않는다」였고, 같은 날 그 반대를
하지 않기 위해서다. 등재하지 않은 이유도 절 머리말에 적어 뒀다 — 나중에 「왜 렌더 확인이
없지」라고 물을 사람을 위해서다.

### 「범위 밖 의존」 — 백로그 후보 1건

`CaptionPreviewPlayer`의 `playUrl` 기반 `<video>` 재생 machinery와 `:125` `displayText`의
`: activeText` 잔여 갈래. 현재 유일 소비자가 `playUrl={null}`·`sample=true`라 **둘 다 도달 불가**이고,
계획서가 (B)로 기각하며 한 덩어리로 묶어 둔 것이다. 소유자에게 제시한다.

