# FEAT-44 — 메인 루프 기록

## 필수 경로 확정 (2026-09-22)

**넷**을 골랐다. 이 항목은 코드가 한 줄도 안 바뀌고 **문서 인용만** 바꾼다.

| # | 경로 | 판정 | 이유 |
| --- | --- | --- | --- |
| 1 | 인용 전수 대조 | **채택** | 모든 항목. 이 항목은 **인용 자체가 대상**이라 이중으로 필수다 |
| 3 | before/after 기계 적용 | **채택** | 기존 파일 수정 52건 |
| 4 | 전칭 여집합 열거 | **채택** | 「66건」·「49건」·「17파일」·「표본 12건 중 0건」·「완료 시 0건」 — 이 계획은 **거의 전부가 수를 세는 전칭**이다 |
| 7 | 음성 시험 | **채택** | 계획이 「새 앵커는 안 낡는다」에 기댄다. **앵커가 `grep`으로 실제로 찾히는지**를 보지 않으면 오타 난 앵커가 줄번호보다 나을 게 없다 |
| 2 | 스케치 추출·실행 | **비채택** | 스케치에 실행 가능한 코드가 없다(주석 문자열 치환뿐). 게이트 넷 실행이 대신한다 |
| 5·6·8·9 | | **비채택** | 판정 로직 없음 · 외부 신호 없음 · 화면 변경 없음 · schema/config 변경 없음 |

**메인 루프가 쓴 계획서라 경로 4를 특히 세게 걸었다.** FEAT-53에서 내 계획의 결함 4건 중 3건이
「수를 세는 전칭 문장」이었고, 이 항목은 전칭의 밀도가 그보다 높다.

## 라운드 1 — 네 경로 전수 (소득 2건)

**경로 1.** 내가 쓴 「표본 대조」 표의 **「실제로 있는 것」 열 11건**을 기계 검산 — 불일치 0.
표가 주장한 **실제 위치 9건**(`create_subtitles_with_ffmpeg :300` · `clip_segments :313` ·
`start_rel :334` · `PlayResY :367` · `MARGINV :153` · `Anton :373` · `ProcessVideoRequest :56` ·
`resolve_caption_style :170` · Korean 분기 `:850`)도 전부 일치.

**경로 4.** 전칭을 전부 다시 셌다.

| 주장 | 실측 |
| --- | --- |
| `main.py:N` 총 **66건** | 66 ✓ |
| `TASK_BACKLOG.md` **17건** | 17 ✓ |
| 대상 **49건** | 49 ✓ |
| **17파일** | 16 + `ClipDraftCard.tsx`(웹 내부만) = 17 ✓ |
| 파일별 건수 16행 | **16행 전부 일치** ✓ |
| 계획서 표에 빠진 파일 | **없음 — 전수 등재** ✓ |

> 중간에 「총 74건」이라는 거짓 불일치가 한 번 났다. 내 체커가 제외 필터를
> `f.startswith("docs/proposals")`로만 써서 `apps/web/docs/proposals/`를 못 걸렀다.
> 필터를 고치니 66이 나왔다. **체커가 틀렸지 계획이 틀린 게 아니다** —
> 이 세션에서 네 번째 체커 아티팩트다(FEAT-53 정규식, FEAT-56 하니스 입력, 여기 둘).

**경로 7 — 새 앵커가 실제로 찾히는가.** 계획서가 쓰겠다는 앵커 **35개**를 `main.py`에 대고
`grep`했다 — **없는 것 0개.** `moment_prompt.py`로 옮겨갔다는 문장도 확인했다
(`moment_prompt.py` 1회 / `main.py` **0회** — 계획서 주장이 참).

**그런데 이 경로가 결함을 냈다.** 35개 중 **15개가 `main.py`에 2회 이상** 나온다 —
`create_subtitles_with_ffmpeg`(영어)와 `create_korean_subtitles_with_ffmpeg`(한국어)가 거의 같은
코드를 갖기 때문이다. 코드 인용만으로는 어느 함수인지 정해지지 않는다.

**경로 3.** 계획서 표의 before 조각 **40개**(`apps/backend/CLAUDE.md` 11건 제외)가 각 파일에서
유일한지 셌다 — **1개가 유일하지 않았다.**

### 소득 2건

| # | 결함 | 근거 | 성격 |
| --- | --- | --- | --- |
| 1 | `main.py:1019-1025` 조각이 `test_reference_translation.py`에 **2회**(`:21`·`:204`) | 조각만으로 치환하면 어느 줄이 바뀌는지 정해지지 않는다. 두 줄의 after가 서로 다르므로 잘못 맞으면 틀린 문장이 들어간다 | **구현 영향** |
| 2 | per-line 앵커가 두 자막 함수에 **공통인 코드**를 가리켜 모호 | 경로 7 실측 — `start_rel = ...` 등 15개가 2회씩 나온다. 새 앵커가 줄번호보다 나아지려면 `grep` 결과가 하나여야 한다 | **구현 영향** |

**적용한 편집(일괄 1회)**
- 결함 1 → 그 두 줄은 **줄 전체를 before로** 삼도록 스케치를 고치고, ⚠️로 이유를 남겼다.
  나머지 50개는 조각이 유일함을 기계로 확인했다고 명시했다.
- 결함 2 → 「앵커 형식」 절에 **규칙 둘**을 추가했다. ① 파일 머리에 함수 문맥이 있으면
  per-line 앵커는 코드 인용만(=`caption-preview.ts`, `:10`이 문맥을 세운다) ② 머리가 없으면
  앵커에 **함수명을 넣는다**(=`caption-preview.test.mjs`·`constants.ts`).
  그에 맞춰 `caption-preview.test.mjs:18`의 after에 함수명을 넣었다.

**결함 둘 다 경로 7·3이 아니었으면 안 나왔다.** 경로 1·4만 돌렸으면 계획서는 깨끗해 보였을
것이고, 구현이 모호한 앵커를 쓰거나 엉뚱한 줄을 치환했을 것이다.

## 라운드 2 — 무소득

정정한 두 before(줄 전체)가 각각 **1회**임을 확인했고, 계획서가 그 두 줄을 전체로 싣는 것도
확인했다. 수 주장 넷(66·49·52/17파일·표본 12건)이 편집 뒤에도 그대로다.

**소득 0 → `plan-verifier` 독립 패스 디스패치 자격.**
