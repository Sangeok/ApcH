# FEAT-44 — 메인 루프 기록

## 필수 경로 확정 (2026-09-22)

**넷**을 골랐다. 이 항목은 코드가 한 줄도 안 바뀌고 **문서 인용만** 바꾼다.

| # | 경로 | 판정 | 이유 |
| --- | --- | --- | --- |
| 1 | 인용 전수 대조 | **채택** | 모든 항목. 이 항목은 **인용 자체가 대상**이라 이중으로 필수다 |
| 3 | before/after 기계 적용 | **채택** | 기존 파일 수정 52건 |
| 4 | 전칭 여집합 열거 | **채택** | 「66건」·「49건」·「17파일」·「표본 12건 중 0건」·「완료 시 0건」 — 이 계획은 **거의 전부가 수를 세는 전칭**이다 |
| 7 | 음성 시험 | **채택** | 계획이 「새 앵커는 안 낡는다」에 기댄다. **앵커가 `grep`으로 실제로 찾히는지**를 보지 않으면 오타 난 앵커가 줄번호보다 나을 게 없다 |
| 2 | 스케치 추출·실행 | **비채택** | 스케치에 실행 가능한 코드가 없다(주석 문자열 치환뿐). 게이트 넷 실행이 대신한다 |
| 5·6·8·9 | | **비채택** | 판정 로직 없음 · 외부 신호 없음 · 화면 변경 없음 · schema/config 변경 없음 |

**메인 루프가 쓴 계획서라 경로 4를 특히 세게 걸었다.** FEAT-53에서 내 계획의 결함 4건 중 3건이
「수를 세는 전칭 문장」이었고, 이 항목은 전칭의 밀도가 그보다 높다.

## 라운드 1 — 네 경로 전수 (소득 2건)

**경로 1.** 내가 쓴 「표본 대조」 표의 **「실제로 있는 것」 열 11건**을 기계 검산 — 불일치 0.
표가 주장한 **실제 위치 9건**(`create_subtitles_with_ffmpeg :300` · `clip_segments :313` ·
`start_rel :334` · `PlayResY :367` · `MARGINV :153` · `Anton :373` · `ProcessVideoRequest :56` ·
`resolve_caption_style :170` · Korean 분기 `:850`)도 전부 일치.

**경로 4.** 전칭을 전부 다시 셌다.

| 주장 | 실측 |
| --- | --- |
| `main.py:N` 총 **66건** | 66 ✓ |
| `TASK_BACKLOG.md` **17건** | 17 ✓ |
| 대상 **49건** | 49 ✓ |
| **17파일** | 16 + `ClipDraftCard.tsx`(웹 내부만) = 17 ✓ |
| 파일별 건수 16행 | **16행 전부 일치** ✓ |
| 계획서 표에 빠진 파일 | **없음 — 전수 등재** ✓ |

> 중간에 「총 74건」이라는 거짓 불일치가 한 번 났다. 내 체커가 제외 필터를
> `f.startswith("docs/proposals")`로만 써서 `apps/web/docs/proposals/`를 못 걸렀다.
> 필터를 고치니 66이 나왔다. **체커가 틀렸지 계획이 틀린 게 아니다** —
> 이 세션에서 네 번째 체커 아티팩트다(FEAT-53 정규식, FEAT-56 하니스 입력, 여기 둘).

**경로 7 — 새 앵커가 실제로 찾히는가.** 계획서가 쓰겠다는 앵커 **35개**를 `main.py`에 대고
`grep`했다 — **없는 것 0개.** `moment_prompt.py`로 옮겨갔다는 문장도 확인했다
(`moment_prompt.py` 1회 / `main.py` **0회** — 계획서 주장이 참).

**그런데 이 경로가 결함을 냈다.** 35개 중 **15개가 `main.py`에 2회 이상** 나온다 —
`create_subtitles_with_ffmpeg`(영어)와 `create_korean_subtitles_with_ffmpeg`(한국어)가 거의 같은
코드를 갖기 때문이다. 코드 인용만으로는 어느 함수인지 정해지지 않는다.

**경로 3.** 계획서 표의 before 조각 **40개**(`apps/backend/CLAUDE.md` 11건 제외)가 각 파일에서
유일한지 셌다 — **1개가 유일하지 않았다.**

### 소득 2건

| # | 결함 | 근거 | 성격 |
| --- | --- | --- | --- |
| 1 | `main.py:1019-1025` 조각이 `test_reference_translation.py`에 **2회**(`:21`·`:204`) | 조각만으로 치환하면 어느 줄이 바뀌는지 정해지지 않는다. 두 줄의 after가 서로 다르므로 잘못 맞으면 틀린 문장이 들어간다 | **구현 영향** |
| 2 | per-line 앵커가 두 자막 함수에 **공통인 코드**를 가리켜 모호 | 경로 7 실측 — `start_rel = ...` 등 15개가 2회씩 나온다. 새 앵커가 줄번호보다 나아지려면 `grep` 결과가 하나여야 한다 | **구현 영향** |

**적용한 편집(일괄 1회)**
- 결함 1 → 그 두 줄은 **줄 전체를 before로** 삼도록 스케치를 고치고, ⚠️로 이유를 남겼다.
  나머지 50개는 조각이 유일함을 기계로 확인했다고 명시했다.
- 결함 2 → 「앵커 형식」 절에 **규칙 둘**을 추가했다. ① 파일 머리에 함수 문맥이 있으면
  per-line 앵커는 코드 인용만(=`caption-preview.ts`, `:10`이 문맥을 세운다) ② 머리가 없으면
  앵커에 **함수명을 넣는다**(=`caption-preview.test.mjs`·`constants.ts`).
  그에 맞춰 `caption-preview.test.mjs:18`의 after에 함수명을 넣었다.

**결함 둘 다 경로 7·3이 아니었으면 안 나왔다.** 경로 1·4만 돌렸으면 계획서는 깨끗해 보였을
것이고, 구현이 모호한 앵커를 쓰거나 엉뚱한 줄을 치환했을 것이다.

## 라운드 2 — 무소득

정정한 두 before(줄 전체)가 각각 **1회**임을 확인했고, 계획서가 그 두 줄을 전체로 싣는 것도
확인했다. 수 주장 넷(66·49·52/17파일·표본 12건)이 편집 뒤에도 그대로다.

**소득 0 → `plan-verifier` 독립 패스 디스패치 자격.**

## 라운드 3 — `plan-verifier` 독립 무편집 패스 1차 (2026-09-22)

**결함 1건 — 구현 영향.** 무편집(직접 검산: `git status`에 `?? nul`뿐, 대상 파일 diff 공란).

### 잡힌 것

`feature-scout.md:166`·`:174`의 `main.py:585`를 내가 **「한국어 번역 프롬프트 /
`create_korean_subtitles_with_ffmpeg`」로 앵커했는데 틀렸다.** 그 자리 산문은
`generate_youtube_metadata`를 말한다(`:165` 「generate_youtube_metadata는 script_text와
language만」). 실제 위치는 `def generate_youtube_metadata` `:608`, 호출 `:869`(=`process_clip` 안).

**왜 틀렸나**: `main.py:585`가 **지금** `create_korean_subtitles_with_ffmpeg`(`:416-607`) 범위
안에 떨어진다는 이유로 그 함수를 앵커로 삼았다. **낡은 숫자가 가리키는 곳을 보고 앵커를 정한
것이다** — 그 숫자는 정의상 틀렸는데도.

**이 항목이 없애려는 바로 그 실패를 계획서가 저질렀다.** 게다가 더 나쁜 방향이다 —
낡은 줄번호는 낡아 보이기라도 하지만, **틀린 심볼명은 권위 있는 얼굴로 박힌다.**
구현했으면 에이전트가 매 세션 읽는 문서가 자기모순 거짓이 됐을 것이다.

### 고친 것

- 두 행을 `generate_youtube_metadata`(호출은 `process_clip`)로 정정
- 「앵커 형식」에 절을 하나 추가했다 — **「산문이 대상을 말한다 — 낡은 숫자가 가리키는 곳이
  아니라」**. 구현 절차로 못박았다: ① 인용하는 쪽 산문을 읽어 대상을 정하고 ② 그 대상을
  심볼로 찾아 앵커를 쓴다. **낡은 줄번호는 읽지 않는다.** 실패 사례로 이번 건을 그대로 적었다.

### 자가 감사 — 같은 실수가 더 있나

나머지 앵커 **11개**를 「인용하는 쪽 산문이 말하는 대상」 기준으로 다시 대조했다
(`feature-scout.md:64`·`:122` · `backend-dev.md:154` · `constants.ts:75`·`:78` ·
`layout.tsx:71`·`:79` · `CaptionPreviewPlayer.tsx:120`·`:122` · `apps/web/CLAUDE.md:77` ·
`release-checks.md:355`). **전부 산문과 일치** — 썩은 포인터를 따라간 것은 그 둘뿐이었다.

### 독립 패스가 보탠 관측

- **경로 7을 나보다 정밀하게 돌렸다** — 묶기 조각 9종이 각각 **정확히 2회**(`:397`/`:589` 등
  영·한 함수 쌍)임을 세어, disambiguator를 빼면 검사가 **실제로 실패**함을 보였다.
  더해서 단일 출현 조각(`new_style.shadow`·`korean_style.shadow`·`new_style.fontname = "Anton"`·
  `korean_style.fontname` 각 1회)은 함수명 없이도 유일함을 확인했다 — 내 규칙이 **과하지도
  않다**는 반대 방향 증거다.
- `clip-draft-review/ui/index.tsx`에 `CaptionStyleEditor` **임포트·렌더가 없고 주석 언급뿐**임을
  확인해, 「그 다이얼로그는 FEAT-52가 없앴다」 정정의 근거를 하나 더 세웠다.
- `feature-scout.md:64`의 한국어 폴백 앵커가 옳음을 코드로 확인했다(`:541-544` 줄 폴백,
  `:548` 클립 폴백).

**다음**: 계획서가 바뀌었으므로 독립 패스 2차를 돌린다.

## 라운드 4 — 독립 무편집 패스 2차 (2026-09-22) — 클린

**결함 0건. 무편집**(직접 검산: `git status`에 `?? nul`뿐, `git diff HEAD` 공란).
필수 4경로 전수 실행. 1차 정정(`generate_youtube_metadata`)이 옳게 반영됐음도
검증자가 실측으로 확인했다(def `:608` · 호출 `:869`가 `process_clip` 안).

**경로 7을 2차도 실측했다** — 묶기 조각 9종이 `main.py`에 **각각 정확히 2회**.
규칙을 빼면 `grep`이 2를 반환해 검사가 실제로 깨진다. 장식이 아니다.

### 검증자가 올린 관측 둘 — 결함은 아니나 게이트에 올린다

**① `feature-scout.md:64`의 before 표기가 생략형이다.** 계획서 표가 `main.py:524-...`로
적었는데 실제는 `main.py:524-526`이다(`:519-521`도 함께 있다 — 확인했다). 구현 절차가
「낡은 줄번호는 읽지 않는다」이고 완료 grep이 `main\.py:\d+` 0을 요구하므로 **구현에 영향은
없다.** 정밀도를 위해 고칠지는 선택.

**② 같은 편집 파일 안에 계획서가 다루지 않는 웹 내부 줄인용이 2건 더 있다.**

| 자리 | 인용 | 실측 |
| --- | --- | --- |
| `review-language-notice.ts:9` | `clip-display/model/subtitle-status.ts:5 주석` | **지금은 맞다** — `:5`가 「앱 UI 언어는 영어다」 주석. 다만 같은 취약 형식 |
| `ClipDraftCard.tsx:322` | `배지(:285-286)` | **낡았다** — `:285-286`은 배지가 아니라 미리보기 **버튼**(`aria-label="Preview clip start"`)이다. FEAT-56이 이 파일에서 두 줄을 지워 더 밀렸다 |

**계획서 결함은 아니다** — 웹 내부 범위를 「백로그 추가 범위 ⑤의 3건」으로 명시 한정했고,
완료 grep도 그 3건만 주장한다. 검증자의 판단이 옳다.

**그러나 ②의 둘째는 이 항목이 고치려는 병의 실제 사례이고, 이미 고치는 파일 안에 있다.**
2건을 범위에 넣을지는 **소유자 결정**이라 게이트②에 함께 올린다.

