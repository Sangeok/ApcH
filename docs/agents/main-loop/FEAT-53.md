# FEAT-53 — `ClipDraft.captionStyle` 컬럼 제거 (메인 루프 기록)

## 게이트① (2026-09-17)

소유자 직접 발주 — 세션 지시 "Feat 53 진행". 미결 0건을 확인하고 보드에 `계획지시`로 올려
커밋·푸시한 뒤, **계획서도 내가 썼다** — `packages/db`는 dev 로스터 쓰기 범위 밖이라 담당이
main-loop다(FEAT-19·FEAT-26·FEAT-38·FEAT-47 전례).

**그래서 이 항목은 구조적으로 약하다.** 계획서 저자와 검증 라운드 수행자가 같다. 내 라운드는
평소보다 더 회상에 가깝고, `plan-verifier`의 독립 패스가 평소보다 더 무겁다. 이 사실을 여기
적어 둔다.

선행 조사로 FEAT-47(같은 스키마 마이그레이션, main-loop)을 먼저 읽었다. 그 계획서가 남긴 것이
이번에 그대로 쓰였다 — 마이그레이션 히스토리 드리프트(`ClipDraft` 생성 마이그레이션 부재),
`migrate dev` 금지, `packages/db`에서 Prisma CLI가 루트 `.env`를 못 읽는 문제와 그 우회 명령.

## 필수 경로 확정

| # | 경로 | 판정 | 근거 |
| --- | --- | --- | --- |
| 1 | 인용 전수 대조 | **채택** | 모든 항목 |
| 2 | 스케치 추출·실행 | **채택** | 스케치가 `prisma` 블록과 SQL이다. 실행 형태를 바꿔 받는다 — 제거본을 만들어 `prisma validate`로 돌린다 |
| 3 | before/after 기계 적용 | **채택** | 기존 파일 수정 + 명시적 before/after 블록 |
| 4 | 전칭 여집합 열거 | **채택** | 전칭 주장이 이 항목의 **안전 근거 자체**다 — "활성 참조 0", "`select` 없는 쿼리 다섯", "`apps/*` 소스는 한 줄도 안 바뀐다" |
| 5 | 돌연변이 검사 | 비해당 | 순수 함수 신설·변경이 없다 |
| 6 | 실제 사건 재생 | 비해당 | 외부 신호 해석이 없다 |
| 7 | 음성 시험 | **채택** | 계획서가 **`tsc`가 "참조 0"을 판정해 준다**는 데 기댄다. 그 기댐이 장식이 아닌지 봐야 한다 |
| 8 | 실물 렌더 | 비해당 | 화면 변경 0 |
| 9 | 구조적 아티팩트 검사 | **채택** | **`schema.prisma` 변경이다 — 카탈로그가 이 행의 트리거로 적은 바로 그것**(`schema·config·생성 파일 변경`). 카탈로그에서 이 행은 아직 「실증 사례 대기」다 |

채택 6 / 비해당 3.

**경로 9의 첫 실증이 될 수 있다.** `docs/plans/verification-paths.md:29`가 그 행의 실증 사례 칸을
비워 두고 있고, FEAT-27 백로그 항목도 "경로 9는 실증 사례가 아직 없어 첫 fixture가 곧 첫 실증이
된다"고 적었다. 이번이 그 자리다.

## 라운드 1 (메인 루프, 편집) — 2026-09-17

**경로 1 — 인용 전수 대조.** 계획서의 `파일:줄` 인용 11건을 내용까지 대조. **불일치 0.**

**경로 3 — before/after.** `prisma` 블록 둘을 추출해 before가 `schema.prisma`에 **바이트 그대로**
존재하고 after는 부재(정상)임을 확인.

**경로 9 — 구조적 아티팩트 검사.** 스키마 사본에서 컬럼·주석 4줄을 제거해
`node node_modules/prisma/build/index.js validate --schema <사본>`을 돌렸다 →
**`The schema at … is valid 🚀`**. 텍스트 편집이 아니라 파서가 판정했다. 원본 10026자 → 사본
9751자(275자 감소)로 제거 범위도 수치로 확인했다. **저장소 파일은 건드리지 않았다**(사본은
스크래치패드).

**경로 4 — 전칭 여집합 열거.** 여기서 소득이 둘 나왔다(아래).

### 라운드 1 소득 — 내 계획서의 결함 2건

둘 다 **경로 4가 잡았다.** 내가 쓴 문서인데 내 라운드가 잡았다는 점에서, 저자=검증자 구조에서도
기계적 경로는 값을 한다는 실측이다.

1. **전칭 열거가 불완전했다.** 계획서가 "`select` 없는 쿼리가 **다섯**"이라 하고 괄호로 둘만
   제외 설명했는데(`:44` `select` 있음, `:22` `createMany`), `clipDraft.*` 호출부를 전수로 세면
   **여덟**이다. 내가 설명하지 않은 여덟 번째가 `:117`
   `    const aggregate = await tx.clipDraft.aggregate({`다. 확인해 보니 `:119`
   `      _max: { index: true },`만 읽는 집계라 스칼라 행을 돌려주지 않아 **안전하다** — 결론은
   같지만 **근거가 비어 있었다.** 여집합 셋을 전부 적도록 고쳤다.
2. **주석 둘이 삭제될 컬럼을 가리킨다.** `packages/db/prisma/schema.prisma:60`
   `    // ClipDraft.captionStyle과 같은 CaptionStyle 모양이다.`(`User.defaultCaptionStyle`의
   설명)와 `apps/web/src/fsd/shared/config/caption-style-schema.ts:7-8`(「세 컬럼이 같은
   `CaptionStyle` 모양을 공유」)이다. **이건 내가 FEAT-52 인수에서 지적했던 것과 똑같은 부류다**
   (`constants.ts:113`이 삭제될 `RenderCaptionStyle`을 가리키던 건). 내 계획서가 같은 실수를
   반복했다. 전자는 내 범위라 「고칠 파일」에 넣었고, 후자는 `apps/web`(web-dev 몫)이라
   **FEAT-56에 관측 6으로 추가**했다 — 그 항목이 이미 같은 성격의 잔여 문구를 들고 있다.
   순서 제약(FEAT-53 뒤에 완전히 거짓이 된다)도 함께 적었다.

### 이 계획서의 핵심 — 적용 순서가 FEAT-47과 **반대**다

기록해 둘 값어치가 있다. FEAT-47(컬럼 **추가**)은 「적용 → 확인 → 커밋·푸시」로 **컬럼이 코드보다
먼저**여야 했다. **삭제는 반대다** — 지금 프로덕션에 떠 있는 클라이언트가 `captionStyle`을
SELECT하므로(위 다섯 쿼리), DB에서 먼저 지우면 검토 화면·편집 저장·렌더 디스패치·업로드 상세가
그 순간 `column does not exist`로 깨진다.

올바른 순서는 **커밋·푸시 → 새 클라이언트 배포 → `migrate deploy`**이고, 그 사이 상태(DB에는
컬럼이 있고 코드는 안 읽음)가 안전한 중간 지점이다. 반대 순서에는 안전한 중간 지점이 없다.
**FEAT-47의 순서를 습관적으로 따르면 장애**라서 계획서 앞쪽에 절로 박고 「대안 (C)」로도 기각해
뒀다.

## 라운드 2 (메인 루프, 무편집) — 2026-09-17

라운드 1의 편집이 준비도를 초기화하므로 무편집 재대조를 돌렸다. 계획서 인용 재대조 **불일치 0**,
내가 새로 주입한 인용 다섯(`schema.prisma:60` · `caption-style-schema.ts:7` ·
`clip-draft/api:117`·`:119`·`:22`)도 내용까지 일치. before 블록 BYTE-MATCH 유지.

**라운드 2 소득 0 — 무편집 무소득.** 판정이 아니라 디스패치 자격이다. `plan-verifier`에 독립
무편집 패스를 요청한다. 브리핑은 계약대로 셋만 보낸다(항목ID·계획서 경로·필수 경로 목록).

**이 항목에서 독립 패스는 평소보다 무겁다** — 계획서 저자가 나이므로, 내 라운드가 못 본 것을
볼 컨텍스트가 그것뿐이다.

## 독립 패스 1차 (`plan-verifier`) — 2026-09-17 · **결함 2건**

브리핑 계약은 온전했다(항목ID·계획서 경로·필수 경로 목록, 셋뿐). 검증자가 필수 6경로를 전부
실행했고 **내 계획서에서 결함 둘을 잡았다.** 둘 다 내가 직접 재대조했고 **둘 다 사실이었다.**

**결함 1 — 「깨지는 쿼리 다섯」이 틀렸다. 넷이다.** 내가 `:124`
`    return tx.clipDraft.create({`(`createCustomClipDraft`)를 "스칼라 전부 SELECT" 쪽에 넣었는데,
그 호출은 `:135` `      select: { id: true },`를 갖는다(내가 재독 확인). Prisma가
`INSERT … RETURNING id`로 컴파일하므로 이 컬럼을 참조하지 않는다.

**이건 내 문서가 자기 기준과 모순이었다** — 같은 문단이 `:44` findFirst를 "`select`를 갖는다"는
이유로 여집합에 넣고 있었다. 같은 기준이면 `:124`도 여집합이다. 그리고 검증자가 든 방증이
날카롭다: **내 「적용 순서」 절의 위험 문장이 나열하는 표면이 처음부터 넷**(검토 화면·편집 저장·
렌더 디스패치·업로드 상세)이고 커스텀 클립 생성은 거기 없었다. 문서 안에 반증이 이미 있었다.

임팩트는 검증자가 정확히 달았다 — **구현 산출물은 바뀌지 않는다**(schema 편집 + 마이그레이션 +
클라이언트 재생, `apps/web` 0줄). 안전 순서 결론도 깨지는 쿼리가 넷 남아 그대로 유효하다.
다만 **배포 후 검증 대상 목록**이 위험한 적 없던 쿼리를 포함하게 된다. 그래서 「못 덮는 범위」의
그 줄도 함께 고쳤다.

**결함 2 — 「apps/web에 주석이 하나 남는다」가 틀렸다. 둘이다.** 내가 지목한
`caption-style-schema.ts:7` 외에 `widgets/clip-draft-review/model/use-clip-draft-review.ts:128`
`                  // captionStyle은 의도적으로 낙관 반영하지 않는다. 헤더 카운트와`가 있다 —
낙관 업데이트가 `draft`(= `ClipDraft`)의 그 필드를 왜 안 건드리는지 설명하는 주석이다.

**다만 검증자가 우려한 결과는 일어나지 않는다** — 그 줄은 **이미 FEAT-56 관측 2가 들고 있다**
(「낡은 주석 3곳」의 하나로 `:128`·`:206`·`:216`을 인용). 내 문장이 여집합을 놓친 것이지 항목이
비어 있는 건 아니다. 그 사실까지 계획서에 적어 다음 사람이 중복 등재하지 않게 했다.

### 이 두 결함의 공통점

**둘 다 전칭이고, 둘 다 「하나/다섯」처럼 수를 세는 문장이었다.** 내 라운드 1도 같은 부류를
하나 잡았다(「다섯 쿼리」의 여집합이 비어 있던 것 — 그때 `aggregate`를 보강했다). **같은 문단을
세 번 고쳤다는 뜻이다.** 저자=검증자 구조에서 수를 세는 문장이 특히 약하다는 실측으로 남긴다.

## 라운드 3 (메인 루프, 무편집) — 2026-09-17

정정 뒤 무편집 재대조. 계획서 인용 **불일치 0**, 새로 주입한 둘(`clip-draft/api:135` ·
`use-clip-draft-review.ts:128`)도 내용까지 일치. 수치 정합도 기계로 확인했다 —
`clip-draft/api/index.ts`의 `clipDraft.` 호출 7건 + `uploaded-file/api/index.ts`의 1건 = **여덟**이고,
계획서의 넷(깨짐) + 넷(안전) 분해와 맞물린다.

**라운드 3 소득 0.** 독립 패스를 한 번 더 받는다 — 1차가 결함을 냈으므로 무소득 패스가 아직 없다.

## 독립 패스 2차 (`plan-verifier`) — 2026-09-17 · **클린 패스**

브리핑은 계약대로 셋만. 검증자가 판정 자격을 확인했다. **결함 0건, 필수 6경로 전부 실행.**

독립적으로 재현·확장된 것:

- 경로 1 — 계획서의 모든 인용을 재독 대조, **함수 소속과 `select` 유무까지** 확인. `package.json`의
  네 스크립트(`postinstall`·`db:generate:client`·`db:generate`·`db:migrate`)도 대조해 계획서의
  "`db:generate`는 `migrate dev`라 쓰면 안 된다"는 서술이 정확함을 확인했다.
- 경로 3 — `sed 191,195d`로 실제 삭제를 적용해 산출이 스케치 after와 정확히 일치함을 보였다.
  `:60` 주석 재지정이 before/after 블록 없이 산문 지시로만 있는 점을 짚되, 대상(`UploadedFile.captionStyle`)이
  실재·활성이라 **지시 불명확이 아니라고 판정**했다 — 내가 스케치를 안 쓴 선택을 받아 준 것이다.
- 경로 4 — 「여덟」·「넷/넷」·「활성 참조 0」·「`ClipDraft` 생성 마이그레이션 없음」을 전부 재열거.
  마지막 것은 `CREATE TABLE ClipDraft`를 대소문자·공백 관대 grep으로도 0건 확인했다(내가 안 한 것).
- **경로 7 — 진짜 음성 시험을 했다.** 생성 클라이언트의 `ClipDraft` 타입에서
  `Omit<ClipDraft,"captionStyle">`로 마이그레이션 후 상태를 흉내 내 `tsc`를 돌렸고,
  `draft.captionStyle` 읽기가 **`TS2339: Property 'captionStyle' does not exist`로 실패**하고
  `draft.selected`는 무오류임을 보였다. 계획서의 "`tsc`가 진짜 게이트"가 **장식이 아님을 실증**했다.
  나는 이 주장을 쓰기만 하고 시험하지 않았다.
- 경로 9 — `prisma validate` 통과에 더해, `@@unique`·`@@index`·relation이 그 컬럼을 참조하지 않음과,
  신규 타임스탬프 `20260917000000`이 기존 최신보다 뒤로 정렬돼 충돌이 없음을 구조로 확인했다.
  1차 패스는 여기에 더해 `prisma migrate diff`로 마이그레이션을 **구조적으로 생성**해 내
  `migration.sql`과 **바이트 일치**함을 보였다.

프로덕션 DDL만 실행하지 않았다 — 계획서가 소유자 승인 단계로 명시한 부분이고, 명령 **형태**는
검증됐다(`node_modules/prisma/build/index.js` 6.19.1 실재, `.env`에 `DATABASE_URL`·
`DATABASE_URL_UNPOOLED` 둘 다 존재).

**무수정 준수 — 직접 검산했다.** `git status --short`는 `?? nul`(세션 시작 전부터 있던 미추적
파일)뿐이고 `git diff --stat HEAD -- packages docs apps`는 **빈 출력**이다.

### 판정

보드에 `검증: 클린 패스`를 기록한다. 카운트는 이 2차 패스 하나 — 1차는 결함 2건을 냈으므로
무소득이 아니다. 누적 소득은 **메인 루프 라운드 2건 + 독립 패스 1차 2건 = 4건**이고, 그중
구현 산출물을 바꾸는 것은 **0건**이었다(전부 근거·열거·검증 대상 목록의 정확성).

정지 규칙에 걸리지 않는다 — 독립 패스가 구현 영향 결함을 3사이클 연속 낸 적이 없다.

**남길 교훈 하나**: 이 항목의 소득 4건 중 **3건이 "수를 세는 전칭 문장"**이었다(다섯→넷, 하나→둘,
여집합 누락). 저자=검증자인 main-loop 항목에서는 그 문장들을 먼저 의심하는 편이 싸다.

**다음은 게이트② — 소유자만 연다.**

## 구현 (2026-09-18) — 코드만, DB 적용은 별개 단계

게이트②가 열려 구현했다. 계획서 「고칠 파일」 그대로 둘을 고쳤다.

1. `packages/db/prisma/schema.prisma` — ① `ClipDraft`에서 주석 3줄 + `captionStyle   Json?` +
   앞 빈 줄 제거(스케치 before/after 그대로 적용). ② `User.defaultCaptionStyle`의
   `    // ClipDraft.captionStyle과 같은 CaptionStyle 모양이다.`를
   `    // UploadedFile.captionStyle(업로드 시점 스냅샷)과 같은 CaptionStyle 모양이다.`로.
2. `packages/db/prisma/migrations/20260917000000_drop_clip_draft_caption_style/migration.sql` 신규 —
   `-- AlterTable` + `ALTER TABLE "ClipDraft" DROP COLUMN "captionStyle";`.

**마이그레이션 디렉터리 이름의 날짜(0917)가 오늘(0918)과 다르다.** 계획·검증이 그 이름으로
확정됐고 이 문자열은 정렬 키이지 적용 시각의 주장이 아니므로 그대로 뒀다. 사이클 중 날짜가
넘어간 것뿐이다.

### 생성 클라이언트 — 계획서 「고칠 파일」의 누락

`npm run db:generate:client -w @repo/db`로 재생성했다(DB 무접속). 그 결과
**`packages/db/generated/prisma/` 7파일이 변경됐고, 이 파일들은 추적 대상이다.**

**계획서 「고칠 파일」은 둘만 적었다.** §3이 재생성을 "읽기 전용"이라 부른 것도 절반만 맞다 —
DB에 안 닿는다는 뜻이었지 파일을 안 쓴다는 뜻이 아닌데, 그렇게 읽힌다.

선례로 판정했다: FEAT-47(`71bf498`, 컬럼 추가)의 커밋이 **정확히 같은 9파일 모양**
(생성 7 + 마이그레이션 + 스키마)이다. 커밋에 포함하는 것이 이 저장소의 방식이므로 그대로 따랐다.

**이 누락은 내 라운드 셋도, 독립 패스 두 번도 잡지 못했다.** 여섯 경로가 전부 "계획서가 말하는
것"을 검사했고, "계획서가 말하지 않은 산출물"은 아무 경로도 보지 않았다. FEAT-52에서 나온
「삭제로 죽는 식별자를 아무도 열거하지 않았다」와 같은 모양의 구멍이다 — 그때는 삭제, 이번엔
생성이다. **카탈로그 갱신 규칙(「표 밖에서 잡힌 결함은 표의 결함이다」)에 해당할 수 있다.**
사용자에게 제시한다.

### 게이트 — 전부 직접 실행

| 게이트 | 결과 |
| --- | --- |
| `npm run check -w apps/web` | **EXIT 0** (verify:fsd:test → verify:fsd "passed" → next lint → tsc). 경고 1건은 FEAT-52가 남긴 `ClipDraftCard.tsx:54 'playUrl'`로 이 항목과 무관하며 FEAT-56이 받는다 |
| `npm test -w apps/web` | **`suites 42 / pass 178 / fail 0`** — 계획서가 못박은 "같은 숫자"와 정확히 일치 |
| `python -m unittest discover -s apps/backend` | **`Ran 113 tests ... OK`** — 불변(백엔드는 Prisma를 쓰지 않는다) |

**`tsc`가 이 항목의 진짜 게이트라는 계획서 주장이 여기서 실증됐다.** `captionStyle`이 없는 새
클라이언트로 `tsc --noEmit`이 통과했다 — 남은 활성 참조가 하나라도 있었으면 `TS2339`로
깨졌을 자리다(독립 패스 2차가 음성 시험으로 그 사실을 미리 보였다).

구조 확인도 했다: `ClipDraftScalarFieldEnum`에서 `captionStyle` **소멸**,
`UploadedFileScalarFieldEnum`에는 **잔존**, `ClipDraftPayload` 인근 0건.

### 아직 하지 않은 것 — DB 적용

**마이그레이션을 적용하지 않았다.** 계획서 「적용 순서」대로 **커밋·푸시 → `main` 합류·배포(새
클라이언트) → 그 뒤에 별도 승인 → `migrate deploy`** 순서다. 지금 적용하면 프로덕션에 떠 있는
옛 클라이언트가 없는 컬럼을 SELECT해 검토 화면·편집 저장·렌더 디스패치·업로드 상세가 깨진다.

적용 직전에 `SELECT count(*) FROM "ClipDraft" WHERE "captionStyle" IS NOT NULL;`로 **지워지는
행 수를 소유자에게 보고한 뒤** 승인을 받는다.
