# TASK_BACKLOG

> 최초 부트스트랩: README.md의 Known Issues / Credit System 항목을 옮겨 담음.
> PM 에이전트는 이 파일과 PROJECT_BOARD.md만 읽고 우선순위를 판단한다.
> 보드에 올라가는 것만으로는 제거하지 않는다. 담당 에이전트가 보드에 `status: 완료`로 기록한 시점에, 그 에이전트가 여기서 해당 항목을 제거한다.
> `area`는 실제 코드 경로여야 한다. PM은 코드를 읽지 않으므로 이 값을 그대로 보드에 옮긴다 — 여기가 틀리면 보드도 틀린다.

## Backend / Pipeline

- [ ] **BUG-14**: Korean 렌더 자막이 영어 3단어 조각을 줄마다 번역해 끊기거나 어색할 수 있다 — **실물 확인이 먼저**, 결함으로 확인되면 번역 단위 개선
  - area: apps/backend/main.py
  - source: 2026-09-14 소유자와의 대화(FEAT-45~49와 같은 대화). **관측 1 — 번역 단위가 영어 단어 묶음이다**: Korean 렌더는 `main.py:847` `        script_text, subtitle_status = create_korean_subtitles_with_ffmpeg(transcript_segments, start_time, end_time, vertical_mp4_path, korean_output_path, gemini_client, max_word=3, caption_style=caption_style)`로 영어 단어를 기본 3개씩 묶은 뒤 그 줄들을 번역한다. **관측 2 — 줄 수와 줄 시각을 영어에 묶는다**: 프롬프트가 `main.py:486` `        3. Return the same number of translated lines as the input.`, `:505` `        - Do not skip or duplicate indices. If unsure about a line, repeat the English text as the translation.`을 요구하고, 결과는 `:548` `        korean_subtitles.append((start, end, korean_texts[i]))`로 영어 줄의 시각에 그대로 올린다. **진단(추정 — 실물 미확인)**: 영어(SVO)와 한국어(SOV)는 어순이 달라, 3단어 조각마다 한 줄씩 맞추라는 제약 아래에서는 (a) 뜻이 줄 경계를 넘어 흩어지거나 (b) 조각마다 억지로 완결된 어색한 문장이 나올 수 있다. 문맥은 전체 페이로드로 주므로 실제 품질은 괜찮을 수도 있다. **요구**: ① **선행 조건 — 소유자 실물 확인**: 기존 Korean 렌더 클립 1~2개를 보고 자막이 끊기거나 어색한지 판정한다. 문제가 없으면 이 항목은 닫는다(원인이 없는 수정을 하지 않는다). ② 결함으로 확인되면 번역 단위를 개선한다(예: 문장·구두점 경계로 묶어 번역한 뒤 줄 시각에 나눠 싣기). 방식은 계획 단계 판단. **⚠️ 계획에 영향 주는 결합**: 웹 캡션 미리보기는 백엔드의 큐 묶기를 그대로 이식했다(`apps/web/src/fsd/features/caption-style/model/caption-preview.ts:12` `export function buildCaptionCues(`, `caption-preview.test.mjs`가 계약으로 지킴). Korean의 묶기·줄 시각을 바꾸면 미리보기가 실렌더와 어긋나므로 **웹 이식을 같은 사이클에 맞춰야 한다** — `apps/web` 쪽은 별도 web-dev 항목이 필요하다(범위 밖 의존). FEAT-49도 같은 큐 타이밍을 전제하므로 영향이 간다. **못 덮는 범위**: 번역 품질은 Gemini 실출력이라 러너가 판정하지 못한다. **선행: 소유자 실물 확인.**

- [ ] **FEAT-55**: FEAT-52 뒤 죽는 클립별 캡션 스타일 경로 제거 — `moment_style` 인자 · moments `caption_style` 키 · 주석
  - area: apps/backend/caption_style_source.py + apps/backend/main.py
  - source: 2026-09-17 FEAT-51 인수에서 소유자 승인(`docs/plans/FEAT-51.md` 「범위 밖 의존」의 후속 정리 — 그 계획서가 "별도 후속 항목이며 FEAT-51에서는 하지 않는다"로 명시적으로 미뤘다). **관측**: FEAT-51은 `select_caption_style(moment_style, request_style)`에서 **moment 우선순위를 유지했다** — `caption_style_source.py`의 `    if isinstance(moment_style, dict):` / `        return moment_style`이 그대로다. 유지한 이유도 같은 파일 독스트링에 있다: `moment_style 인자는 FEAT-52 배포 전까지 웹이 여전히 보내는 클립별 스타일을 존중하기` / `위해 남긴다(전이 구간 회귀 0).` `main.py`도 render moments 조립의 `                                "caption_style": m.get("caption_style"),`와 `ProcessVideoRequest.moments` 주석의 `caption_style` 서술을 그대로 뒀다. **진단(추정)**: FEAT-52가 **배포되어** 웹이 `ClipDraft.captionStyle`을 끊고 FEAT-53이 컬럼까지 지우면 `moment.get("caption_style")`은 영구히 `None`이 되어 위 세 곳이 죽은 코드가 된다. 죽은 채 남으면 "클립별 스타일이 존재한다"는 잘못된 인상을 계약 문서(주석·시그니처)가 계속 준다. **요구**: ① 세 곳 제거. ② `select_caption_style`이 인자 하나짜리가 되면 순수 모듈로 유지할 가치가 줄어드니 **모듈 유지 vs `main.py` 인라인**을 「대안」에 적고 고른다 — FEAT-51이 대안 (B)를 기각한 근거(`main.py`는 `whisperx`→`torch`라 stdlib 러너로 안 돈다, B-5 커버리지 상실)가 인자 수와 무관하게 여전히 유효한지 재판정한다. ③ `test_caption_style_source.py`를 새 계약으로 갱신(모듈을 지우면 테스트도). ④ 게이트 `PYTHONUTF8=1 python -m unittest discover -s apps/backend -p "test_*.py"` — 착수 시점 기준선을 먼저 실측해 기대값을 계획서에 못박는다(FEAT-51 인수 시점 기준선은 113). **못 덮는 범위**: 실렌더 자막은 GPU·ffmpeg·pysubs2라 배포 후 실물. **선행: FEAT-52 배포 + FEAT-53.** 코드 병합만으로는 부족하다 — 프로덕션 웹이 옛 페이로드를 보내는 동안 이걸 지우면 그 클립들이 언어 기본값으로 떨어진다(FEAT-51이 막으려던 바로 그 회귀). **급하지 않다** — 동작 영향이 없다. **선행 충족(2026-09-19)**: FEAT-52 배포 완료 + FEAT-53 DB 적용 완료(`ClipDraft.captionStyle` 컬럼 소멸) — 착수 가능.

## Credit / Billing

(FEAT-01 「Credit System 마무리」는 2026-09-17 15차 감사 뒤 소유자 결정으로 제거했다 — 유령 항목이었다. 근거는 `docs/agents/doc-auditor/감사기록.md` 15차 「사용자 결정」. 결제·크레딧에 실제 결함이 드러나면 그때 관측·진단을 갖춘 새 항목으로 세운다.)

## Admin / Dashboard

## Web / 프론트엔드 구조

- [ ] **BUG-12**: admin 분석 화면의 시각 표시가 서버 런타임 타임존에 암묵적으로 의존한다
  - area: apps/admin/src/fsd/pages/analytics/ui/index.tsx
  - source: 2026-09-04 BUG-10 검증 중 "모든 시간 UTC" 지시의 여집합 확인에서 관측. **관측**: `pages/analytics/ui/index.tsx:236`이 `row.lastSeenAt.toLocaleString()`으로 로케일·타임존 인자 없이 시각을 그린다. 그 파일은 `"use client"` 지시자가 없어 서버 컴포넌트이고, 라우트 `app/(protected)/analytics/page.tsx:11`이 `AdminAnalyticsPage`를 렌더한다 — 서버에서만 렌더되므로 **하이드레이션 불일치(React #418)는 없고**, Vercel 런타임이 UTC라 현재 화면도 이미 UTC다. 같은 파일의 다른 `toLocaleString()` 여섯(`:59,60,61,154,161,198,235`)은 숫자 포매팅이라 시간이 아니다. **진단(추정)**: 결함이 아니라 암묵적 의존이다. 표시가 "UTC"인 이유가 코드가 아니라 배포 환경이라, 런타임 TZ가 바뀌거나 이 컴포넌트가 클라이언트로 내려가는 순간 조용히 달라진다. 후자는 web의 BUG-10이 실제로 겪은 경로다. **범위**: 한 줄이다. `apps/web`의 BUG-10이 만드는 `shared/lib/format-date.ts`와 같은 형태(로케일 `"en"` + 타임존 `"UTC"` 고정)를 admin에도 두고 그 한 곳을 교체한다. 워크스페이스가 달라 코드 공유는 하지 않는다(`packages/` 공용 모듈은 현재 없음 — 만들지 여부는 계획 단계 판단). **순서**: BUG-10 뒤. 그쪽 포매터 모양이 정해지면 그대로 베낀다.

- [ ] **FEAT-44**: 낡는 줄번호 인용(다른 파일의 `main.py` 30줄 + web 내부 3곳)을 함수명·코드 내용 앵커로 교체 — **동작 무변경**
  - area: apps/web/src/fsd/features/caption-style + apps/web/src/fsd/widgets/clip-draft-review + apps/web/src/fsd/shared/config/constants.ts + apps/web/src/app/layout.tsx + apps/backend/translation_fallback.py + .claude/agents/backend-dev.md + .claude/agents/feature-scout.md
  - source: 2026-09-14 FEAT-43 인수에서 소유자와 결정. **관측 1**: FEAT-43이 `main.py` 맨 위에 import 한 줄을 더하자(`main.py:44` `from moment_prompt import build_moment_prompt`) 그 아래를 가리키던 교차 파일 인용이 전부 한 줄씩 어긋났다. 인수 때 `main\.py:\d+` 전역 grep(이력 문서 제외)으로 열거한 결과 코드 주석·지시문에 **30줄**이 남아 있다 — `apps/web/src` 23줄(`caption-preview.ts` 10·`constants.ts` 4·`layout.tsx` 2·`CaptionPreviewPlayer.tsx` 2·`clip-draft-review/ui/index.tsx` 2·`caption-preview.test.mjs` 2·`review-language-notice.ts` 1), `apps/backend/translation_fallback.py` 2줄, `.claude/agents/` 5줄(`backend-dev.md` 1·`feature-scout.md` 4). 메인 루프 소유 문서(두 `CLAUDE.md`·백로그·원장)는 인수 때 이미 교정했다. **관측 2 — 표본(현재 내용 대조)**: `review-language-notice.ts:3` `// (apps/backend/main.py:837 elif selected_language == "Korean" → :840`인데 그 줄은 이제 `main.py:838` `    elif selected_language == "Korean":`. `constants.ts:77` `MARGINV: { top: 200, bottom: 260 }, // main.py:141-142`인데 실제는 `main.py:142` `    "top": 200,`·`:143` `    "bottom": 260,`. `caption-preview.ts:10` `// apps/backend/main.py:287-345 create_subtitles_with_ffmpeg 의 묶기를 그대로 옮긴다.`인데 함수는 `main.py:288` `def create_subtitles_with_ffmpeg(`. **관측 3 — FEAT-43 이전부터 낡은 것**: `clip-draft-review/ui/index.tsx:293` `(main.py:904 "Return exactly TARGET_COUNT moments if possible")` — 그 문장은 FEAT-43 전에도 `:997`이었고 지금은 `main.py`에 없다(`apps/backend/moment_prompt.py:74` `Return exactly TARGET_COUNT moments if possible.`). `backend-dev.md:154` `(`main.py:136`)`은 `resolve_caption_style`을 가리키는데 FEAT-43 전에도 `:157`, 지금은 `main.py:158` `def resolve_caption_style(`. `feature-scout.md:122` `ProcessVideoRequest`(`main.py:26-41`) — 실제 `main.py:47` `class ProcessVideoRequest(BaseModel):`. **진단(추정)**: 원천 파일이 한 줄만 바뀌어도 교차 파일 줄번호 인용은 전부 조용히 어긋나고, 아무 게이트도 이를 잡지 않는다. 다음 `main.py` 수정(FEAT-41)이 또 민다. `caption-preview.ts`류 주석은 "백엔드 자막 로직의 어느 부분을 이식했나"라는 계약의 연결이라, 에이전트가 계획 때 실제로 따라가다 다른 코드를 읽게 된다. **결정(대화에서 확정)**: 줄번호를 다시 맞추지 않는다 — 곧 또 낡는다. **함수명(+필요하면 그 줄의 짧은 코드 인용)으로 교체**한다(예: `// main.py create_subtitles_with_ffmpeg — 잔여 단어 flush`). 백로그 비고의 "줄 내용을 함께 인용한다"와 같은 원리다. **요구**: ① 위 30줄의 인용을 앵커로 교체. 가리키는 대상 자체가 옮겨간 곳(`index.tsx:293`)은 올바른 파일로 바꾼다. ② `translation_fallback.py:21·45`의 `(기존 main.py:516-524와 동치`처럼 **리팩터링 이전 코드를 가리키는 역사적 서술**은 교체할지 "기존" 서술로 남길지 계획 단계에서 줄마다 판정한다. ③ 코드·테스트 동작 무변경 — 주석과 지시문 문장만. 게이트: `npm run check -w apps/web` · `npm test -w apps/web` · `python -m unittest discover -s apps/backend -p "test_*.py"`. ④ 완료 시 `main\.py:\d+` 전역 grep을 다시 돌려 area 안 잔존 0을 보인다. **제외**: (`schema.prisma`의 `Clip` 주석 몫은 **FEAT-47이 2026-09-16 처리했다** — 주석 한 줄에도 생성 클라이언트 재생성이 따라붙어 스키마를 바꾸는 항목에 태웠다. 이제 함수명·모델명 앵커다.) `docs/plans`·`docs/agents`·`docs/proposals`·완료 문서의 인용은 작성 시점 기록이라 제외(`docs/agents/README.md` 「감사 대상이 아니다」와 같은 이유). **담당: main-loop.** 쓰기 범위가 web-dev(`apps/web`)·backend-dev(`apps/backend`)·어느 dev도 못 쓰는 `.claude/agents/`로 갈린다 — FEAT-19·FEAT-26·FEAT-38 전례. **순서 제약**: FEAT-40이 `caption-preview.ts`·`caption-preview.test.mjs`를 옮기고 "옮긴 테스트 diff는 경로 변경뿐"을 인수 기준으로 삼으므로 **FEAT-40과 동시 진행하지 않는다** — 먼저 끝난 쪽의 결과를 뒤쪽이 기준으로 삼는다. (FEAT-40은 2026-09-14 완료 — `caption-preview.ts`·`caption-preview.test.mjs`·`CaptionPreviewPlayer.tsx`는 이제 `apps/web/src/fsd/features/caption-style/` 아래다.) **추가 범위 ⑤ (2026-09-15 소유자 승인 — 10차 문서 감사 어긋남 1·2·4)**: `main.py`가 아니라 web 내부를 가리키는 낡은 줄번호 인용 세 곳도 같은 원리(심볼명·문구 앵커)로 교체한다. 셋 다 area(`widgets/clip-draft-review`) 안이다. (a) `widgets/clip-draft-review/model/review-language-notice.ts:2` `// 영어 단어들이고(ClipDraftCard.tsx:113), 한국어 번역은 렌더 단계에서만 만들어진다` — `previewText` 선언은 이제 `ClipDraftCard.tsx:114` `  const previewText = wordsInRange.map((word) => word.word).join(" ");`. (b) `widgets/clip-draft-review/ui/_component/ClipDraftCard.tsx:476` `{/* previewText는 영어 원문이다(:113). 헤더 안내가 스크롤로 벗어나도` — 같은 파일 자기 인용, 실제 `:114`. (c) `widgets/clip-draft-review/ui/index.tsx:281-282` `화면은 영어 원문을 보여준다. 유일한 기존 안내(CaptionStyleEditor` / `:310-311)는 기본 닫힘 다이얼로그 안 11px라 소유자조차 못 봤다 — 항상` — 가리키는 `features/caption-style/ui/CaptionStyleEditor.tsx`의 라이브 미리보기 안내는 이제 `:318-323`(`:319` `Live preview on your video — the whole frame is shown here. The final`)이다. FEAT-42가 `sample` 분기를 넣으며 밀었다. 같은 주석 `:280`의 `main.py:837/840`은 이미 ①의 30줄에 들어 있다. 완료 시 세 인용 원문(`ClipDraftCard.tsx:113`·`(:113)`·`CaptionStyleEditor` 뒤 `:310-311`)이 area에서 0건임을 grep으로 보인다. **선행 없음. 급하지 않다** — 동작 영향이 없어 우선순위는 pm 판단.

- [ ] **FEAT-54**: 캡션 기본값을 언어별로 — 한 번 커스터마이즈하면 영어·한국어가 같은 값을 쓴다
  - area: packages/db/prisma/schema.prisma + apps/web/src/fsd/features/settings + apps/web/src/fsd/features/upload + apps/web/src/fsd/pages/settings
  - source: 2026-09-16 소유자와의 대화(FEAT-52 설계 중 관측, 범위에서 분리). **관측 1**: `User.defaultCaptionStyle`은 `Json?` **한 칸**이고 언어를 모른다(`schema.prisma`의 `    defaultCaptionStyle         Json?`). **관측 2 — 기본값은 언어마다 다르다**: `shared/config/constants.ts`의 `  DEFAULT_FONT_SIZE: { English: 122, Korean: 130 },` · `  DEFAULT_MAX_WORDS: { English: 5, Korean: 3 },` · `  DEFAULT_OUTLINE_WIDTH: { English: 1.1, Korean: 1.3 },`. **관측 3 — 프리셋은 그걸 피해 간다**: `CAPTION_STYLE_PRESETS` 넷이 전부 `      fontSize: null,`·`      maxWordsPerLine: null,`이다. "언어가 정해야 할 값은 프리셋이 손대지 않는다"가 이미 선 설계다. **진단(추정)**: 그래서 프리셋만 고르면 문제가 없지만, `fontSize`나 `maxWordsPerLine`을 **한 번이라도 직접 건드리면** 그 값이 두 언어 모두에 적용되어 언어 적정값이 깨진다(영어용 6단어를 고르면 한국어 자막도 6단어 — 적정은 3). 두 언어를 다 쓰면서 커스터마이즈한 사용자에게만 나타나므로 지금까지 드러나지 않았다. **요구**: ① `User.defaultCaptionStyle`을 `defaultCaptionStyleEnglish`·`defaultCaptionStyleKorean` **두 컬럼**으로 나눈다 — `captionStyleSchema`를 모양 변경 없이 그대로 재사용할 수 있다. (Json 맵 한 칸은 맵 검증기와 부분 갱신 로직이 새로 필요하고 `saveDefaultCaptionStyle(null)`의 "비우기" 의미가 "어느 언어를 비우나"로 흐려진다 — `docs/plans/FEAT-38.md` 대안 (A)가 블롭을 기각한 이유와 같은 계열.) ② 업로드 스냅샷(`features/upload/api`의 `      captionStyle: defaultCaptionStyle, // 업로드 시점에 고정되는 스냅샷`)이 `UploadedFile.language`에 맞는 쪽을 **대입**한다 — 하류(`UploadedFile.captionStyle`·render 요청·`resolve_caption_style`)는 단일 값 그대로라 **무변경**이다. ③ 설정 화면은 FEAT-52가 넣는 미리보기 언어 토글을 **편집 대상 전환**으로 승격한다. ④ 기존 행 마이그레이션 — 사용자의 `defaultLanguage` 쪽에 넣고 반대쪽은 `null`. **담당**: 스키마가 끼어 있어 마이그레이션만 main-loop로 뗄지는 계획 단계 판단(FEAT-47·FEAT-53 전례). **못 덮는 범위**: 언어 전환 시 편집 대상이 바뀌는지는 배포 후 육안. **선행: FEAT-52. 급하지 않다** — 두 언어를 다 쓰면서 커스터마이즈하는 사용자에게만 나타난다.

- [ ] **BUG-15**: 홈페이지가 만들지 못하는 화면비를 약속한다 — `square`·`landscape` 문구 철회
  - area: apps/web/src/fsd/pages/home/config/index.ts
  - source: 2026-09-17 15차 문서 감사가 발견, 소유자 결정(철회). **관측 1 — 약속**: `pages/home/config/index.ts:100` `      "Accept, tweak, or regenerate. Export vertical, square, and landscape ratios.",`(workflowSteps 3단계 `Review & publish`의 설명). **관측 2 — 코드는 세로 하나뿐**: `apps/backend/main.py:214` `    target_width = 1080` · `:215` `    target_height = 1920`이고 ASS `PlayResX/Y`도 같다. `square`·`landscape`·`aspect` 경로를 `apps/backend/*.py`와 `apps/web/src/inngest/` 전역에서 찾으면 **0건**이다(메인 루프 직접 grep). **관측 3 — 같은 파일이 자기모순이다**: `pages/home/config/index.ts:51` `      "Columbia face tracks steer 1080x1920 crops or blurred backgrounds, rendered via NVENC at 25 fps.",` — 한 페이지 안에서 `:51`은 1080x1920만, `:100`은 세 비율을 약속한다. 다른 마케팅 페이지도 전부 세로만 약속한다(`pages/features/config/index.ts:42` · `pages/how-it-works/config/index.ts:25` · `pages/ai-podcast-clipper/config/index.ts:46`). **진단(추정)**: 이 문장은 "못 지킨 약속"이 아니라 **처음부터 제품과 안 맞는 문장**이다 — 라우트 이름이 `youtube-shorts-generator`이고 산출물은 세로 쇼츠다. **BUG-06과 같은 부류다**(코드가 아니라 판매 문구가 거짓을 말한다). 다만 BUG-06은 사용자가 우연히 발견했고 이건 감사가 먼저 잡았다. **요구**: ① `:100`의 문구에서 `square, and landscape`를 걷어내고 세로 산출물에 맞는 표현으로 바꾼다(`:51`과 어긋나지 않게). ② 같은 종류의 약속이 다른 유저 대면 카피에 더 있는지 `square|landscape|aspect|ratio` grep으로 여집합을 확인한다. **화면비 3종을 실제로 만드는 것은 이 항목이 아니다** — `create_vertical_video` 합성부·ASS `PlayRes`·크롭 로직·미리보기의 9:16 전제를 전부 건드리는 별개 기능이고, 필요해지면 그때 자기 근거로 선다. **못 덮는 범위**: 문구는 배포 후 육안. **선행 없음. 급하지 않다** — 아직 유저가 없어 이 약속에 걸린 사람이 없다.

- [ ] **FEAT-56**: FEAT-52·53이 남긴 죽은 코드 여섯 정리 — `playUrl` prop · 낡은 주석 3곳 · 소비자 없는 재수출 · 잔여 문구 둘 · **도달 불가가 된 라이브 미리보기 경로**
  - area: apps/web/src/fsd/widgets/clip-draft-review + apps/web/src/fsd/features/clip-review + apps/web/src/fsd/shared/config/constants.ts
  - source: 2026-09-17 FEAT-52 인수에서 관측·소유자 승인. **관측 1 — lint 경고가 새로 생겼다**: `npm run check -w apps/web`가 `./src/fsd/widgets/clip-draft-review/ui/_component/ClipDraftCard.tsx` `54:3  Warning: 'playUrl' is defined but never used.`를 낸다(경고라 EXIT 0은 유지). 삭제된 `CaptionStyleDialog`가 미리보기 플레이어에 `playUrl`을 넘기던 **유일한 소비자**였다. 죽은 것은 셋이다 — `ClipDraftCard.tsx:38` `  playUrl: string | null;`(props 타입) · `:54` `  playUrl,`(구조분해) · `widgets/clip-draft-review/ui/index.tsx:454` `              playUrl={readyPlayUrl}`(전달). **`readyPlayUrl` 자체는 죽지 않았다** — `ui/index.tsx:228` `  }, [readyPlayUrl]);`가 의존성으로 쓰고 위젯 본체 플레이어(`:403` `{playUrlState.status === "ready" && (`)는 `playUrlState`를 직접 쓴다. 지우는 것은 세 줄이다. **관측 2 — 사라진 코드를 가리키는 주석 3곳**: `widgets/clip-draft-review/model/use-clip-draft-review.ts:128` `                  // captionStyle은 의도적으로 낙관 반영하지 않는다. 헤더 카운트와`(그 필드가 이제 없다) · `:206` `  // 전체 선택/해제. applyStyleMutation과 같은 순차 루프 패턴을 따르되,` · `:216` `      // applyStyleMutation과 동일하게 대상 전체를 무조건 저장한다.`(`applyStyleMutation`은 삭제됐다). **관측 3 — 소비자 0인 재수출**: `features/clip-review/index.ts:7` `export type { CaptionStyleInput } from "./model/schemas";` — FEAT-52가 마지막 소비자를 지웠다. 정의는 `shared/config/caption-style-schema.ts:44`에 남고 설정 화면 경로가 쓰므로 **재수출만** 대상이다. **관측 4 — 부분적으로만 고친 문장**: `shared/config/constants.ts:114` `` * 렌더 디스패처의 JSON 캐스팅, 검토 UI가 전부 이 타입 하나를 참조한다.`` — 바로 윗줄은 FEAT-52 인수 때 갱신했는데 이 줄의 "검토 UI"는 그대로다. 검토 UI는 이제 `CaptionStyle`을 참조하지 않는다. **진단(추정)**: 넷 다 "삭제가 만든 잔재"라는 한 부류다. 계획서 「고칠 파일」이 *추가·수정할* 것은 정밀하게 열거했지만 *삭제로 죽는* 식별자·주석은 열거하지 않았고, 메인 루프 검증도 스케치가 **쓰는** 식별자만 대조해 같은 구멍을 지났다(`docs/agents/main-loop/FEAT-52.md` 「인수에서 새로 나온 것」). **관측 5 (2026-09-17 15차 감사 추가) — 라이브 미리보기 경로가 통째로 도달 불가가 됐다**: `CaptionStyleEditor`의 비-`sample` 소비자는 삭제된 `CaptionStyleDialog` 하나뿐이었다. 렌더 소비자를 전수 확인하면 이제 `pages/settings/ui/index.tsx:260` **하나**이고 `sample`(항상 true)·`playUrl={null}`을 고정 전달한다(메인 루프 직접 확인). 그래서 `features/caption-style/ui/CaptionStyleEditor.tsx:336` `        {sample ? (`의 else 쪽 라이브 안내 둘과, `features/caption-style/model/sample-captions.ts:89` `export function previewCaptionCues(` / `:94` `  return language === "Korean" && !sample ? koreanSampleCues(cues) : cues;`의 `!sample` 갈래(그리고 `:70` `export function koreanSampleCues(`)가 실행되지 않는다. **⚠️ 이건 lint로 안 잡힌다** — `sample`·`playUrl`은 타입상 여전히 "쓰이는" prop이라 `no-unused-vars`가 걸리지 않는다(감사가 짚은 경고). **관측 6 (2026-09-17 FEAT-53 계획 검증에서 추가) — 공용 검증기 주석이 없어질 컬럼을 센다**: `shared/config/caption-style-schema.ts:7` `// User.defaultCaptionStyle / UploadedFile.captionStyle / ClipDraft.captionStyle JSON의` · `:8` `// 공용 검증기. 세 컬럼이 같은 CaptionStyle 모양을 공유하므로 하위 레이어(shared)에 둬` — FEAT-53이 `ClipDraft.captionStyle`을 drop하면 **컬럼은 셋이 아니라 둘**이 되고 그 인용도 거짓이 된다. 같은 문장이 드는 `features/clip-review`도 FEAT-52 뒤엔 그 스키마를 `ClipDraft` 저장에 쓰지 않는다(재수출만 남았고 그 재수출이 관측 3이다). **⚠️ 순서**: 이 줄은 **FEAT-53이 적용된 뒤에 완전히 거짓이 된다** — FEAT-56을 FEAT-53보다 먼저 하면 "둘"이라 쓴 주석이 잠시 틀린 상태가 된다. FEAT-53 뒤에 하거나, 그 전에 한다면 두 상태를 다 견디는 표현으로 쓴다. **요구**: ① 위 **여섯** 제거·정정. ② 게이트 둘이다 — (a) `npm run check -w apps/web`가 **경고 0으로** 통과한다(현재 `playUrl` 경고 1건), (b) **`sample=false`로 도달하는 경로가 0임을 소비자 전수 열거로 보인다.** (a)만으로는 관측 5가 안 잡히므로 (b)가 없으면 이 항목을 끝내도 잔재가 남는다. ③ `sample-captions.test.mjs`의 라이브(`!sample`) 케이스도 함께 판정한다 — 그 테스트가 지키던 계약이 도달 불가가 됐다면 케이스도 정리 대상이고, 그 판단을 계획서에 근거와 함께 남긴다. ④ `npm test -w apps/web`의 변화는 ③의 결정에 따라 정해지므로, 착수 시점 기준선(`tests 178 / suites 42`, 파일 25)을 실측해 기대값을 계획서에 못박는다. **동작 무변경** — 실제로 렌더되는 마크업·저장되는 값·발신되는 계측이 하나도 달라지지 않는다(도달 불가 코드만 사라진다). **선행 없음**(FEAT-52 완료). **급하지 않다.**

- [ ] **FEAT-57**: `clip_review_caption_style_edited` 계측 이름을 계약에서 완전 제거 — 지금은 죽은 등록으로 남아 있다
  - area: packages/db/src/analytics-contract.ts + apps/web/src/fsd/shared/analytics/lib/metadata.ts + apps/admin/src/fsd/entities/analytics-event/api/queries.test.mjs
  - source: 2026-09-17 FEAT-52 인수에서 소유자 승인(`docs/plans/FEAT-52.md` 「범위 밖 의존」이 "백로그 후보로 제시한다"로 미룬 것). **관측 1 — 발신부는 이미 없다**: FEAT-52가 검토 다이얼로그와 훅을 지워 이 이벤트를 발신하는 코드가 0이 됐다. **관측 2 — 그런데 키는 지울 수 없었다**: `apps/web/src/fsd/shared/analytics/lib/metadata.ts:48` `  clip_review_caption_style_edited: ["uploadedFileId", "preset", "appliedToAll"],`는 같은 파일 `:63` `} as const satisfies Record<AnalyticsEventName, readonly string[]>;`에 묶여 있고 `AnalyticsEventName`은 `packages/db/src/analytics-contract.ts:30` `  "clip_review_caption_style_edited",`에서 온다. **실측(FEAT-52 검증 경로 7)**: 키만 지우고 `tsc --noEmit`을 돌리면 `metadata.ts(64,12): error TS1360: ... Property 'clip_review_caption_style_edited' is missing in type ... but required in type 'Record<...>'`가 난다. 그래서 `packages/db`를 먼저 고쳐야 하는데 web-dev 쓰기 범위 밖이었다. **관측 3 — 소비자 여집합**: 이 이벤트 **이름**을 읽는 곳은 `apps/web`의 계약 맵과 `packages/db`의 목록뿐이고 `apps/admin`에 직접 참조는 0이다. 다만 **목록 전체를 쓰는 곳이 셋 있다** — `apps/admin/src/fsd/entities/analytics-event/api/queries.ts:74`·`:90` `  const events = await listRangeEvents(input, ANALYTICS_EVENT_NAMES);`(범위 조회의 이름 집합), `apps/web/src/app/api/analytics/events/route.ts:15` `  name: z.enum(ANALYTICS_EVENT_NAMES),`(수집 엔드포인트 검증), 그리고 `apps/admin/.../queries.test.mjs:6` `const ANALYTICS_EVENT_NAMES = [`(**자체 하드코딩 사본**). **진단(추정)**: 이름을 지우면 (a) 수집 엔드포인트가 그 이름의 이벤트를 거부하고(발신자가 없으므로 무해), (b) admin 범위 조회가 그 이름을 더는 묻지 않아 **과거에 쌓인 행이 분석 화면에서 보이지 않게 된다**, (c) admin 테스트의 하드코딩 사본이 계약과 어긋나 깨진다. **요구**: ① **(b)를 먼저 결정한다** — 과거 행의 가시성을 포기할지, 아니면 이름을 남기되 "폐기됨" 표시를 두는 방식을 쓸지. 결정 없이 지우면 이력이 조용히 사라진다. 포기 쪽이면 그 판단을 계획서에 근거와 함께 남긴다. ② 결정에 따라 `analytics-contract.ts`의 이름과 `metadata.ts`의 키를 제거한다. ③ `queries.test.mjs`의 하드코딩 사본을 맞춘다. ④ 게이트: `npm run check -w apps/web` · `npm test -w apps/web` · `npm run check -w apps/admin` · `npm test -w apps/admin` 넷 다. **담당: main-loop** — 쓰기 범위가 `packages/db`(어느 dev도 못 씀)·`apps/web`·`apps/admin` 셋으로 갈린다(FEAT-19·FEAT-26·FEAT-38·FEAT-47 전례). **선행 없음. 급하지 않다** — 죽은 등록의 비용은 미사용 enum 멤버 하나다.

## Pipeline 운영 / 검증 하니스

- [ ] **FEAT-27**: 계획서 검증 하니스 — `verification-paths.md` 9경로를 deterministic/hybrid/reasoning으로 분류하고, 기계 재현 가능한 경로만 script/tool로 구현
  - area: scripts/verify-plan (신설, 루트) + docs/plans/verification-paths.md + .claude/agents/plan-verifier.md
  - source: 사용자 발주(2026-08-27 세션, 브리프 원문은 아래 「요구」). **관측**: 카탈로그의 9경로(`docs/plans/verification-paths.md:17-29`)는 매 검증 라운드 사람(메인 루프·`plan-verifier`)이 손으로 실행한다 — FEAT-22 기록에서 경로 1 인용 전수는 인용 줄을 하나씩 눈으로 대조(`docs/agents/main-loop/FEAT-22.md:47-48`), 경로 3 before 바이트 일치는 diff 전문 대조(`:172`), 경로 2 조립 게이트는 check·test·verify:fsd:final 수동 실행(`:36`)이었고, 3사이클 동안 같은 경로를 매 라운드 재수행했다. `plan-verifier`는 도구가 없어 하니스(스케치 추출·돌연변이·렌더)를 매번 스크래치패드에 재조립한다(`.claude/agents/plan-verifier.md:31`). 저장소 안 기계 검증 선례는 `apps/admin/scripts/verify-fsd-boundaries.mjs` 하나 — 종료코드 PASS/FAIL(`:722` `process.exitCode = 1`), `--final` 모드(`:708`), 규칙 ID별 메시지, 셀프테스트 13건(`verify-fsd-boundaries.test.mjs:21-196`, 음성 픽스처 포함)을 갖춰 이 항목이 요구하는 script/tool 성질의 저장소 내 모델이다. **진단(추정 — 분류 확정은 계획 단계)**: 9경로는 성격이 셋으로 갈린다. *deterministic*(입력이 계획서+트리뿐, 판단 불요): 1 인용 전수 대조의 "줄 존재 + 인용 내용 일치" 부분, 3 before/after 기계 적용(before 블록 바이트 일치·patch dry-run), 9 구조적 아티팩트 검사(파서), 7 음성 시험 중 검사가 스크립트인 경우(규칙 제거→검사 실패 확인). *hybrid*(대상·설정 선택은 판단, 실행·판정은 기계): 2 스케치 추출·실행(어느 워크스페이스 설정으로 어떤 파일에 실행할지), 5 돌연변이(명세→테스트 변환은 reasoning, 주입·사멸 판정은 기계), 8 실물 렌더(대상 선택 reasoning, `renderToStaticMarkup` 실행 기계). *reasoning*(의미 해석 본체): 4 전칭 여집합 열거(주장 발췌·여집합 설계), 6 실제 사건 재생(실측 데이터 선택·의미 판정), 그리고 hybrid의 판단 절반. 현재는 세 부류가 구분 없이 산문 증거로만 남아 라운드마다 비용과 편차가 생긴다. **요구(발주 원문 요지)**: ① `verification-paths.md`는 에이전트가 읽는 source of truth로 유지하고 전체를 하나의 script PASS/FAIL 시스템으로 바꾸지 않는다. ② 9경로를 deterministic/hybrid/reasoning으로 분류한 뒤 기계적으로 재현 가능한 path만 script/tool로 구현한다. ③ script/tool은 동일 입력→동일 결과 · exit code 기반 PASS/FAIL · evidence 출력 · negative fixture/self-test를 가진다. ④ 의미 해석이 필요한 검증은 `plan-verifier`에 남긴다. ⑤ 최종 verdict는 mechanical evidence + reasoning evidence를 종합하는 구조로 만든다. ⑥ 새 verification agent는 추가하지 않고 현재 main loop + `plan-verifier` 구조를 유지한다. ⑦ 기존 `apps/admin`의 `verify:fsd` 패턴 등 재사용 가능한 구현을 먼저 조사한다. ⑧ 절차: 현재 구조 분석 → 변경 계획 제시 → 구현 → 변경 파일·검증 결과·남은 reasoning-only 영역 보고(파이프라인상 계획서=분석+계획, 구현 보고=③④). **설계 시 판단**: 카탈로그 표에는 열 추가(분류·도구 명령)만 하고 행 제거는 하지 않는다(행 제거는 사용자 승인만 — `verification-paths.md:43`). 종합 구조는 새 파일이 아니라 기존 기록 자리에 둔다 — `docs/agents/main-loop/<항목ID>.md` 라운드 기록과 `plan-verifier` 보고 템플릿(`plan-verifier.md:65-82` [실행한 경로])에 mechanical 증거(도구 출력·종료코드)와 reasoning 증거를 나눠 적는 형식. 도구는 루트 `scripts/verify-plan/`에 두고 계획서 경로를 인자로 받는다(계획서는 backend·web·admin 공통이라 워크스페이스 안에 두지 않음). **담당**: 루트 `scripts/`·`docs/plans/`·`.claude/agents/`는 dev 로스터의 쓰기 범위 밖 — FEAT-19·FEAT-26 전례대로 main-loop가 계획·구현. **범위 밖 의존**: `plan-verifier`가 도구를 쓰려면 브리핑에 명령이 실려야 하고 스크래치패드 제약(`plan-verifier.md:31·36`)과 충돌하지 않아야 한다 — 도구는 저장소 파일을 읽기만 하고 출력은 stdout이라 충돌 없음을 계획서에서 확인. 경로 9는 실증 사례가 아직 없어(`verification-paths.md:29`) 첫 fixture가 곧 첫 실증이 된다. **경로 1의 실측 사례 추가(2026-09-05)**: 이 세션에서 인용 드리프트가 네 건 나왔고 전부 사람이 손으로 잡았다 — ①② 백로그의 `upload-detail :20,21`·`OrderHistory :16`이 각각 `:21,22`·`:17`로 밀림(원인: BUG-10이 같은 파일에 임포트 추가), ③ FEAT-31 계획서의 선례 인용 `uploaded-file/index.ts:1-55`가 실제 54행, ④ FEAT-33 계획서의 `public-header.tsx:15-19`가 실제 16-19행(15행은 빈 주석줄). 넷 다 **줄 존재가 아니라 내용 대조로만** 잡히는 부류이고, 넷 다 기계적으로 판정 가능하다(인용 문자열이 그 줄에 있는가). ③④는 독립 패스 두 사이클을 더 쓰게 만들었다 — 경로 1의 자동화가 아끼는 것은 사람의 눈이 아니라 **사이클 수**다.

## 비고

- 위 항목의 우선순위는 아직 정해지지 않음 — PM 에이전트가 매일 이 목록에서 오늘 처리할 1~2개를 골라 PROJECT_BOARD.md에 "승인대기"로 기록한다.
- 새 이슈가 생기면 이 파일 하단에 추가한다 (형식: ID, 설명, area, source).
- `source`를 쓸 때는 **관측과 진단을 분리한다.** 무엇을 봤는지(관측)와 왜 그렇다고 생각하는지(진단)를 한 문장에 섞지 않고, 진단은 추정임을 표시한다. FEAT-02가 실제로 이렇게 틀렸다 — 관측("10분 소스에 4개 요청 시 적게 생성됨")은 맞았는데 진단("유저가 무리한 개수를 골라서")이 틀렸고, 둘이 섞여 있어 담당 에이전트가 틀린 진단을 요구사항으로 받아 계획을 세 번 썼다. 반대로 BUG-06은 `파일:줄` 근거, 이미 맞는 곳, 수정 방향까지 적혀 있어 계획이 한 번에 끝났다.
- **`파일:줄`을 쓸 때는 그 줄의 내용도 함께 인용한다.** 백로그 항목은 몇 주를 살고 그 사이 우리 커밋이 줄을 민다 — 줄번호는 낡지만 내용은 안 낡는다. 2026-09-05 FEAT-33 계획 단계에서 실측: 이 파일이 적은 `upload-detail/ui/index.tsx:20,21`과 `OrderHistory.tsx:16`이 각각 `:21,22`·`:17`로 밀려 있었고, 원인은 우리 자신의 작업이었다(BUG-10이 두 파일에 `formatDate` 임포트를 추가하면서 아래 줄이 한 칸씩 내려갔다). 내용이 함께 적혀 있었다면 담당 에이전트가 줄번호 대신 문자열로 찾아 드리프트를 즉시 알아봤을 것이다. 이 대조는 FEAT-27이 기계화하려는 경로 1(인용 전수 대조)의 몫이기도 하다.
