# TASK_BACKLOG

> 최초 부트스트랩: README.md의 Known Issues / Credit System 항목을 옮겨 담음.
> PM 에이전트는 이 파일과 PROJECT_BOARD.md만 읽고 우선순위를 판단한다.
> 보드에 올라가는 것만으로는 제거하지 않는다. 담당 에이전트가 보드에 `status: 완료`로 기록한 시점에, 그 에이전트가 여기서 해당 항목을 제거한다.
> `area`는 실제 코드 경로여야 한다. PM은 코드를 읽지 않으므로 이 값을 그대로 보드에 옮긴다 — 여기가 틀리면 보드도 틀린다.

## Backend / Pipeline

## Credit / Billing

- [ ] **FEAT-01**: Credit System 마무리 (현재 개발 중 상태)
  - area: apps/web/src/fsd/features/billing + apps/web/src/fsd/entities/user + apps/web/src/inngest
  - source: README "Currently in Development"

## Admin / Dashboard

## Web / 프론트엔드 구조

- [ ] **BUG-12**: admin 분석 화면의 시각 표시가 서버 런타임 타임존에 암묵적으로 의존한다
  - area: apps/admin/src/fsd/pages/analytics/ui/index.tsx
  - source: 2026-09-04 BUG-10 검증 중 "모든 시간 UTC" 지시의 여집합 확인에서 관측. **관측**: `pages/analytics/ui/index.tsx:236`이 `row.lastSeenAt.toLocaleString()`으로 로케일·타임존 인자 없이 시각을 그린다. 그 파일은 `"use client"` 지시자가 없어 서버 컴포넌트이고, 라우트 `app/(protected)/analytics/page.tsx:11`이 `AdminAnalyticsPage`를 렌더한다 — 서버에서만 렌더되므로 **하이드레이션 불일치(React #418)는 없고**, Vercel 런타임이 UTC라 현재 화면도 이미 UTC다. 같은 파일의 다른 `toLocaleString()` 여섯(`:59,60,61,154,161,198,235`)은 숫자 포매팅이라 시간이 아니다. **진단(추정)**: 결함이 아니라 암묵적 의존이다. 표시가 "UTC"인 이유가 코드가 아니라 배포 환경이라, 런타임 TZ가 바뀌거나 이 컴포넌트가 클라이언트로 내려가는 순간 조용히 달라진다. 후자는 web의 BUG-10이 실제로 겪은 경로다. **범위**: 한 줄이다. `apps/web`의 BUG-10이 만드는 `shared/lib/format-date.ts`와 같은 형태(로케일 `"en"` + 타임존 `"UTC"` 고정)를 admin에도 두고 그 한 곳을 교체한다. 워크스페이스가 달라 코드 공유는 하지 않는다(`packages/` 공용 모듈은 현재 없음 — 만들지 여부는 계획 단계 판단). **순서**: BUG-10 뒤. 그쪽 포매터 모양이 정해지면 그대로 베낀다.

- [ ] **FEAT-38**: 사용자 기본값 스키마 — `User` 컬럼 4개 + `UploadedFile.captionStyle` + analytics 이벤트 2개 (마이그레이션 1회)
  - area: packages/db/prisma/schema.prisma + packages/db/prisma/migrations + packages/db/src/analytics-contract.ts + apps/web/src/fsd/shared/analytics/lib/metadata.ts
  - source: 2026-09-09 소유자와의 설계 대화. **발단**: "영상마다 캡션 스타일·언어를 다시 고르지 말고 미리 설정해두게 하자". **관측 1**: 업로드 옵션 셋이 매번 리셋된다 — `UploadPodcast.tsx:69` `const [language, setLanguage] = useState<string>(DEFAULT_LANGUAGE);`, `:70` `useState<number>(DEFAULT_CLIP_COUNT)`, `:75` `useState<boolean>(false)`. 상수는 `shared/config/constants.ts:27-28`(`DEFAULT_LANGUAGE = SUPPORTED_LANGUAGES[0].value` / `DEFAULT_CLIP_COUNT = CLIP_COUNT_OPTIONS[2].value`)이고 사용자별 저장소가 없다 — `User` 모델에 설정 컬럼이 하나도 없고 `/dashboard/settings` 라우트도 없다(현재 dashboard·billing·uploads·uploads/[id]가 전부). **결정(대화에서 확정)**: ① 기본값 범위는 넷 — 언어·클립 수·생성 모드·캡션 스타일. ② 프로필 N개가 아니라 **단일 기본값 1벌**(N개면 업로드마다 "어느 프로필?"을 다시 고르게 되어 해결하려던 문제가 되살아난다. 이름 붙은 룩 수요는 기존 `CAPTION_STYLE_PRESETS` 4개가 흡수한다). ③ **컬럼 4개**(`defaultLanguage String?`·`defaultClipCount Int?`·`defaultReviewBeforeGenerate Boolean?`·`defaultCaptionStyle Json?`), 전부 nullable = "설정 안 함, 시스템 기본을 따른다". Json 블롭 한 칸으로 묶지 않는 이유는 `prepareUpload`의 복사가 파싱이 아니라 대입이어야 하기 때문이다 — 목적지 컬럼이 이미 타입을 갖고 있고(`schema.prisma:78` `language String @default("English")`, `:79` `targetClipCount Int @default(3)`, `:81` `reviewBeforeGenerate Boolean @default(false)`), 블롭이 망가지면 업로드 자체가 막힌다(결제 파이프라인 앞단). 또 `apps/admin`이 "몇 명이 Korean을 기본으로 두나"를 쿼리할 수 있어야 한다. ④ `UploadedFile.captionStyle Json?` 추가 — 업로드 시점 **스냅샷**. 라이브 참조(디스패치 시점에 User를 읽기)를 버린 이유: 검토 중 설정을 바꾸면 미리보기와 실렌더가 어긋나는데 그 회귀는 크레딧을 쓴 뒤에야 드러나고(`widgets/clip-draft-review/model/caption-preview.test.mjs`가 지키는 계약), 재처리 결과도 원본과 달라진다. 기존 셋이 이미 `UploadedFile` 컬럼에 박히는 것과 대칭이다. ⑤ analytics 이벤트 2개 추가 — `settings_viewed`, `settings_defaults_saved`(metadata `["source", "preset"]`; `source`로 설정 페이지 저장과 검토 다이얼로그의 "내 기본으로 저장"을 구분). 기존 `clip_review_caption_style_edited`(`analytics-contract.ts:30`, metadata `metadata.ts:48` `["uploadedFileId", "preset", "appliedToAll"]`)로 재사용하면 안 된다 — 그건 ClipDraft를 바꾸는 이벤트고 기본값 저장은 `User`만 바꿔서 `appliedToAll` 집계가 오염된다. **⚠️ 계획에 영향 주는 결합**: `ANALYTICS_METADATA_KEYS_BY_EVENT`는 `as const satisfies Record<AnalyticsEventName, readonly string[]>`로 **모든 이벤트 이름에 대한 Record**다(apps/web/CLAUDE.md 명시). 즉 `packages/db`에 이름만 추가하고 `apps/web`의 메타데이터 키를 안 넣으면 **컴파일 오류**다. 두 파일을 같은 항목에서 함께 고쳐야 한다 — 워크스페이스가 갈린다는 이유로 쪼개면 중간 상태가 빌드되지 않는다. `event-catalog.test.mjs`·`metadata.test.mjs`도 함께 본다. **담당: main-loop.** dev 로스터의 쓰기 범위 밖이다 — `.claude/agents/web-dev.md:54` `- \`packages/db/prisma/schema.prisma\` 수정`이 금지 목록에 있고, `:50`이 "계획서에 적혔고 사용자가 승인했더라도 범위는 넓어지지 않는다"고 못 박았으며, `:53`이 `db:push`/`db:migrate` 실행도 금지한다. FEAT-19·FEAT-26 전례대로 main-loop가 계획·구현. **마이그레이션은 1회로 묶는다** — 캡션 컬럼도 여기서 같이 만들고 FEAT-42까지 안 쓴 채 둔다(둘로 나누면 Neon 작업이 두 번이 된다). **선행 없음.** FEAT-39의 선행이다.

- [ ] **FEAT-39**: 설정 화면 + 업로드 기본값(언어·클립 수·생성 모드) — 이 항목이 끝나면 배포 가능하고 가치가 난다
  - area: apps/web/src/app/dashboard/settings + apps/web/src/fsd/pages/settings + apps/web/src/fsd/pages/dashboard/ui/_component/UploadPodcast.tsx + apps/web/src/fsd/entities/user
  - source: 2026-09-09 소유자와의 설계 대화(FEAT-38 참조). **관측**: FEAT-38 관측 1과 같다 — 폼 상태 셋이 매 페이지 로드마다 상수로 리셋된다. **진단(추정)**: 사용자가 매 업로드에서 실제로 다시 누르는 것은 이 셋이다. 폼에 노출돼 있고 매번 초기화되기 때문이다. 캡션 스타일은 `Review first`를 고른 사람만 만지고 거기엔 이미 "Apply to all clips"가 있다(`widgets/clip-draft-review/model/use-clip-draft-review.ts:181` `const applyStyleMutation = useMutation({`). **그래서 이 셋이 전체 작업량의 일부로 반복 클릭의 대부분을 없앤다** — 이 항목을 캡션 본체(FEAT-42)에서 떼어 앞에 두는 유일한 이유가 그것이다. **요구**: ① `/dashboard/settings` 라우트와 `pages/settings` 슬라이스 신설. 캡션 섹션 자리는 비워두고 FEAT-42가 채운다. ② 서버 액션으로 `User`의 기본값 넷 중 스칼라 셋을 읽고/쓰고/비운다(비움 = null = 시스템 기본). ③ `UploadPodcast.tsx`의 초기값을 상수에서 사용자 기본값(`?? DEFAULT_LANGUAGE` 등)으로 교체. 클립 수는 영상 길이 상한에 걸리면 기존대로 자동 하향 클램프된다(`getMaxFeasibleClipCount`). ④ 계측 호출 — `settings_viewed`, `settings_defaults_saved`(`source: "settings_page"`). **업로드 폼에는 "이 값을 기본으로 저장" 버튼을 붙이지 않는다** — 이번만 바꾼 것과 기본을 바꾼 것이 같은 자리에서 헷갈린다. **선행: FEAT-38**(컬럼과 이벤트가 없으면 못 쓴다). **범위 밖 의존 없음** — 전부 `apps/web` 안이다.

- [ ] **FEAT-42**: 캡션 스타일 기본값 본체 — 설정 화면 캡션 섹션 · 업로드 스냅샷 · 드래프트 시드 · 디스패치 페이로드 · 검토 화면 인라인 저장
  - area: apps/web/src/fsd/pages/settings + apps/web/src/fsd/features/upload/api + apps/web/src/fsd/entities/clip-draft/api + apps/web/src/fsd/widgets/clip-draft-review/ui/_component/CaptionStyleDialog.tsx + apps/web/src/inngest
  - source: 2026-09-09 소유자와의 설계 대화(FEAT-38·40·41과 한 묶음). **관측 1**: 캡션 스타일은 `ClipDraft.captionStyle`(`schema.prisma:170` `captionStyle   Json?`)에만 있고 `reviewBeforeGenerate=true`로 검토 화면에 들어가야만 편집된다. **관측 2**: 설정 화면에서 `CaptionPreviewPlayer`를 그대로 재사용하면 **빈 검은 상자만 그려진다** — `playUrl`이 없으면 두 이펙트가 early return하고(`CaptionPreviewPlayer.tsx:53` `if (!video) return;`, `:63` `if (!video || playUrl === null) return;`) `activeText`가 `""`로 남는다. 영상 태그 자체도 `:112` `{playUrl !== null && (`로 가려진다. **요구**: ① **설정 화면 캡션 섹션** — FEAT-40이 옮긴 편집기를 재사용하고, `playUrl === null`일 때 샘플 단어 배열(영/한 각 1벌, `features/caption-style` 상수)로 `buildCaptionCues`가 만든 **첫 큐를 고정**으로 그리는 정지 미리보기를 추가한다(타이머·정리 로직이 안 생기고, 첫 큐의 단어 수가 곧 `maxWordsPerLine`이라 그 효과도 보인다). 샘플 영상을 `public/`에 넣지 않는 이유: 어떤 영상을 골라도 사용자의 실제 영상이 아니라 검은 배경과 마찬가지로 치우친 판단 근거이고, 진짜 판단은 검토 화면에서 하도록 ③을 넣었다. 언어 기본값을 Korean으로 바꾸면 미리보기 폰트·샘플 문장·기본 크기(122→130)가 함께 바뀌어야 한다 — 언어가 캡션 기본값을 흔든다는 사실이 화면에 드러나는 유일한 자리다. ② **스냅샷·시드·디스패치** — `prepareUpload`가 **서버에서** `User.defaultCaptionStyle`을 읽어 `UploadedFile.captionStyle`에 복사한다(클라이언트는 스타일을 보내지 않으므로 `prepareUpload`에 검증 표면이 늘지 않는다). `createClipDraftsBulk`(`entities/clip-draft/api/index.ts:16`)가 드래프트를 만들 때 그 값을 `captionStyle`에 시드한다 — 그러면 검토 UI·미리보기·"Apply to all"·`matchPresetId`를 **하나도 안 건드려도** `initialValue`가 채워진 값으로 들어온다(상속식 `draft.captionStyle ?? snapshot` 간접층을 안 만드는 이유: 그 층이 틀리면 미리보기가 실렌더와 어긋나고 크레딧을 쓴 뒤에야 드러난다). auto 디스패치 페이로드에 요청 단위 `caption_style`로 스냅샷을 싣는다(백엔드는 render 모드에서 요청 단위 스타일을 쓰지 않는다 — FEAT-41 소유자 결정 2026-09-14. render는 드래프트 스타일만 쓰므로 `captionStyle`이 null인 드래프트(커스텀 클립·Reset)는 언어 기본값으로 렌더된다). ③ **검토 화면 인라인 저장** — 다이얼로그에 "내 기본으로 저장" 추가(계측 `settings_defaults_saved`, `source: "review_dialog"`). 마음에 드는 스타일은 **내 영상 위에서** 정해지므로 그 순간에 캡처할 자리가 필요하다. 함께 `CaptionStyleDialog.tsx:104` `onClick={() => setWorking(null)}`의 Reset을 언어 기본값이 아니라 **업로드 스냅샷**으로 되돌리게 바꾼다(한 줄). 푸터 버튼이 넷→다섯이 되므로 배치는 계획 단계 판단. **선행: FEAT-38·39·40.** **배포 순서: FEAT-41이 먼저** 나가야 요청 단위 필드가 먹는다. **범위 밖 의존**: 없음(스키마는 FEAT-38이 먼저 만든다). **못 덮는 범위(예상)**: 정지 미리보기의 시각적 정합과 실제 렌더 대조는 배포 후 수동 확인이다. **계획 단계 판단**: 계획서가 과하게 커지면 "기본값이 렌더까지 도달"(①②)과 "검토 화면에서 캡처"(③)로 쪼갠다 — 앞쪽만으로 이미 쓸모가 있고 뒤쪽은 작다.

- [ ] **FEAT-44**: 다른 파일의 `main.py` 줄번호를 가리키는 주석·지시문 인용을 함수명·코드 내용 앵커로 교체 — **동작 무변경**
  - area: apps/web/src/fsd/widgets/clip-draft-review + apps/web/src/fsd/shared/config/constants.ts + apps/web/src/app/layout.tsx + apps/backend/translation_fallback.py + .claude/agents/backend-dev.md + .claude/agents/feature-scout.md
  - source: 2026-09-14 FEAT-43 인수에서 소유자와 결정. **관측 1**: FEAT-43이 `main.py` 맨 위에 import 한 줄을 더하자(`main.py:44` `from moment_prompt import build_moment_prompt`) 그 아래를 가리키던 교차 파일 인용이 전부 한 줄씩 어긋났다. 인수 때 `main\.py:\d+` 전역 grep(이력 문서 제외)으로 열거한 결과 코드 주석·지시문에 **30줄**이 남아 있다 — `apps/web/src` 23줄(`caption-preview.ts` 10·`constants.ts` 4·`layout.tsx` 2·`CaptionPreviewPlayer.tsx` 2·`clip-draft-review/ui/index.tsx` 2·`caption-preview.test.mjs` 2·`review-language-notice.ts` 1), `apps/backend/translation_fallback.py` 2줄, `.claude/agents/` 5줄(`backend-dev.md` 1·`feature-scout.md` 4). 메인 루프 소유 문서(두 `CLAUDE.md`·백로그·원장)는 인수 때 이미 교정했다. **관측 2 — 표본(현재 내용 대조)**: `review-language-notice.ts:3` `// (apps/backend/main.py:837 elif selected_language == "Korean" → :840`인데 그 줄은 이제 `main.py:838` `    elif selected_language == "Korean":`. `constants.ts:77` `MARGINV: { top: 200, bottom: 260 }, // main.py:141-142`인데 실제는 `main.py:142` `    "top": 200,`·`:143` `    "bottom": 260,`. `caption-preview.ts:10` `// apps/backend/main.py:287-345 create_subtitles_with_ffmpeg 의 묶기를 그대로 옮긴다.`인데 함수는 `main.py:288` `def create_subtitles_with_ffmpeg(`. **관측 3 — FEAT-43 이전부터 낡은 것**: `clip-draft-review/ui/index.tsx:293` `(main.py:904 "Return exactly TARGET_COUNT moments if possible")` — 그 문장은 FEAT-43 전에도 `:997`이었고 지금은 `main.py`에 없다(`apps/backend/moment_prompt.py:74` `Return exactly TARGET_COUNT moments if possible.`). `backend-dev.md:154` `(`main.py:136`)`은 `resolve_caption_style`을 가리키는데 FEAT-43 전에도 `:157`, 지금은 `main.py:158` `def resolve_caption_style(`. `feature-scout.md:122` `ProcessVideoRequest`(`main.py:26-41`) — 실제 `main.py:47` `class ProcessVideoRequest(BaseModel):`. **진단(추정)**: 원천 파일이 한 줄만 바뀌어도 교차 파일 줄번호 인용은 전부 조용히 어긋나고, 아무 게이트도 이를 잡지 않는다. 다음 `main.py` 수정(FEAT-41)이 또 민다. `caption-preview.ts`류 주석은 "백엔드 자막 로직의 어느 부분을 이식했나"라는 계약의 연결이라, 에이전트가 계획 때 실제로 따라가다 다른 코드를 읽게 된다. **결정(대화에서 확정)**: 줄번호를 다시 맞추지 않는다 — 곧 또 낡는다. **함수명(+필요하면 그 줄의 짧은 코드 인용)으로 교체**한다(예: `// main.py create_subtitles_with_ffmpeg — 잔여 단어 flush`). 백로그 비고의 "줄 내용을 함께 인용한다"와 같은 원리다. **요구**: ① 위 30줄의 인용을 앵커로 교체. 가리키는 대상 자체가 옮겨간 곳(`index.tsx:293`)은 올바른 파일로 바꾼다. ② `translation_fallback.py:21·45`의 `(기존 main.py:516-524와 동치`처럼 **리팩터링 이전 코드를 가리키는 역사적 서술**은 교체할지 "기존" 서술로 남길지 계획 단계에서 줄마다 판정한다. ③ 코드·테스트 동작 무변경 — 주석과 지시문 문장만. 게이트: `npm run check -w apps/web` · `npm test -w apps/web` · `python -m unittest discover -s apps/backend -p "test_*.py"`. ④ 완료 시 `main\.py:\d+` 전역 grep을 다시 돌려 area 안 잔존 0을 보인다. **제외**: `packages/db/prisma/schema.prisma:116` `    // 백엔드가 클립마다 생성해 성공 콜백에 싣는 선택 근거다(main.py:1182-1184).` — 주석 한 줄에도 생성 클라이언트 사본 4곳 재생성이 따르므로 다음에 스키마를 바꾸는 항목에서 함께 고친다. `docs/plans`·`docs/agents`·`docs/proposals`·완료 문서의 인용은 작성 시점 기록이라 제외(`docs/agents/README.md` 「감사 대상이 아니다」와 같은 이유). **담당: main-loop.** 쓰기 범위가 web-dev(`apps/web`)·backend-dev(`apps/backend`)·어느 dev도 못 쓰는 `.claude/agents/`로 갈린다 — FEAT-19·FEAT-26·FEAT-38 전례. **순서 제약**: FEAT-40이 `caption-preview.ts`·`caption-preview.test.mjs`를 옮기고 "옮긴 테스트 diff는 경로 변경뿐"을 인수 기준으로 삼으므로 **FEAT-40과 동시 진행하지 않는다** — 먼저 끝난 쪽의 결과를 뒤쪽이 기준으로 삼는다. **선행 없음. 급하지 않다** — 동작 영향이 없어 우선순위는 pm 판단.

## Pipeline 운영 / 검증 하니스

- [ ] **FEAT-27**: 계획서 검증 하니스 — `verification-paths.md` 9경로를 deterministic/hybrid/reasoning으로 분류하고, 기계 재현 가능한 경로만 script/tool로 구현
  - area: scripts/verify-plan (신설, 루트) + docs/plans/verification-paths.md + .claude/agents/plan-verifier.md
  - source: 사용자 발주(2026-08-27 세션, 브리프 원문은 아래 「요구」). **관측**: 카탈로그의 9경로(`docs/plans/verification-paths.md:17-29`)는 매 검증 라운드 사람(메인 루프·`plan-verifier`)이 손으로 실행한다 — FEAT-22 기록에서 경로 1 인용 전수는 인용 줄을 하나씩 눈으로 대조(`docs/agents/main-loop/FEAT-22.md:47-48`), 경로 3 before 바이트 일치는 diff 전문 대조(`:172`), 경로 2 조립 게이트는 check·test·verify:fsd:final 수동 실행(`:36`)이었고, 3사이클 동안 같은 경로를 매 라운드 재수행했다. `plan-verifier`는 도구가 없어 하니스(스케치 추출·돌연변이·렌더)를 매번 스크래치패드에 재조립한다(`.claude/agents/plan-verifier.md:31`). 저장소 안 기계 검증 선례는 `apps/admin/scripts/verify-fsd-boundaries.mjs` 하나 — 종료코드 PASS/FAIL(`:722` `process.exitCode = 1`), `--final` 모드(`:708`), 규칙 ID별 메시지, 셀프테스트 13건(`verify-fsd-boundaries.test.mjs:21-196`, 음성 픽스처 포함)을 갖춰 이 항목이 요구하는 script/tool 성질의 저장소 내 모델이다. **진단(추정 — 분류 확정은 계획 단계)**: 9경로는 성격이 셋으로 갈린다. *deterministic*(입력이 계획서+트리뿐, 판단 불요): 1 인용 전수 대조의 "줄 존재 + 인용 내용 일치" 부분, 3 before/after 기계 적용(before 블록 바이트 일치·patch dry-run), 9 구조적 아티팩트 검사(파서), 7 음성 시험 중 검사가 스크립트인 경우(규칙 제거→검사 실패 확인). *hybrid*(대상·설정 선택은 판단, 실행·판정은 기계): 2 스케치 추출·실행(어느 워크스페이스 설정으로 어떤 파일에 실행할지), 5 돌연변이(명세→테스트 변환은 reasoning, 주입·사멸 판정은 기계), 8 실물 렌더(대상 선택 reasoning, `renderToStaticMarkup` 실행 기계). *reasoning*(의미 해석 본체): 4 전칭 여집합 열거(주장 발췌·여집합 설계), 6 실제 사건 재생(실측 데이터 선택·의미 판정), 그리고 hybrid의 판단 절반. 현재는 세 부류가 구분 없이 산문 증거로만 남아 라운드마다 비용과 편차가 생긴다. **요구(발주 원문 요지)**: ① `verification-paths.md`는 에이전트가 읽는 source of truth로 유지하고 전체를 하나의 script PASS/FAIL 시스템으로 바꾸지 않는다. ② 9경로를 deterministic/hybrid/reasoning으로 분류한 뒤 기계적으로 재현 가능한 path만 script/tool로 구현한다. ③ script/tool은 동일 입력→동일 결과 · exit code 기반 PASS/FAIL · evidence 출력 · negative fixture/self-test를 가진다. ④ 의미 해석이 필요한 검증은 `plan-verifier`에 남긴다. ⑤ 최종 verdict는 mechanical evidence + reasoning evidence를 종합하는 구조로 만든다. ⑥ 새 verification agent는 추가하지 않고 현재 main loop + `plan-verifier` 구조를 유지한다. ⑦ 기존 `apps/admin`의 `verify:fsd` 패턴 등 재사용 가능한 구현을 먼저 조사한다. ⑧ 절차: 현재 구조 분석 → 변경 계획 제시 → 구현 → 변경 파일·검증 결과·남은 reasoning-only 영역 보고(파이프라인상 계획서=분석+계획, 구현 보고=③④). **설계 시 판단**: 카탈로그 표에는 열 추가(분류·도구 명령)만 하고 행 제거는 하지 않는다(행 제거는 사용자 승인만 — `verification-paths.md:43`). 종합 구조는 새 파일이 아니라 기존 기록 자리에 둔다 — `docs/agents/main-loop/<항목ID>.md` 라운드 기록과 `plan-verifier` 보고 템플릿(`plan-verifier.md:65-82` [실행한 경로])에 mechanical 증거(도구 출력·종료코드)와 reasoning 증거를 나눠 적는 형식. 도구는 루트 `scripts/verify-plan/`에 두고 계획서 경로를 인자로 받는다(계획서는 backend·web·admin 공통이라 워크스페이스 안에 두지 않음). **담당**: 루트 `scripts/`·`docs/plans/`·`.claude/agents/`는 dev 로스터의 쓰기 범위 밖 — FEAT-19·FEAT-26 전례대로 main-loop가 계획·구현. **범위 밖 의존**: `plan-verifier`가 도구를 쓰려면 브리핑에 명령이 실려야 하고 스크래치패드 제약(`plan-verifier.md:31·36`)과 충돌하지 않아야 한다 — 도구는 저장소 파일을 읽기만 하고 출력은 stdout이라 충돌 없음을 계획서에서 확인. 경로 9는 실증 사례가 아직 없어(`verification-paths.md:29`) 첫 fixture가 곧 첫 실증이 된다. **경로 1의 실측 사례 추가(2026-09-05)**: 이 세션에서 인용 드리프트가 네 건 나왔고 전부 사람이 손으로 잡았다 — ①② 백로그의 `upload-detail :20,21`·`OrderHistory :16`이 각각 `:21,22`·`:17`로 밀림(원인: BUG-10이 같은 파일에 임포트 추가), ③ FEAT-31 계획서의 선례 인용 `uploaded-file/index.ts:1-55`가 실제 54행, ④ FEAT-33 계획서의 `public-header.tsx:15-19`가 실제 16-19행(15행은 빈 주석줄). 넷 다 **줄 존재가 아니라 내용 대조로만** 잡히는 부류이고, 넷 다 기계적으로 판정 가능하다(인용 문자열이 그 줄에 있는가). ③④는 독립 패스 두 사이클을 더 쓰게 만들었다 — 경로 1의 자동화가 아끼는 것은 사람의 눈이 아니라 **사이클 수**다.

## 비고

- 위 항목의 우선순위는 아직 정해지지 않음 — PM 에이전트가 매일 이 목록에서 오늘 처리할 1~2개를 골라 PROJECT_BOARD.md에 "승인대기"로 기록한다.
- 새 이슈가 생기면 이 파일 하단에 추가한다 (형식: ID, 설명, area, source).
- `source`를 쓸 때는 **관측과 진단을 분리한다.** 무엇을 봤는지(관측)와 왜 그렇다고 생각하는지(진단)를 한 문장에 섞지 않고, 진단은 추정임을 표시한다. FEAT-02가 실제로 이렇게 틀렸다 — 관측("10분 소스에 4개 요청 시 적게 생성됨")은 맞았는데 진단("유저가 무리한 개수를 골라서")이 틀렸고, 둘이 섞여 있어 담당 에이전트가 틀린 진단을 요구사항으로 받아 계획을 세 번 썼다. 반대로 BUG-06은 `파일:줄` 근거, 이미 맞는 곳, 수정 방향까지 적혀 있어 계획이 한 번에 끝났다.
- **`파일:줄`을 쓸 때는 그 줄의 내용도 함께 인용한다.** 백로그 항목은 몇 주를 살고 그 사이 우리 커밋이 줄을 민다 — 줄번호는 낡지만 내용은 안 낡는다. 2026-09-05 FEAT-33 계획 단계에서 실측: 이 파일이 적은 `upload-detail/ui/index.tsx:20,21`과 `OrderHistory.tsx:16`이 각각 `:21,22`·`:17`로 밀려 있었고, 원인은 우리 자신의 작업이었다(BUG-10이 두 파일에 `formatDate` 임포트를 추가하면서 아래 줄이 한 칸씩 내려갔다). 내용이 함께 적혀 있었다면 담당 에이전트가 줄번호 대신 문자열로 찾아 드리프트를 즉시 알아봤을 것이다. 이 대조는 FEAT-27이 기계화하려는 경로 1(인용 전수 대조)의 몫이기도 하다.
